diff --git a/frontend/src/features/decision-schedule/DecisionScheduleReviewView.css b/frontend/src/features/decision-schedule/DecisionScheduleReviewView.css
index b8f7f0a..1ecbcfe 100644
--- a/frontend/src/features/decision-schedule/DecisionScheduleReviewView.css
+++ b/frontend/src/features/decision-schedule/DecisionScheduleReviewView.css
@@ -1,52 +1,48 @@
-/* 문서 상세 분석 탭의 단일 액션 아이템 검토 패널. 종류는 카드 배지로 구분한다. */
-.decision-schedule-panel { display: grid; gap: 14px; margin-top: 24px; }
-.decision-schedule-panel__heading { display: flex; align-items: flex-start; justify-content: space-between; gap: 18px; padding: 18px 20px; border: 1px solid #d1d1d1; border-bottom: 2px solid #111; background: #fff; }
-.decision-schedule-panel__heading > div:first-child > span { color: #666; font-size: 11px; font-weight: 800; letter-spacing: .08em; text-transform: uppercase; }
-.decision-schedule-panel__heading h2 { margin: 5px 0 0; color: #111; font-size: 18px; }
-.decision-schedule-panel__heading p { margin: 7px 0 0; color: #555; }
-.review-summary { display: flex; align-items: flex-start; gap: 14px; }
-.review-summary dl { display: flex; gap: 18px; margin: 0; }
-.review-summary dl div { min-width: 72px; text-align: right; }
-.review-summary dt { color: #666; font-size: 12px; }
-.review-summary dd { margin: 3px 0 0; color: #111; font-size: 20px; font-weight: 800; }
-.review-summary > strong { padding: 4px 8px; border: 1px solid #999; border-radius: 999px; color: #333; font-size: 11px; white-space: nowrap; }
-.detail-card.action-item-review { padding: 0; overflow: hidden; border: 1px solid #d1d1d1; background: #fff; }
-.review-group { padding: 18px 22px; border-bottom: 1px solid #ddd; }
-.review-group-heading { display: flex; align-items: baseline; justify-content: space-between; gap: 16px; margin-bottom: 12px; }
-.review-group h3, .review-edit h4 { margin: 0; color: #111; }
-.review-group-heading span, .review-limit { color: #666; font-size: 12px; }
-.review-card-list { display: grid; gap: 9px; }
-.review-card { display: flex; align-items: center; justify-content: space-between; gap: 18px; padding: 14px 15px; border: 1px solid #d5d5d5; background: #fff; }
-.review-card.is-stale { background: #f1f1f1; border-style: dashed; }
+/* 문서 상세 오른쪽의 압축 액션 아이템 검토 패널. 상태별 목록을 한 번에 하나만 보여준다. */
+.decision-schedule-panel { display: grid; margin-top: 24px; overflow: hidden; border: 1px solid #d6dce8; border-radius: 14px; background: #fff; box-shadow: 0 8px 24px rgba(27, 36, 54, .06); }
+.decision-schedule-panel__heading { padding: 16px 18px 10px; }
+.review-title-line { display: flex; align-items: flex-start; justify-content: space-between; gap: 12px; }
+.review-title-line > div > span { color: #727d91; font-size: 10px; font-weight: 800; letter-spacing: .08em; text-transform: uppercase; }
+.review-title-line h2 { display: flex; align-items: center; gap: 8px; margin: 4px 0 0; color: #111827; font-size: 18px; }
+.review-title-line h2 b { display: inline-grid; min-width: 22px; height: 22px; place-items: center; border-radius: 999px; background: #eef0ff; color: var(--c-accent, #554bd8); font-size: 11px; }
+.review-title-line > strong { padding: 4px 8px; border: 1px solid #a8afbc; border-radius: 999px; color: #4b5565; font-size: 11px; white-space: nowrap; }
+.decision-schedule-panel__heading > p { margin: 7px 0 0; color: #687386; font-size: 12px; }
+.review-state-tabs { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 6px; padding: 0 14px 12px; border-bottom: 1px solid #e2e6ed; }
+.review-state-tabs button { display: flex; align-items: center; justify-content: center; gap: 6px; min-width: 0; padding: 8px 7px; border: 1px solid #d8dde7; border-radius: 8px; background: #fff; color: #5c6678; font: inherit; font-size: 12px; cursor: pointer; }
+.review-state-tabs button b { font-size: 11px; }
+.review-state-tabs button.is-active { border-color: var(--c-accent, #554bd8); background: var(--c-accent-soft, #eeedff); color: var(--c-accent, #554bd8); font-weight: 800; }
+.action-item-review { min-height: 150px; padding: 14px; background: #f7f8fb; }
+.review-list-body { min-width: 0; }
+.review-card-list { display: grid; gap: 10px; }
+.review-card { display: grid; gap: 12px; padding: 14px; border: 1px solid #dbe0e8; border-radius: 12px; background: #fff; box-shadow: 0 2px 8px rgba(28, 39, 60, .04); }
+.review-card.is-stale { background: #f2f3f5; border-style: dashed; }
 .review-card-main { min-width: 0; }
-.review-card-title { display: flex; align-items: center; flex-wrap: wrap; gap: 7px; }
-.review-card-title strong { color: #111; font-size: 15px; }
-.review-card-title span { padding: 2px 7px; border: 1px solid #aaa; border-radius: 999px; color: #333; font-size: 11px; }
-.review-card-title .review-card-kind { border-color: #111; background: #111; color: #fff; font-weight: 800; }
-.review-card-main p { margin: 7px 0; color: #333; white-space: pre-wrap; }
-.review-card-main small { display: block; color: #666; }
-.review-card-main em { display: block; margin-top: 7px; color: #111; font-style: normal; font-weight: 700; }
-.review-actions { display: flex; flex: 0 0 auto; gap: 7px; }
-.review-actions button, .review-edit button, .review-rejected-toggle { padding: 7px 11px; border: 1px solid #777; border-radius: 5px; background: #fff; color: #111; cursor: pointer; }
-.review-actions button.is-primary, .review-edit button.is-primary { border-color: #111; background: #111; color: #fff; }
+.review-card-title { display: flex; align-items: center; gap: 8px; min-width: 0; }
+.review-card-title strong { min-width: 0; overflow: hidden; color: #111827; font-size: 14px; text-overflow: ellipsis; white-space: nowrap; }
+.review-card-title .review-card-kind { flex: 0 0 auto; padding: 3px 7px; border-radius: 999px; background: var(--c-accent-soft, #eeedff); color: var(--c-accent, #554bd8); font-size: 10px; font-weight: 800; }
+.review-card-meta { display: flex; flex-wrap: wrap; gap: 5px; margin-top: 9px; }
+.review-card-meta span { padding: 3px 7px; border-radius: 999px; background: #f0f2f6; color: #5b6577; font-size: 10px; font-weight: 700; }
+.review-card-main > p { display: -webkit-box; margin: 9px 0 0; overflow: hidden; -webkit-box-orient: vertical; -webkit-line-clamp: 3; color: #394354; font-size: 12px; line-height: 1.55; white-space: pre-wrap; }
+.review-card-reason { display: -webkit-box; margin-top: 9px; overflow: hidden; -webkit-box-orient: vertical; -webkit-line-clamp: 2; color: #626d7f; font-size: 11px; line-height: 1.5; }
+.review-card-reason b { margin-right: 6px; color: #303a4b; }
+.review-card-source { display: block; margin-top: 5px; overflow: hidden; color: #8a93a2; font-size: 10px; text-overflow: ellipsis; white-space: nowrap; }
+.review-card-main em { display: block; margin-top: 8px; color: #323b49; font-size: 11px; font-style: normal; font-weight: 700; }
+.review-actions { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 7px; }
+.review-actions button, .review-edit button { padding: 8px 10px; border: 1px solid #cdd3de; border-radius: 7px; background: #fff; color: #263043; font: inherit; font-size: 12px; font-weight: 700; cursor: pointer; }
+.review-actions button:only-child { grid-column: 1 / -1; }
+.review-actions button.is-primary, .review-edit button.is-primary { border-color: var(--c-accent, #554bd8); background: var(--c-accent, #554bd8); color: #fff; }
 .review-actions button:disabled, .review-edit button:disabled { opacity: .45; cursor: not-allowed; }
-.review-empty { margin: 0; padding: 15px; background: #f7f7f7; color: #666; text-align: center; }
-.review-limit { margin: 10px 0 0; }
-.review-rejected { padding: 16px 22px; background: #f7f7f7; }
-.review-rejected-toggle { width: 100%; text-align: left; font-weight: 700; }
-.review-rejected .review-card-list { margin-top: 10px; }
-.review-edit { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 12px; padding: 16px; border: 2px solid #111; background: #fafafa; }
-.review-edit h4, .review-edit p, .review-edit > div { grid-column: 1 / -1; }
-.review-edit label { display: grid; gap: 5px; color: #333; font-size: 12px; }
-.review-edit input, .review-edit select, .review-edit textarea { width: 100%; box-sizing: border-box; padding: 9px; border: 1px solid #999; background: #fff; color: #111; font: inherit; }
+.review-empty { display: grid; min-height: 120px; margin: 0; place-items: center; border: 1px dashed #d6dbe4; border-radius: 10px; background: #fff; color: #7b8493; font-size: 12px; text-align: center; }
+.review-limit { margin: 10px 0 0; color: #6c7687; font-size: 11px; }
+.review-edit { display: grid; grid-template-columns: 1fr; gap: 10px; padding: 14px; border: 1px solid var(--c-accent, #554bd8); border-radius: 10px; background: #fff; }
+.review-edit h4, .review-edit p, .review-edit > div { margin: 0; }
+.review-edit label { display: grid; gap: 5px; color: #394354; font-size: 11px; }
+.review-edit input, .review-edit select, .review-edit textarea { width: 100%; box-sizing: border-box; padding: 8px; border: 1px solid #bbc2cf; border-radius: 6px; background: #fff; color: #111827; font: inherit; }
 .review-edit textarea { resize: vertical; }
-.review-edit p { margin: 0; color: #555; font-size: 12px; }
-.review-edit > div { display: flex; gap: 8px; }
+.review-edit p { color: #6b7484; font-size: 11px; }
+.review-edit > div { display: grid; grid-template-columns: 1fr 1fr; gap: 7px; }
 @media (max-width: 760px) {
-  .decision-schedule-panel__heading, .review-card, .review-group-heading { align-items: stretch; flex-direction: column; }
-  .review-summary { justify-content: space-between; }
-  .review-summary dl div { text-align: left; }
-  .review-actions { width: 100%; }
-  .review-actions button { flex: 1; }
-  .review-edit { grid-template-columns: 1fr; }
+  .review-title-line { align-items: stretch; flex-direction: column; }
+  .review-state-tabs { grid-template-columns: 1fr; }
+  .review-state-tabs button { justify-content: space-between; }
 }
diff --git a/frontend/src/features/decision-schedule/DecisionScheduleReviewView.jsx b/frontend/src/features/decision-schedule/DecisionScheduleReviewView.jsx
index ba89d42..3609a40 100644
--- a/frontend/src/features/decision-schedule/DecisionScheduleReviewView.jsx
+++ b/frontend/src/features/decision-schedule/DecisionScheduleReviewView.jsx
@@ -63,7 +63,7 @@ export default function DecisionScheduleReviewPanel({ projectId, documentId, can
   const queryClient = useQueryClient()
   const [editing, setEditing] = useState(null)
   const [rejectTarget, setRejectTarget] = useState(null)
-  const [rejectedOpen, setRejectedOpen] = useState(false)
+  const [activeState, setActiveState] = useState('pending')
   const decisionReview = useResourceQueries(RESOURCES[0], projectId, documentId)
   const scheduleReview = useResourceQueries(RESOURCES[1], projectId, documentId)
   const reviews = [decisionReview, scheduleReview]
@@ -74,6 +74,13 @@ export default function DecisionScheduleReviewPanel({ projectId, documentId, can
   const pendingQueries = reviews.map(review => review.pending)
   const approvedQueries = reviews.map(review => review.approved)
   const rejectedQueries = reviews.map(review => review.rejected)
+  const stateViews = {
+    pending: { label: '승인 대기', items: pendingItems, queries: pendingQueries, empty: '승인 대기 액션 아이템이 없습니다.' },
+    approved: { label: '반영됨', items: approvedItems, queries: approvedQueries, empty: '산출물에 반영된 액션 아이템이 없습니다.' },
+    rejected: { label: '거절됨', items: rejectedItems, queries: rejectedQueries, empty: '거절된 액션 아이템이 없습니다.' },
+  }
+  const activeView = stateViews[activeState]
+  const totalItems = Object.values(stateViews).reduce((sum, view) => sum + sumTotal(view.queries), 0)
 
   const invalidate = resource => {
     const workspaceProjectId = Number(projectId)
@@ -117,47 +124,40 @@ export default function DecisionScheduleReviewPanel({ projectId, documentId, can
 
   return <section className='decision-schedule-panel' aria-label='이 문서에서 추출한 액션 아이템'>
     <header className='decision-schedule-panel__heading'>
-      <div>
-        <span>AI 추출 결과</span>
-        <h2>추출된 액션 아이템</h2>
-        <p>문서에서 찾은 결정사항과 일정입니다. 카드의 종류를 확인한 뒤 승인하거나 수정해 주세요.</p>
-      </div>
-      <div className='review-summary'>
-        <dl>
-          <div><dt>승인 대기</dt><dd>{sumTotal(pendingQueries)}</dd></div>
-          <div><dt>반영 중</dt><dd>{sumTotal(approvedQueries)}</dd></div>
-        </dl>
+      <div className='review-title-line'>
+        <div><span>AI 추출 결과</span><h2>추출된 액션 아이템 <b>{totalItems}</b></h2></div>
         {!canEdit && <strong>읽기 전용</strong>}
       </div>
+      <p>결정사항과 일정을 카드별로 검토합니다.</p>
     </header>
 
-    <section className='detail-card action-item-review'>
-      <ReviewGroup title='승인 대기' note='카드마다 종류와 근거를 확인할 수 있습니다.' queries={pendingQueries} items={pendingItems} empty='승인 대기 액션 아이템이 없습니다.'>
-        {pendingItems.map(item => <ReviewCard
-          key={itemKey(item)}
-          item={item}
-          canEdit={canEdit}
-          disabled={reviewLocked}
-          busy={busyKey === itemKey(item)}
-          actions='pending'
-          onApprove={() => actionMutation.mutate({ item, action: 'approve' })}
-          onReject={() => setRejectTarget(item)}
-        />)}
-      </ReviewGroup>
+    <nav className='review-state-tabs' aria-label='액션 아이템 상태'>
+      {Object.entries(stateViews).map(([key, view]) => <button
+        type='button'
+        key={key}
+        className={activeState === key ? 'is-active' : ''}
+        aria-pressed={activeState === key}
+        onClick={() => { setActiveState(key); setEditing(null) }}
+      ><span>{view.label}</span><b>{sumTotal(view.queries)}</b></button>)}
+    </nav>
 
-      <ReviewGroup title='산출물 반영 항목' note='승인 또는 수정 승인된 카드만 산출물에 반영됩니다.' queries={approvedQueries} items={approvedItems} empty='승인된 액션 아이템이 없습니다.'>
-        {approvedItems.map(item => <ReviewCard
+    <section className='action-item-review'>
+      <ReviewList queries={activeView.queries} items={activeView.items} empty={activeView.empty}>
+        {activeView.items.map(item => <ReviewCard
           key={itemKey(item)}
           item={item}
           canEdit={canEdit}
           disabled={reviewLocked}
           busy={busyKey === itemKey(item) || (updateMutation.isPending && itemKey(updateMutation.variables?.item) === itemKey(item))}
-          actions='approved'
+          actions={activeState}
           editing={editingKey === itemKey(item)}
+          onApprove={() => actionMutation.mutate({ item, action: 'approve' })}
+          onReject={() => setRejectTarget(item)}
           onEdit={() => setEditing(item)}
           onCancel={() => actionMutation.mutate({ item, action: 'cancel' })}
+          onRestore={() => actionMutation.mutate({ item, action: 'cancel' })}
         />)}
-        {editing && <EditForm
+        {activeState === 'approved' && editing && <EditForm
           key={`edit-${itemKey(editing)}`}
           type={editing.resource.key}
           row={editing.row}
@@ -166,23 +166,7 @@ export default function DecisionScheduleReviewPanel({ projectId, documentId, can
           onClose={() => setEditing(null)}
           onSave={changes => updateMutation.mutate({ item: editing, changes })}
         />}
-      </ReviewGroup>
-
-      <RejectedItems
-        items={rejectedItems}
-        queries={rejectedQueries}
-        open={rejectedOpen}
-        onToggle={() => setRejectedOpen(value => !value)}
-        renderItem={item => <ReviewCard
-          key={itemKey(item)}
-          item={item}
-          canEdit={canEdit}
-          disabled={reviewLocked}
-          busy={busyKey === itemKey(item)}
-          actions='rejected'
-          onRestore={() => actionMutation.mutate({ item, action: 'cancel' })}
-        />}
-      />
+      </ReviewList>
     </section>
 
     <ConfirmDialog
@@ -232,53 +216,41 @@ function itemKey(item) {
   return item ? `${item.resource.key}-${item.row.id}` : null
 }
 
-function ReviewGroup({ title, note, queries, items, empty, children }) {
+function ReviewList({ queries, items, empty, children }) {
   const pending = queries.some(query => query.isPending)
   const errors = queries.filter(query => query.isError)
   const total = sumTotal(queries)
   const returned = queries.reduce((sum, query) => sum + (query.data?.returned ?? 0), 0)
   const truncated = queries.some(query => query.data?.truncated)
-  return <section className='review-group'>
-    <div className='review-group-heading'><h3>{title}</h3><span>{note}</span></div>
+  return <div className='review-list-body'>
     {pending && <p className='review-empty'>불러오는 중입니다.</p>}
     {errors.map((query, index) => <p className='review-empty' key={index}>일부 항목을 불러오지 못했습니다. {query.error?.message}</p>)}
     {!pending && errors.length === 0 && items.length === 0 && <p className='review-empty'>{empty}</p>}
     {items.length > 0 && <div className='review-card-list'>{children}</div>}
     {truncated && <p className='review-limit'>전체 {total}건 중 {returned}건만 보여줍니다.</p>}
-  </section>
-}
-
-function RejectedItems({ items, queries, open, onToggle, renderItem }) {
-  const pending = queries.some(query => query.isPending)
-  const errors = queries.filter(query => query.isError)
-  if (!pending && errors.length === 0 && items.length === 0) return null
-  return <div className='review-rejected'>
-    {pending && <p className='review-empty'>거절된 항목을 불러오는 중입니다.</p>}
-    {errors.map((query, index) => <p className='review-empty' key={index}>일부 거절 항목을 불러오지 못했습니다. {query.error?.message}</p>)}
-    {items.length > 0 && <>
-      <button type='button' className='review-rejected-toggle' aria-expanded={open} onClick={onToggle}>
-        {open ? '거절된 항목 숨기기' : `거절된 항목 보기 (${sumTotal(queries)}건)`}
-      </button>
-      {open && <div className='review-card-list'>{items.map(renderItem)}</div>}
-    </>}
   </div>
 }
 
 function ReviewCard({ item, canEdit, disabled, busy, actions, editing, onApprove, onReject, onEdit, onCancel, onRestore }) {
   const { resource, row } = item
   const locked = disabled || busy || !canEdit
+  const confidence = confidenceLabel(row.confidence)
+  const scheduleDate = resource.key === 'schedule' ? scheduleDates(row) : null
   return <article className={`review-card${row.stale ? ' is-stale' : ''}`}>
     <div className='review-card-main'>
       <div className='review-card-title'>
         <span className='review-card-kind'>{itemTypeLabel(item)}</span>
         <strong>{row.title}</strong>
+      </div>
+      <div className='review-card-meta'>
         <span>{DECISION_LABELS[row.decision] ?? row.decision}</span>
         {resource.key === 'decision' && <span>{STATUS_LABELS[row.status] ?? row.status}</span>}
+        {scheduleDate && <span>{scheduleDate}</span>}
+        {confidence && <span>{confidence}</span>}
       </div>
-      {resource.key === 'decision'
-        ? <p>{row.content || '세부 내용 없음'}</p>
-        : <p>{scheduleDates(row)}</p>}
-      <small>{row.filename || '출처 문서 삭제됨'} · 근거: {row.reason}</small>
+      {resource.key === 'decision' && row.content && <p>{row.content}</p>}
+      {row.reason && <small className='review-card-reason'><b>근거</b>{row.reason}</small>}
+      {row.filename && <small className='review-card-source'>{row.filename}</small>}
       {row.stale && <em>{actions === 'rejected'
         ? '원문이 수정된 뒤의 오래된 제안입니다. 다시 분석하기 전에는 되살릴 수 없습니다.'
         : '원문이 수정된 뒤의 오래된 제안입니다. 승인·수정할 수 없지만 거절·취소로 산출물에서 뺄 수 있습니다.'}</em>}
@@ -290,13 +262,21 @@ function ReviewCard({ item, canEdit, disabled, busy, actions, editing, onApprove
       </>}
       {actions === 'approved' && <>
         <button type='button' disabled={locked || row.stale || editing} onClick={onEdit}>{editing ? '수정 중' : '수정'}</button>
-        <button type='button' disabled={locked || editing} onClick={onCancel}>{busy ? '처리 중…' : '취소'}</button>
+        <button type='button' disabled={locked || editing} onClick={onCancel}>{busy ? '처리 중…' : '승인 취소'}</button>
       </>}
       {actions === 'rejected' && <button type='button' disabled={locked || row.stale} onClick={onRestore}>{busy ? '처리 중…' : '되살리기'}</button>}
     </div>}
   </article>
 }
 
+function confidenceLabel(value) {
+  if (value === null || value === undefined || value === '') return null
+  const confidence = Number(value)
+  if (!Number.isFinite(confidence)) return null
+  const percent = confidence <= 1 ? confidence * 100 : confidence
+  return `확신 ${Math.round(percent)}%`
+}
+
 function itemTypeLabel(item) {
   if (item.resource.key === 'decision') return '결정사항'
   return KIND_LABELS[item.row.kind] ?? item.row.kind
@@ -345,7 +325,7 @@ function EditForm({ type, row, saving, notify, onClose, onSave }) {
 }
 
 function scheduleDates(row) {
-  if (!row.starts_on && !row.ends_on) return '날짜 미기재'
+  if (!row.starts_on && !row.ends_on) return null
   if (row.starts_on && row.ends_on) return `${row.starts_on} ~ ${row.ends_on}`
   return row.starts_on ? `시작 ${row.starts_on}` : `종료 ${row.ends_on}`
 }
diff --git a/frontend/src/features/document-detail/DocumentAnalysisTab.jsx b/frontend/src/features/document-detail/DocumentAnalysisTab.jsx
index c1bb907..afcc74c 100644
--- a/frontend/src/features/document-detail/DocumentAnalysisTab.jsx
+++ b/frontend/src/features/document-detail/DocumentAnalysisTab.jsx
@@ -1,4 +1,12 @@
-import { useMemo, useState } from 'react'
+// =============================================================================
+// 이 파일의 책임: 문서의 최신 요약·분류와 과거 분석 이력을 보여준다.
+// 다른 파일과의 관계: DocumentDetailPage의 분석 결과 왼쪽 영역으로 조립되며,
+//   긴 최신 결과는 접어서 오른쪽 액션 아이템과 한 화면에서 비교하게 한다.
+// Spring 비교: 분석 결과 DTO를 표시하는 MVC View이며 재분석·다운로드 이벤트는
+//   상위 Controller 역할의 DocumentDetailPage에 위임한다.
+// =============================================================================
+
+import { useEffect, useMemo, useRef, useState } from 'react'
 import { getAnalysisCategoryLabel } from '../../utils/analysisCategory'
 
 export default function DocumentAnalysisTab({ document, canAnalyze, analyzing, onAnalyze, downloading, onDownload }) {
@@ -14,9 +22,36 @@ export default function DocumentAnalysisTab({ document, canAnalyze, analyzing, o
   </div>
 }
 
-function AnalysisCard({ analysis, textVersion, compact = false }) { return <article className={`detail-card${compact ? ' compact-analysis' : ''}`}><header><h2>{analysis.analyzer_type === 'summary' ? '문서 요약' : '문서 분류'}</h2>{analysis.source_text_revision !== textVersion && <span className="stale-badge">이전 텍스트 기준</span>}</header><AnalysisBody analysis={analysis}/><AnalysisScope result={analysis.result ?? {}}/><footer>{analysis.model_name} · {analysis.prompt_version ?? "이전 프롬프트"} · 텍스트 v{analysis.source_text_revision} · {new Date(analysis.created_at).toLocaleString()}</footer></article> }
+function AnalysisCard({ analysis, textVersion, compact = false }) {
+  const contentRef = useRef(null)
+  const [expanded, setExpanded] = useState(false)
+  const [expandable, setExpandable] = useState(false)
+  const collapsed = !compact && !expanded
+  useEffect(() => {
+    if (compact || expanded || !contentRef.current) return undefined
+    const measure = () => {
+      const content = contentRef.current
+      setExpandable(Boolean(content && content.scrollHeight > content.clientHeight + 1))
+    }
+    measure()
+    window.addEventListener('resize', measure)
+    return () => window.removeEventListener('resize', measure)
+  }, [analysis, compact, expanded])
+  return <article className={`detail-card${compact ? ' compact-analysis' : ''}`}>
+    <header><h2>{analysis.analyzer_type === 'summary' ? '문서 요약' : '문서 분류'}</h2>{analysis.source_text_revision !== textVersion && <span className="stale-badge">이전 텍스트 기준</span>}</header>
+    <AnalysisBody analysis={analysis} collapsed={collapsed} contentRef={contentRef}/>
+    {expandable && <button type="button" className="analysis-expand" aria-expanded={expanded} onClick={() => setExpanded(value => !value)}>{expanded ? '간략히 보기' : '전체 내용 보기'}</button>}
+    <AnalysisScope result={analysis.result ?? {}}/>
+    <footer>{analysis.model_name} · {analysis.prompt_version ?? "이전 프롬프트"} · 텍스트 v{analysis.source_text_revision} · {new Date(analysis.created_at).toLocaleString()}</footer>
+  </article>
+}
 function AnalysisHistory({ analyses, type, textVersion }) { return <section className="analysis-history"><h3>과거 {type === 'summary' ? '요약' : '분류'} 기록</h3>{analyses.length ? analyses.map(item => <AnalysisCard key={item.id} analysis={item} textVersion={textVersion} compact/>) : <p>과거 기록이 없습니다.</p>}</section> }
-function AnalysisBody({ analysis }) { const result = analysis.result ?? {}; if (analysis.analyzer_type === 'summary') return <p className="analysis-copy">{result.summary ?? '요약 내용이 없습니다.'}</p>; if (analysis.analyzer_type === 'category') return <dl className="category-result"><dt>분류</dt><dd>{getAnalysisCategoryLabel(result.category)}</dd><dt>근거</dt><dd>{result.reason ?? '-'}</dd></dl>; return <pre>{JSON.stringify(result, null, 2)}</pre> }
+function AnalysisBody({ analysis, collapsed = false, contentRef }) {
+  const result = analysis.result ?? {}
+  if (analysis.analyzer_type === 'summary') return <p ref={contentRef} className={`analysis-copy${collapsed ? ' is-collapsed' : ''}`}>{result.summary ?? '요약 내용이 없습니다.'}</p>
+  if (analysis.analyzer_type === 'category') return <dl className="category-result"><dt>분류</dt><dd>{getAnalysisCategoryLabel(result.category)}</dd><dt>근거</dt><dd ref={contentRef} className={collapsed ? 'analysis-reason is-collapsed' : 'analysis-reason'}>{result.reason ?? '-'}</dd></dl>
+  return <pre>{JSON.stringify(result, null, 2)}</pre>
+}
 function groupAnalyses(analyses) { return analyses.reduce((result, item) => { (result[item.analyzer_type] ??= []).push(item); result[item.analyzer_type].sort((a, b) => new Date(b.created_at) - new Date(a.created_at) || b.id - a.id); return result }, {}) }
 
 function AnalysisScope({ result }) {
diff --git a/frontend/src/styles/document-detail-updates.css b/frontend/src/styles/document-detail-updates.css
index f256d1f..e28042b 100644
--- a/frontend/src/styles/document-detail-updates.css
+++ b/frontend/src/styles/document-detail-updates.css
@@ -29,6 +29,33 @@
   cursor: pointer;
 }
 
+.analysis-copy.is-collapsed,
+.analysis-reason.is-collapsed {
+  display: -webkit-box;
+  overflow: hidden;
+  -webkit-box-orient: vertical;
+}
+
+.analysis-copy.is-collapsed {
+  -webkit-line-clamp: 8;
+}
+
+.analysis-reason.is-collapsed {
+  -webkit-line-clamp: 6;
+}
+
+.analysis-expand {
+  margin: 10px 0 0;
+  padding: 0;
+  border: 0;
+  background: transparent;
+  color: var(--c-accent, #554bd8);
+  font: inherit;
+  font-size: 12px;
+  font-weight: 800;
+  cursor: pointer;
+}
+
 .analysis-footer-actions {
   display: flex;
   justify-content: flex-end;
@@ -138,9 +165,9 @@
 /* 분석 결과는 목업처럼 기존 요약·분류와 추출 액션 아이템을 나란히 배치한다. */
 .document-analysis-layout {
   display: grid;
-  grid-template-columns: minmax(0, .82fr) minmax(0, 1.18fr);
+  grid-template-columns: minmax(0, 1.6fr) minmax(360px, 1fr);
   align-items: start;
-  gap: 16px;
+  gap: 18px;
 }
 
 .document-analysis-layout > * {
@@ -156,7 +183,7 @@
   overflow-wrap: anywhere;
 }
 
-@media (max-width: 1340px) {
+@media (max-width: 1160px) {
   .document-analysis-layout {
     grid-template-columns: minmax(0, 1fr);
   }
