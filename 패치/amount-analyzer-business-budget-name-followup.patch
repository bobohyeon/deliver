diff --git a/backend/app/analyzers/amount_analyzer.py b/backend/app/analyzers/amount_analyzer.py
index 14ba27b..0eda6f1 100644
--- a/backend/app/analyzers/amount_analyzer.py
+++ b/backend/app/analyzers/amount_analyzer.py
@@ -14,7 +14,7 @@ from app.core.config import settings
 from app.schemas.amount import AmountExtractionOut
 from app.services.amount_normalizer import normalize_number
 
-AMOUNT_PROMPT_VERSION = "amount-v5"
+AMOUNT_PROMPT_VERSION = "amount-v6"
 _AMOUNT_STAGE = "금액 추출"
 _AMOUNT_KEYWORDS = (
     "사업예산", "예산", "계약금액", "추정가격", "예정가격", "총액", "합계",
@@ -192,6 +192,12 @@ def _line_amounts(line: str) -> set[int]:
     return values
 
 
+def _canonical_item_name(item_name: str) -> str:
+    """사업예산 뒤에 금액 원문까지 붙인 모델 응답만 짧은 항목명으로 정리한다."""
+    compact = re.sub(r"\s+", "", item_name)
+    return "사업예산" if compact.startswith("사업예산") else item_name
+
+
 def _ground_amount_quotes(
     extraction: AmountExtractionOut,
     source: str,
@@ -203,7 +209,10 @@ def _ground_amount_quotes(
         matches = [line for line in allowed if item.amount in _line_amounts(line)]
         if len(matches) != 1:
             raise ValueError("추출 금액과 일치하는 원문 줄이 유일하지 않습니다")
-        grounded_items.append(item.model_copy(update={"source_quote": matches[0]}))
+        grounded_items.append(item.model_copy(update={
+            "item_name": _canonical_item_name(item.item_name),
+            "source_quote": matches[0],
+        }))
     return extraction.model_copy(update={"items": grounded_items})
 
 
diff --git a/backend/tests/test_amount_analyzer.py b/backend/tests/test_amount_analyzer.py
index 995a0ef..4a88c95 100644
--- a/backend/tests/test_amount_analyzer.py
+++ b/backend/tests/test_amount_analyzer.py
@@ -92,11 +92,12 @@ def test_amount_analyzer_uses_schema_and_parser_normalization(config):
     assert "100,000,000" not in client.requests[0].system
     prompt_data = json.loads(client.requests[0].user)
     assert prompt_data["allowed_source_quotes"] == source.splitlines()
-    assert client.requests[0].prompt_version == "amount-v5"
+    assert client.requests[0].prompt_version == "amount-v6"
     assert result.model_name == "current-default-model"
     assert result.result["stated_total"] == 97_500_000
     assert result.result["items"][0]["quantity"] == "3"
     assert result.result["items"][0]["unit_price"] == 9_500_000
+    assert result.result["items"][0]["item_name"] == "특급기술자"
     assert result.result["items"][0]["amount"] == 28_500_000
 
 
@@ -107,7 +108,8 @@ def test_amount_analyzer_replaces_model_quote_with_unique_amount_line(config):
         "currency": "KRW",
         "stated_total": 7_430_000_000,
         "items": [{
-            "item_name": "사업 예산",
+            # 실제 모델이 반환한 것처럼 항목명에 금액 원문 전체가 붙은 형태다.
+            "item_name": "사 업 예 산 금 칠십사억삼천만 원 (7,430,000,000, 부가세 포함)",
             "category": "OTHER",
             "quantity": None,
             "unit": None,
@@ -125,6 +127,7 @@ def test_amount_analyzer_replaces_model_quote_with_unique_amount_line(config):
 
     result = asyncio.run(AmountAnalyzer(FakeAmountAI(payload), config).analyze(source))
 
+    assert result.result["items"][0]["item_name"] == "사업예산"
     assert result.result["items"][0]["source_quote"] == source
     assert result.result["items"][0]["amount"] == 7_430_000_000
 
@@ -542,7 +545,7 @@ def test_document_level_budget_is_an_item_even_without_cost_table(config):
 
     assert result.result["stated_total"] == 100_000_000
     assert result.result["items"] == [{
-        "item_name": "사업 예산",
+        "item_name": "사업예산",
         "category": "OTHER",
         "quantity": None,
         "unit": None,
