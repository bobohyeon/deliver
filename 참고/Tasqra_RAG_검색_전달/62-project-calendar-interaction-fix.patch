diff --git a/frontend/src/features/dashboard/DashboardView.jsx b/frontend/src/features/dashboard/DashboardView.jsx
index 290edde..0291c67 100644
--- a/frontend/src/features/dashboard/DashboardView.jsx
+++ b/frontend/src/features/dashboard/DashboardView.jsx
@@ -29,7 +29,7 @@ import { getDocumentTypeLabel, isSupportedDocumentTypeFilter, UNCLASSIFIED_DOCUM
 import { formatDateShort, formatNumber } from '../../utils/format'
 import ActionTaskPanel from './ActionTaskPanel'
 
-export default function DashboardView({ projectId, documents }) {
+export default function DashboardView({ projectId, documents, members, canEdit, notify }) {
   const navigate = useNavigate()
   const dashboardQuery = useQuery({
     queryKey: ['projects', projectId, 'dashboard'],
@@ -90,7 +90,7 @@ export default function DashboardView({ projectId, documents }) {
       <ActionTaskPanel tasks={tasksQuery.data ?? []} loading={tasksQuery.isPending} onOpenBoard={() => navigate(`/projects/${projectId}/board`)}/>
     </div>
 
-    <ProjectCalendar projectId={projectId}/>
+    <ProjectCalendar projectId={projectId} tasks={tasksQuery.data ?? []} tasksLoading={tasksQuery.isPending} members={members} canEdit={canEdit} notify={notify}/>
 
     <div className='dashboard-distribution-grid'><DocumentTypePanel types={data?.document_types} total={counts?.total} loaded={Boolean(data)} onOpenType={goDocumentsByType}/><ProcessingStatusPanel counts={counts} loaded={Boolean(data)} onOpen={goDocuments}/></div>
 
diff --git a/frontend/src/features/dashboard/ProjectCalendar.jsx b/frontend/src/features/dashboard/ProjectCalendar.jsx
index 5adc1b1..4f21443 100644
--- a/frontend/src/features/dashboard/ProjectCalendar.jsx
+++ b/frontend/src/features/dashboard/ProjectCalendar.jsx
@@ -1,14 +1,16 @@
 // =============================================================================
-// 이 파일의 책임: 프로젝트 태스크 마감과 승인된 일정을 합친 월간 달력을 그린다.
-// 다른 파일과의 관계: api/dashboard.js의 통합 조회를 쓰고, DashboardView 안에
-//   배치된다. 태스크는 기존 보드 상세 흐름으로 보내고 일정은 이 화면에서 본다.
-// Spring 비교: 서버 DTO를 달력 뷰 모델로 표현하는 MVC View에 해당한다.
+// 이 파일의 책임: 프로젝트 태스크 마감과 승인된 일정을 합친 월간 달력을 그리고,
+//   선택한 태스크를 우측 패널에서 수정·삭제한다.
+// 다른 파일과의 관계: api/dashboard.js로 달력을 조회하고 api/task.js의 기존
+//   수정·삭제 API를 재사용한다. DashboardView가 태스크·멤버·권한을 넘긴다.
+// Spring 비교: 서버 DTO를 달력 ViewModel로 표현하고 Command API를 호출하는
+//   MVC View에 해당한다.
 // =============================================================================
 
 import { useMemo, useState } from 'react'
-import { useQuery } from '@tanstack/react-query'
-import { useNavigate } from 'react-router-dom'
+import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
 import { getDashboardCalendar } from '../../api/dashboard'
+import { deleteTask, updateTask } from '../../api/task'
 
 const WEEKDAYS = ['일', '월', '화', '수', '목', '금', '토']
 const KIND_LABELS = {
@@ -19,25 +21,54 @@ const KIND_LABELS = {
   PERIOD: '기간',
 }
 const TASK_STATUS_LABELS = { TODO: '할 일', IN_PROGRESS: '진행 중', DONE: '완료' }
+const TASK_TYPE_LABELS = { DEVELOPMENT: '개발', DESIGN: '디자인', INFRA: '인프라', DOCUMENT: '문서', OTHER: '기타' }
 
-export default function ProjectCalendar({ projectId }) {
-  const navigate = useNavigate()
+export default function ProjectCalendar({ projectId, tasks, tasksLoading, members, canEdit, notify }) {
+  const queryClient = useQueryClient()
   const [month, setMonth] = useState(() => startOfMonth(new Date()))
   const [selectedDate, setSelectedDate] = useState(() => toDateKey(new Date()))
   const [selectedEvent, setSelectedEvent] = useState(null)
   const days = useMemo(() => calendarDays(month), [month])
   const from = toDateKey(days[0])
   const to = toDateKey(days.at(-1))
+  const calendarKey = ['projects', projectId, 'calendar']
+  const tasksKey = ['projects', projectId, 'tasks']
   const calendarQuery = useQuery({
-    queryKey: ['projects', projectId, 'calendar', from, to],
+    queryKey: [...calendarKey, from, to],
     queryFn: () => getDashboardCalendar(projectId, { from, to }),
   })
   const items = calendarQuery.data?.items ?? []
   const selectedItems = items.filter(item => occursOn(item, selectedDate))
+  const selectedTask = selectedEvent?.item_type === 'TASK'
+    ? tasks.find(task => task.id === selectedEvent.source_id)
+    : null
   const monthStart = toDateKey(startOfMonth(month))
   const monthEnd = toDateKey(new Date(month.getFullYear(), month.getMonth() + 1, 0))
   const hasMonthItems = items.some(item => overlaps(item, monthStart, monthEnd))
 
+  const saveTaskMutation = useMutation({
+    mutationFn: values => updateTask(projectId, selectedEvent.source_id, values),
+    onSuccess: saved => {
+      queryClient.setQueryData(tasksKey, current => current?.map(task => task.id === saved.id ? saved : task))
+      queryClient.invalidateQueries({ queryKey: calendarKey })
+      queryClient.invalidateQueries({ queryKey: ['projects', projectId, 'dashboard'] })
+      notify?.('success', '태스크 수정 완료', `${saved.title} 태스크를 저장했습니다.`)
+      setSelectedEvent(null)
+    },
+    onError: error => notify?.('error', '태스크 수정 실패', error.message),
+  })
+  const deleteTaskMutation = useMutation({
+    mutationFn: task => deleteTask(projectId, task.id),
+    onSuccess: (_, task) => {
+      queryClient.setQueryData(tasksKey, current => current?.filter(item => item.id !== task.id))
+      queryClient.invalidateQueries({ queryKey: calendarKey })
+      queryClient.invalidateQueries({ queryKey: ['projects', projectId, 'dashboard'] })
+      notify?.('success', '태스크 삭제 완료', `${task.title} 태스크를 삭제했습니다.`)
+      setSelectedEvent(null)
+    },
+    onError: error => notify?.('error', '태스크 삭제 실패', error.message),
+  })
+
   function moveMonth(offset) {
     const next = new Date(month.getFullYear(), month.getMonth() + offset, 1)
     setMonth(next)
@@ -55,10 +86,6 @@ export default function ProjectCalendar({ projectId }) {
     setSelectedEvent(null)
   }
   function openItem(item, dateKey = selectedDate) {
-    if (item.item_type === 'TASK') {
-      navigate(`/projects/${projectId}/board?task_id=${item.source_id}`)
-      return
-    }
     setSelectedDate(dateKey)
     setSelectedEvent(item)
   }
@@ -89,8 +116,8 @@ export default function ProjectCalendar({ projectId }) {
             return <div className={`project-calendar__day${outside ? ' is-outside' : ''}${selected ? ' is-selected' : ''}${today ? ' is-today' : ''}`} key={dateKey}>
               <button type='button' className='project-calendar__date' aria-pressed={selected} onClick={() => selectDate(dateKey)}><span>{day.getDate()}</span>{today && <small>오늘</small>}</button>
               <div className='project-calendar__events'>
-                {dayItems.slice(0, 3).map(item => <button type='button' className={`project-calendar__event is-${item.item_type.toLowerCase()}`} title={item.title} onClick={() => openItem(item, dateKey)} key={item.id}><span>{KIND_LABELS[item.kind] ?? item.kind}</span><b>{item.title}</b></button>)}
-                {dayItems.length > 3 && <button type='button' className='project-calendar__more' onClick={() => selectDate(dateKey)}>+{dayItems.length - 3}개 더 보기</button>}
+                {dayItems.slice(0, 1).map(item => <button type='button' className={`project-calendar__event is-${item.item_type.toLowerCase()}`} title={item.title} onClick={() => openItem(item, dateKey)} key={item.id}><span>{KIND_LABELS[item.kind] ?? item.kind}</span><b>{item.title}</b></button>)}
+                {dayItems.length > 1 && <button type='button' className='project-calendar__more' onClick={() => selectDate(dateKey)}>+{dayItems.length - 1}개 더 보기</button>}
               </div>
             </div>
           })}
@@ -99,21 +126,66 @@ export default function ProjectCalendar({ projectId }) {
         {calendarQuery.isSuccess && !hasMonthItems && <p className='project-calendar__month-empty'>이 달에 표시할 태스크 마감이나 승인된 일정이 없습니다.</p>}
       </div>
 
-      <aside className='project-calendar__detail' aria-label='선택한 날짜의 항목'>
-        <header><span>{formatDate(selectedDate)}</span><strong>{selectedItems.length}개 항목</strong></header>
-        {selectedItems.length > 0
-          ? <ul>{selectedItems.map(item => <li key={item.id}><button type='button' className={selectedEvent?.id === item.id ? 'is-active' : ''} onClick={() => openItem(item)}><span>{item.item_type === 'TASK' ? '태스크' : '일정'} · {KIND_LABELS[item.kind] ?? item.kind}</span><strong>{item.title}</strong><small>{item.item_type === 'TASK' ? TASK_STATUS_LABELS[item.status] ?? item.status : formatEventDate(item)}</small></button></li>)}</ul>
-          : <div className='project-calendar__empty'><strong>이 날짜에는 항목이 없습니다.</strong><p>마감일이 있는 태스크나 승인된 일정이 생기면 자동으로 표시됩니다.</p></div>}
-        {selectedEvent && <div className='project-calendar__schedule-detail'>
-          <span>일정 · {KIND_LABELS[selectedEvent.kind] ?? selectedEvent.kind}</span>
-          <h3>{selectedEvent.title}</h3>
-          <dl><div><dt>날짜</dt><dd>{formatEventDate(selectedEvent)}</dd></div><div><dt>상태</dt><dd>승인 완료</dd></div></dl>
-        </div>}
+      <aside className={`project-calendar__detail${selectedEvent ? ' is-open' : ''}`} aria-label='선택한 날짜의 항목' role={selectedEvent ? 'dialog' : undefined} aria-modal={selectedEvent ? false : undefined}>
+        {selectedEvent
+          ? <>
+              <header><span>{selectedEvent.item_type === 'TASK' ? '태스크 상세' : '일정 상세'}</span><button type='button' className='project-calendar__detail-close' onClick={() => setSelectedEvent(null)} aria-label='상세 닫기'>×</button></header>
+              {selectedEvent.item_type === 'TASK'
+                ? selectedTask
+                  ? <TaskEditor key={selectedTask.id} task={selectedTask} members={members} canEdit={canEdit} pending={saveTaskMutation.isPending || deleteTaskMutation.isPending} onCancel={() => setSelectedEvent(null)} onSave={values => saveTaskMutation.mutate(values)} onDelete={() => deleteTaskMutation.mutate(selectedTask)}/>
+                  : <div className='project-calendar__empty'><strong>{tasksLoading ? '태스크를 불러오는 중입니다.' : '태스크를 찾을 수 없습니다.'}</strong></div>
+                : <ScheduleDetail item={selectedEvent}/>}
+            </>
+          : <>
+              <header><span>{formatDate(selectedDate)}</span><strong>{selectedItems.length}개 항목</strong></header>
+              {selectedItems.length > 0
+                ? <ul>{selectedItems.map(item => <li key={item.id}><button type='button' onClick={() => openItem(item)}><span>{item.item_type === 'TASK' ? '태스크' : '일정'} · {KIND_LABELS[item.kind] ?? item.kind}</span><strong>{item.title}</strong><small>{item.item_type === 'TASK' ? TASK_STATUS_LABELS[item.status] ?? item.status : formatEventDate(item)}</small></button></li>)}</ul>
+                : <div className='project-calendar__empty'><strong>이 날짜에는 항목이 없습니다.</strong><p>마감일이 있는 태스크나 승인된 일정이 생기면 자동으로 표시됩니다.</p></div>}
+            </>}
       </aside>
     </div>
   </section>
 }
 
+function TaskEditor({ task, members, canEdit, pending, onCancel, onSave, onDelete }) {
+  const [title, setTitle] = useState(task.title ?? '')
+  const [description, setDescription] = useState(task.description ?? '')
+  const [type, setType] = useState(task.type ?? 'OTHER')
+  const [status, setStatus] = useState(task.status ?? 'TODO')
+  const [assigneeId, setAssigneeId] = useState(task.assignee?.id ? String(task.assignee.id) : '')
+  const [dueOn, setDueOn] = useState(task.due_on ?? '')
+  const [confirmDelete, setConfirmDelete] = useState(false)
+  const [titleError, setTitleError] = useState('')
+
+  function submit(event) {
+    event.preventDefault()
+    if (!canEdit || pending) return
+    if (!title.trim()) { setTitleError('태스크 제목을 입력해 주세요.'); return }
+    onSave({ title: title.trim(), description: description.trim() || null, type, status, assignee_id: assigneeId ? Number(assigneeId) : null, due_on: dueOn || null })
+  }
+
+  return <form className='project-calendar__task-editor' onSubmit={submit}>
+    <label>제목<input disabled={!canEdit || pending} value={title} maxLength='300' aria-invalid={Boolean(titleError)} onChange={event => { setTitle(event.target.value); setTitleError('') }}/>{titleError && <small className='field-error'>{titleError}</small>}</label>
+    <label>설명<textarea disabled={!canEdit || pending} rows='4' value={description} onChange={event => setDescription(event.target.value)}/></label>
+    <div><label>유형<select disabled={!canEdit || pending} value={type} onChange={event => setType(event.target.value)}>{Object.entries(TASK_TYPE_LABELS).map(([value, label]) => <option value={value} key={value}>{label}</option>)}</select></label><label>상태<select disabled={!canEdit || pending} value={status} onChange={event => setStatus(event.target.value)}>{Object.entries(TASK_STATUS_LABELS).map(([value, label]) => <option value={value} key={value}>{label}</option>)}</select></label></div>
+    <div><label>담당자<select disabled={!canEdit || pending} value={assigneeId} onChange={event => setAssigneeId(event.target.value)}><option value=''>담당자 미정</option>{members.map(member => <option value={member.user_id} key={member.user_id}>{member.name}</option>)}</select></label><label>마감일<input disabled={!canEdit || pending} type='date' value={dueOn} onChange={event => setDueOn(event.target.value)}/></label></div>
+    {!canEdit && <p className='project-calendar__readonly'>VIEWER는 태스크를 조회만 할 수 있습니다.</p>}
+    <footer>
+      <button type='button' onClick={onCancel} disabled={pending}>닫기</button>
+      {canEdit && <><button type='button' className='danger-text' disabled={pending} onClick={() => confirmDelete ? onDelete() : setConfirmDelete(true)}>{confirmDelete ? '삭제 확인' : '삭제'}</button><button type='submit' className='primary' disabled={pending}>{pending ? '처리 중...' : '저장'}</button></>}
+    </footer>
+  </form>
+}
+
+function ScheduleDetail({ item }) {
+  return <div className='project-calendar__schedule-detail'>
+    <span>일정 · {KIND_LABELS[item.kind] ?? item.kind}</span>
+    <h3>{item.title}</h3>
+    <dl><div><dt>날짜</dt><dd>{formatEventDate(item)}</dd></div><div><dt>상태</dt><dd>승인 완료</dd></div></dl>
+    <p className='project-calendar__readonly'>AI 일정은 승인 이력과 연결되어 있어 이 MVP에서는 조회만 지원합니다.</p>
+  </div>
+}
+
 function startOfMonth(value) {
   return new Date(value.getFullYear(), value.getMonth(), 1)
 }
diff --git a/frontend/src/pages/WorkspacePage.jsx b/frontend/src/pages/WorkspacePage.jsx
index c454f2b..29ded22 100644
--- a/frontend/src/pages/WorkspacePage.jsx
+++ b/frontend/src/pages/WorkspacePage.jsx
@@ -174,7 +174,7 @@ function WorkspaceContent({ project, projects, tab, navigate, notify, user, onLo
 function TabContent({ tab, project, data, documentType, documentState, onDocumentTypeChange, onDocumentStateChange, onClearDocumentFilters, canEdit, notify, onUpload, onFileDrop, uploadQueue, onRetryUpload, onClearUploadQueue, onDeleteProject, deleting }) {
   if (tab === 'documents') return <DocumentsView projectId={project.id} documents={data.documents} documentsTotal={data.documentsTotal} documentType={documentType} documentState={documentState} onDocumentTypeChange={onDocumentTypeChange} onDocumentStateChange={onDocumentStateChange} onClearFilters={onClearDocumentFilters} canEdit={canEdit} onUpload={onUpload} onFileDrop={onFileDrop} uploadQueue={uploadQueue} onRetryUpload={onRetryUpload} onClearUploadQueue={onClearUploadQueue} onRetry={data.retryDocument} retryingDocumentId={data.retryingDocumentId}/>
   if (tab === 'settings') return <MembersView project={project} members={data.members} invitations={data.invitations} onUpdateProject={data.updateProject} updatingProject={data.updatingProject} onInvite={data.invite} onCancelInvitation={data.cancelInvitation} onRole={data.changeRole} onRemove={data.excludeMember} onDeleteProject={onDeleteProject} deleting={deleting}/>
-  if (tab === 'dashboard') return <DashboardView projectId={project.id} documents={data.documents} members={data.members}/>
+  if (tab === 'dashboard') return <DashboardView projectId={project.id} documents={data.documents} members={data.members} canEdit={canEdit} notify={notify}/>
   // 검색은 워크스페이스 데이터(문서 목록 · 멤버)를 쓰지 않는다. 자기 상태만
   // 들고 api/search.js 를 부른다. 범위 토글에 쓸 프로젝트 이름만 넘긴다.
   if (tab === 'search') return <SearchView projectId={project.id} projectName={project.name}/>
diff --git a/frontend/src/styles/workspace.css b/frontend/src/styles/workspace.css
index dabba03..3a50b2e 100644
--- a/frontend/src/styles/workspace.css
+++ b/frontend/src/styles/workspace.css
@@ -193,13 +193,13 @@
 .project-calendar__notice span, .project-calendar__notice small { display: block; }
 .project-calendar__notice small { margin-top: 4px; color: #7d6a6a; }
 .project-calendar__notice button { padding: 8px 11px; border: 1px solid #d9bcbc; border-radius: 8px; background: #fff; color: #8a3d3d; font-weight: 750; cursor: pointer; }
-.project-calendar__layout { display: grid; grid-template-columns: minmax(0, 1fr) 300px; gap: 18px; align-items: start; }
+.project-calendar__layout { display: grid; grid-template-columns: minmax(0, 1fr) 340px; gap: 18px; align-items: start; }
 .project-calendar__month { position: relative; min-width: 0; overflow-x: auto; }
 .project-calendar__weekdays, .project-calendar__grid { min-width: 720px; }
 .project-calendar__weekdays { display: grid; grid-template-columns: repeat(7, minmax(0, 1fr)); border: 1px solid #dce3ee; border-bottom: 0; border-radius: 10px 10px 0 0; background: #f7f8fc; }
 .project-calendar__weekdays span { padding: 9px; color: #65748a; font-size: 12px; font-weight: 800; text-align: center; }
 .project-calendar__grid { display: grid; grid-template-columns: repeat(7, minmax(0, 1fr)); border-top: 1px solid #dce3ee; border-left: 1px solid #dce3ee; }
-.project-calendar__day { min-height: 124px; padding: 7px; border-right: 1px solid #dce3ee; border-bottom: 1px solid #dce3ee; background: #fff; }
+.project-calendar__day { height: 112px; padding: 7px; overflow: hidden; border-right: 1px solid #dce3ee; border-bottom: 1px solid #dce3ee; background: #fff; }
 .project-calendar__day.is-outside { background: #f8f9fc; }
 .project-calendar__day.is-selected { box-shadow: inset 0 0 0 2px #5d55c7; }
 .project-calendar__date { display: flex; width: 100%; min-height: 28px; padding: 0 2px; align-items: center; justify-content: space-between; border: 0; background: transparent; color: #36435a; font: inherit; font-weight: 750; cursor: pointer; }
@@ -237,3 +237,21 @@
 .project-calendar__schedule-detail dd { margin: 0; color: #36435a; font-weight: 700; }
 @media (max-width: 1100px) { .project-calendar__layout { grid-template-columns: 1fr; } .project-calendar__detail { min-height: 240px; } }
 @media (max-width: 680px) { .project-calendar__head { align-items: flex-start; flex-direction: column; } .project-calendar__controls { width: 100%; flex-wrap: wrap; } .project-calendar__controls strong { flex: 1; } .project-calendar__controls .project-calendar__today { width: 100%; } .project-calendar__notice { align-items: flex-start; flex-direction: column; } .project-calendar__notice button { width: 100%; } }
+
+
+/* 캘린더 우측 태스크 편집 패널 */
+.project-calendar__detail.is-open { border-color: #bfc7d5; background: #fff; box-shadow: 0 12px 32px rgba(30, 43, 66, .12); }
+.project-calendar__detail-close { display: grid; width: 32px; height: 32px; padding: 0; place-items: center; border: 1px solid #d2d9e7; border-radius: 8px; background: #fff; color: #4b5870; font-size: 20px; cursor: pointer; }
+.project-calendar__task-editor { display: grid; gap: 13px; margin-top: 14px; }
+.project-calendar__task-editor > div { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
+.project-calendar__task-editor label { display: grid; gap: 6px; color: #4b5870; font-size: 12px; font-weight: 750; }
+.project-calendar__task-editor input, .project-calendar__task-editor textarea, .project-calendar__task-editor select { width: 100%; min-width: 0; padding: 10px; border: 1px solid #d5deeb; border-radius: 8px; background: #fff; color: #202b3e; font: inherit; font-size: 13px; }
+.project-calendar__task-editor textarea { resize: vertical; }
+.project-calendar__task-editor input:disabled, .project-calendar__task-editor textarea:disabled, .project-calendar__task-editor select:disabled { background: #f1f3f7; color: #65748a; }
+.project-calendar__task-editor footer { display: flex; justify-content: flex-end; gap: 8px; padding-top: 3px; }
+.project-calendar__task-editor footer button { min-height: 38px; padding: 8px 13px; border: 1px solid #d2d9e7; border-radius: 8px; background: #fff; color: #4b5870; font-weight: 750; cursor: pointer; }
+.project-calendar__task-editor footer button.primary { border-color: #4f46c8; background: #4f46c8; color: #fff; }
+.project-calendar__task-editor footer button.danger-text { margin-right: auto; border-color: #e0c5c5; color: #9a3f3f; }
+.project-calendar__task-editor footer button:disabled { cursor: wait; opacity: .6; }
+.project-calendar__readonly { margin: 13px 0 0; padding: 10px; border-radius: 8px; background: #f1f3f7; color: #65748a; font-size: 12px; line-height: 1.55; }
+@media (max-width: 430px) { .project-calendar__task-editor > div { grid-template-columns: 1fr; } }
