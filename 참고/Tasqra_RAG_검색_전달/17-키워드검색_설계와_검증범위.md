# 키워드 검색(`SRH-003`) — 무엇을 만들었고 무엇을 검증했나

`17-keyword-search-srh003.patch` 의 설계 근거와 **검증하지 못한 것**을 남긴다.
검증 못 한 것을 적는 이유는, 그것이 DB 를 띄운 뒤 가장 먼저 볼 목록이기 때문이다.

| | |
|---|---|
| 기능 | `SRH-003 키워드 검색` — 미구현 · P1 · 김보현 |
| 적용 순서 | `15` → `16` → **`17`** (세 패치 순차 `git apply --check` 통과) |
| 규모 | 9파일 · `+719 −2`. 새 파일 둘(마이그레이션 · 테스트) |
| 마이그레이션 | `20260820_0016` · `down_revision = "20260819_0015"` |
| 새 API | `POST /api/search/keyword` |

## 설계 결정 다섯

### ① 트라이그램(`pg_trgm` + `ILIKE`) — `tsvector` 가 아니다

**PostgreSQL 에 한국어 전문검색 설정이 없다.** `simple` 설정은 공백으로만 토큰을
나눈다.

```
질의 "계약금액"   본문 "계약금액은"   ->  simple tsvector 는 서로 다른 토큰
```

트라이그램은 글자 3개씩 겹쳐 보므로 조사·붙여쓰기에 걸리지 않는다. 완료 판정이
*"고유명사·숫자·문서번호처럼 **정확한 문자열**로 찾는다"* 이므로 형태소 분석이
아니라 부분 문자열 매칭이 목적에 맞다.

### ② 정확성과 순서를 나눴다

- **정확성은 `ILIKE` 가 보장한다.** 걸린 청크에는 검색어가 반드시 있다
- **순서는 `word_similarity()` 가 정한다.** 이건 정확성이 아니라 표시 순서이므로,
  나중에 바꿔도 **결과 집합은 같다**

`similarity()` 를 쓰지 않은 이유 — 그건 두 문자열 **전체**를 비교해서, 짧은 검색어
와 900자 청크를 비교하면 값이 늘 0 에 가깝다. `word_similarity` 는 본문 안의
**한 구간**과 비교하므로 짧은 질의에 맞다.

### ③ 엔드포인트를 새로 만들었다

`의미 검색(SRH-001)` 은 구현됨이고 프론트가 쓰고 있다. 요청 스키마에 `mode` 를
더하면 그 계약을 고쳐야 하고 회귀 위험이 생긴다. 나중에
`키워드·의미 결합(SRH-004)` 이 `/api/search/hybrid` 를 하나 더 두면 세 방식을
나란히 비교할 수 있다.

### ④ 응답 모양을 같게 두고 필드 셋만 더했다

| 필드 | 뜻 |
|---|---|
| `match_kind` | `"keyword"`. 의미 검색 응답에서는 `null` |
| `match_count` | 검색어가 그 청크에 몇 번 나오는가 |
| `match_offset` | `snippet` 안에서 검색어가 시작하는 위치. 프론트가 강조한다 |

스키마를 나누지 않은 이유는 **하이브리드가 두 결과를 한 순위로 합쳐야** 하기
때문이다. 모양이 다르면 합치는 코드를 또 써야 한다.

`similarity` 는 키워드에서 **코사인 유사도가 아니라 트라이그램 낱말 유사도**다.
어느 쪽인지는 `match_kind` 로 구분한다.

### ⑤ `embedding_model` 조건을 유지했다

키워드 검색에 임베딩은 상관없다. **그런데 빼면 안 된다.** 같은 문서를 두 모델로
청킹해 두면 같은 본문이 여러 행으로 있어서, 조건을 빼면 같은 텍스트가 결과에
중복으로 나온다. 의미 검색은 이 조건으로 중복을 우연히 피하고 있었다.

## 빼먹기 쉬운 곳 둘 — 실제로 막았다

**`ILIKE` 와일드카드.** `%` 는 "아무 글자나 몇 개든" 이므로 검색어에 그대로 들어가면
**조건이 사실상 사라져 프로젝트의 모든 청크가 걸린다.**

```
입력 "100%"   이스케이프 함    -> "%100\%%"    "100%" 를 글자 그대로 찾는다
              이스케이프 없음  -> "%100%%"     "100" 으로 시작하는 것 전부
```

SQL 주입과는 다른 문제다(바인드 파라미터로 넘기므로 주입은 안 된다). 막는 것은
**의미가 뒤바뀌는 것**이다. 역슬래시를 먼저 바꿔야 우리가 넣은 이스케이프를 다시
이스케이프하지 않는다.

**스니펫 offset.** 공백을 **누른 뒤에** 위치를 찾아야 한다. 원문에서 찾고 나서
누르면 글자 수가 줄면서 위치가 어긋난다. 앞에 `…` 를 붙이면 그만큼 offset 이
밀리는 것도 함께 반영해야 한다.

> ⚠ `match_offset` 을 `content_start` 에 더해 **원문 좌표로 쓸 수 없다.** 공백을
> 눌렀으므로 원문과 글자 수가 다르다. 원문 위 강조는 지금처럼 청크 단위
> (`content_start` ~ `content_end`)로 한다.

## 검증한 것

**Python 3.11 기준**이다(`backend/Dockerfile` 이 `python:3.11-slim`). 샌드박스의
기본 `python3` 는 3.9 라서 그것으로 검사하면 안 된다.

| 무엇 | 방법 | 결과 |
|---|---|---|
| 세 패치가 순서대로 붙는가 | `git apply --check` 연속 | 통과 |
| 문법 | 3.11 `py_compile` · 만진 파일 21개 | 통과 |
| `like_pattern` 와일드카드 차단 | `%` · `_` · `\` · `%` 단독 | 통과 |
| `_count_occurrences` | 대소문자 무시 · 없음 · 빈 검색어 | 통과 |
| `_keyword_snippet` offset | 짧은 본문 · 공백 누름 · 가운데 · 맨 앞 · 맨 끝 · 대소문자 · 못 찾음 | 통과 |
| **offset 무작위 대조** | **3000건** — 돌려준 offset 자리에 검색어가 정말 있는지 | **전부 통과** |

순수 함수는 소스에서 `ast` 로 떼어내 돌렸다. 손으로 옮기면 코드와 어긋나므로
**파일을 직접 읽어** 검사했다.

## DB 검증 결과 (2026-08-20 · 사용자 PC · Python 3.11.0 · PostgreSQL 16 + pgvector)

샌드박스에는 `pytest`·`fastapi`·`sqlalchemy` 가 없고 네트워크가 막혀 설치할 수
없다. 그래서 아래는 **사용자 PC 에서 실제로 돌려 확인한 것**이다.

| | 확인한 것 | 결과 |
|---|---|---|
| 1 | 테스트 20개 | **20 passed in 1.17s** ✅ |
| 2 | 마이그레이션 | `20260819_0015 -> 20260820_0016` 적용 ✅ · `\d` 에 `"ix_chunk_text_trgm" gin (text gin_trgm_ops)` ✅ |
| 3 | `ILIKE` 가 인덱스를 쓸 수 있는가 | `Bitmap Index Scan on ix_chunk_text_trgm` · `Index Cond: (text ~~* '%계약금액%')` ✅ |
| 4 | `word_similarity` 한국어 순서 | 불일치 `0` < 조사붙음 `0.8` < 정확일치 `1` ✅ **의도한 순서 그대로** |
| 5 | 의미 검색 회귀 | 미확인 — 아래 |

### 3번에서 배운 것 — 플래너가 `Seq Scan` 을 고르는 것은 고장이 아니다

처음에 합성 청크 6,000행을 넣고 `EXPLAIN` 했더니 `Seq Scan` 이 나왔다.

```
Seq Scan on document_chunks  (cost=0.00..185.20 rows=1 width=8)
  Filter: (text ~~* '%합성 청크 4237%')
  Rows Removed by Filter: 6015
```

**비용 185.20 은 약 125페이지(1MB)다.** 6,000행 × 1024차원 벡터면 24MB 인데 왜
1MB 인가 — **벡터가 TOAST 로 빠졌기 때문이다.** 행당 4KB 는 PostgreSQL 의 2KB
인라인 한계를 넘어 별도 테이블에 저장된다. 본체 힙에는 행 머리와 짧은 `text` 만
남는다. 그리고 합성 청크의 본문은 `합성 청크 4237` — **13글자**다.

즉 **본체 테이블이 1MB밖에 안 되어 통째로 읽는 것이 어떤 인덱스 경로보다 싸다.**
플래너 판단이 맞다.

> `도구/seed_bulk_chunks.sql` 은 **HNSW 벡터 검색을 보려고 만든 도구**다(벡터가
> 저장을 지배하는 전제). 텍스트 검색 계획을 보는 데는 본문이 너무 짧아 맞지 않는다.
> **실제 문서 청크는 900자 내외**(`CHUNK_MAX_TOKENS 480` × `CHARS_PER_TOKEN 1.89`)
> 로 70배 크다. 실제 청크가 쌓이면 자연히 인덱스를 고른다.

**두 질문을 섞지 않는다.**

| 질문 | 성격 | 확인 방법 |
|---|---|---|
| 이 조건에 인덱스를 **쓸 수 있는가** | **정확성** — `gin_trgm_ops` 가 `ILIKE` 를 지원하나 | `SET enable_seqscan = off` 로 강제 → **확인됨** |
| 플래너가 **고를 것인가** | **성능** — 테이블 크기에 달림 | 실제 청크가 쌓이면 자연히 |

`enable_seqscan` 을 끄고 계획 모양만 보는 방식은 이 레포의 기존 관례다
(`도구/explain_rag_search.sql` 이 같은 방법을 쓴다).

### 4번 — 순서 기준이 한국어에서 의도대로 동작한다

```sql
SELECT word_similarity('계약금액','이 계약의 계약금액은 1억원이다') AS 조사붙음,
       word_similarity('계약금액','제1조 계약금액 및 지급방법')     AS 정확일치,
       word_similarity('계약금액','전혀 다른 내용이다')            AS 불일치;
--  조사붙음 0.8 | 정확일치 1 | 불일치 0
```

조사가 붙으면 `0.8` 로 내려가지만 **순위가 뒤집히지 않는다.** 정확히 일치하는
청크가 먼저 오고, 조사가 붙은 것이 다음, 관계없는 것은 애초에 `ILIKE` 에서
걸러진다. 순서 기준으로 쓸 만하다.

### 5번 — 아직 확인되지 않았다

의미 검색(`POST /api/search`) 회귀다. **깨질 이유는 작다** — 새 필드 셋은 모두
`= None` 기본값이라 기존 생성 지점이 그대로 동작하고, `KeywordSearchRequest` 는
새 클래스라 기존 계약을 건드리지 않는다. 프론트에는 `null` 필드 셋이 늘어난다.

다만 **확인하지 않은 것을 확인했다고 적지 않는다.** `docker-compose` 가
`./backend/app:/app/app` 를 마운트하고 `--reload` 로 돌므로 코드는 이미 컨테이너에
살아 있다. 앱이 기동하는지와 새 경로가 스키마에 잡히는지를 보면 된다.

## 남겨 둔 것

- **프론트 화면이 없다.** 완료 판정 *"고유 문자열이 의미 검색에서 누락돼도 키워드
  검색으로 찾힌다"* 는 API 로 확인할 수 있지만, 사용자가 쓰려면 화면이 필요하다.
  `SearchView.jsx` 에 붙이는 것은 별도 작업으로 둔다
- **하이브리드(`SRH-004`)는 다음이다.** 이 패치가 그 선행 조건을 채운다

> `키워드 검색` 은 **임베딩을 만들지 않는다.** 그래서 `임베딩 모델 서빙 경로
> 확정(SYS-002-4)` 이 미결인 상태에서도 동작한다 — 다른 팀원이 모델 서버 없이
> 실제 검색을 처음 돌려 볼 수 있는 경로다.
