# 옛 2단 ID → v5 3단 ID — 63곳 전수 갱신 기록

`16-stale-feature-ids.patch` 가 무엇을 왜 바꿨는지 남긴다. **나중에 "이 ID 가
맞나" 를 다시 묻지 않게 하려는 것이다.**

| | |
|---|---|
| 대상 | 팀 레포 `ParkSehyeon1009/Tasqra` (main `9d3714f` 기준) |
| 범위 | 옛 2단 ID **63곳 · 17파일** — 주석·문서만. **로직 변경 0** |
| 판별법 | 옛 형식은 숫자 **2자리**(`RAG-04`), v5 는 **3자리**(`SRH-001`) |
| 적용 순서 | `15-open-tasks-comment-fix.patch` **다음에** 얹는다(`main.py` 등이 겹친다) |
| 검증 | 두 패치 순차 `git apply --check` 통과 · 바뀐 `.py` 전부 `py_compile` 통과 |

## ① 부록 A 로 확인된 것 — 44곳

기능명세서 부록 A 가 `RAG` 12건 · `VIS` 9건을 **전수 확인된 매핑**으로 갖고 있다.

| 옛 → v5 | 작업명 | 곳 |
|---|---|---|
| `RAG-04` → `SRH-001` | 의미 검색 | 12 |
| `RAG-09` → `RAG-001-3` | 검수 확정 시 재인덱싱 | 9 |
| `RAG-01` → `RAG-001-1` | 텍스트 정규화·청킹 | 6 |
| `RAG-02` → `RAG-001-2` | 임베딩 생성·벡터 인덱스 저장 | 6 |
| `RAG-08` → `SRH-002-2` | 근거 스니펫 연결 | 6 |
| `RAG-12` → `SRH-002-3` | 유사 사업 단가 선례 검색 | 2 |
| `RAG-05` → `SRH-004` | 키워드·의미 결합(하이브리드) | 1 |
| `RAG-07` → `RAG-002-1` | 프롬프트 컨텍스트 조립 | 1 |
| `VIS-07` → `SRH-001` | 검색 화면의 의미 검색 표시 | 1 |

## ② 부록 B 구간 — 19곳. 기능명세서 원본과 대조해 정했다

부록 B 가 **"1:1 로 단정하지 말 것"** 이라고 경고한 구간이다. 그래서 v5 원본의
기능명·기능 설명·완료 판정을 하나씩 맞춰 보고 정했다.

| 옛 ID | 코드의 문맥 | v5 | 판정 근거 | 곳 |
|---|---|---|---|---|
| `AMT-03` | *"합계 대조가 틀리는 가장 흔한 원인"* | `AMT-002-1` | 기능 설명이 *"수량×단가로 항목 금액을 계산하고 **문서 합계와 대조**한다"* — 말이 그대로 겹친다 | 8 |
| `AMT-06` | *"원가 구분별·프로젝트 단위 집계"* | `AMT-002-2` | 기능명 *"**프로젝트 금액 집계**"* · 설명 *"여러 문서의 금액을 프로젝트 단위로 모은다"* | 5 |
| `AMT-01` | *"금액 항목"* · *"금액 분석기가 반환한 JSON"* | `AMT-001-1` | 기능명 *"금액 **항목 추출**"* · 비고 *"LLM 은 추출만"*. 승인·수정은 `AMT-001-2` 로 따로 있고, 이 코드는 **분석기 출력 스키마**라 추출 쪽이다 | 2 |
| `AMT-17` | *"화면 계산식 표시"* | `AMT-003-3` | 기능명 *"**계산식**·산출 근거 **표시**"* | 1 |
| `TSK-03` | *"AI 가 뽑은 것을 자동 등록하지 않는다"* | `TSK-002-1` | `AMT-004-3 불일치 태스크 제안` 의 **선행이 `TSK-002-1`** 이고 완료 판정이 *"승인형 제안 카드가 생기고 **자동 등록은 하지 않는다**"*. 이 주석이 있는 곳도 **AI 제안 승인 상태 enum** 이다 | 1 |

### 기계적으로 못 바꾼 둘

**`ANL-03` — 하나가 셋으로 쪼개졌다.**

```
전  EXTRACT = "extract"    # 액션아이템 · 결정사항 · 일정 (ANL-03)
후  EXTRACT = "extract"    # 액션아이템·결정사항·일정 (ANL-002-1·2·3)
```

v5 에서 `ANL-002-1 액션 아이템 추출` · `ANL-002-2 결정사항 추출` ·
`ANL-002-3 일정·기한 추출` **셋**이 되었다. 주석의 세 낱말과 정확히 짝이 맞는다.

**`ANL-04` — v5 에 후속이 없다. 폐기된 설계다.**

```
전  # 유형별로 골라 쓰는 방식은 폐기했다(ANL-04). 분류 결과를 기다려야 해서
후  # 유형별로 골라 쓰는 방식은 폐기했다(구판 ANL-04 — v5 에 후속 항목이 없다).
```

v5 `ANL-004-*` 는 재시도·폴백 / 정확도 측정 / 교정 / 번역이고 **"분석기 선택
규칙" 은 없다.** 폐기 5건 중 하나이고 `analyzer_rules` 테이블도 만들지 않기로
했다. 주석 자신이 *"폐기했다"* 고 말한다.

> **ID 를 지우지 않고 "구판" 을 붙여 남겼다.** 지우면 옛 ID 로 검색해서 이 결정을
> 찾을 수 없다. 살아 있는 ID 로 오해되지도 않아야 하므로 둘을 함께 적었다.
> **이것이 유일하게 남은 2단 ID 이고, 의도된 것이다.**

## 왜 기능 구현과 같은 브랜치에 두면서 커밋은 나눴나

`키워드 검색(SRH-003)` 과 같은 브랜치·같은 PR 로 올린다. 다만 커밋은 셋으로
나눈다 — **주석 정정 / ID 갱신 / 기능 구현.**

한 커밋에 섞으면 `+76 −64` 짜리 기계적 치환 속에서 **기능 코드 변경이 묻힌다.**
리뷰하는 사람이 "이 줄이 진짜 로직인가 주석인가" 를 매번 판단해야 한다.
커밋을 나누면 앞의 둘은 `git show --stat` 만 보고 넘길 수 있다.
