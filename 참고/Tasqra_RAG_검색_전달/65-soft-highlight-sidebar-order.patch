diff --git a/frontend/src/features/board/BoardView.css b/frontend/src/features/board/BoardView.css
index dc97fd5..ce0e9b5 100644
--- a/frontend/src/features/board/BoardView.css
+++ b/frontend/src/features/board/BoardView.css
@@ -16,13 +16,19 @@
 
 
 
-/* 캘린더에서 들어온 태스크를 찾을 수 있게 스크롤 후 잠시 강조한다. */
-.task-board-card.is-calendar-target { animation: task-calendar-target 2.4s ease-out; outline: 3px solid #5148c8; outline-offset: 3px; }
+/* 캘린더에서 들어온 태스크를 찾을 수 있게 스크롤 후 연하게 강조한다. */
+.task-board-card.is-calendar-target {
+  animation: task-calendar-target 2.4s ease-in-out;
+  outline: 2px solid #5148c829;
+  outline-offset: 2px;
+  background: linear-gradient(120deg, #fff 8%, #faf9ff 48%, #f0f4ff 100%);
+  background-size: 220% 100%;
+}
 @keyframes task-calendar-target {
-  0%, 100% { box-shadow: 0 4px 12px #26344c0d; transform: translateY(0); }
-  18%, 52% { box-shadow: 0 0 0 7px #5148c833, 0 12px 24px #26344c24; transform: translateY(-3px); }
-  35%, 70% { box-shadow: 0 0 0 2px #5148c81f, 0 6px 14px #26344c14; transform: translateY(0); }
+  0%, 100% { background-position: 0 50%; box-shadow: 0 4px 12px #26344c0d; transform: translateY(0); }
+  24%, 58% { background-position: 100% 50%; box-shadow: 0 0 0 5px #5148c814, 0 9px 20px #26344c16; transform: translateY(-1px); }
+  40%, 76% { background-position: 48% 50%; box-shadow: 0 0 0 2px #5148c80d, 0 5px 14px #26344c10; transform: translateY(0); }
 }
 @media (prefers-reduced-motion: reduce) {
-  .task-board-card.is-calendar-target { animation: none; box-shadow: 0 0 0 7px #5148c833; }
+  .task-board-card.is-calendar-target { animation: none; box-shadow: 0 0 0 4px #5148c814; }
 }
diff --git a/frontend/src/features/projects/ProjectSidebar.jsx b/frontend/src/features/projects/ProjectSidebar.jsx
index b4b55d6..6943266 100644
--- a/frontend/src/features/projects/ProjectSidebar.jsx
+++ b/frontend/src/features/projects/ProjectSidebar.jsx
@@ -5,7 +5,7 @@ import { useEffect, useState } from 'react'
 //   · TABS = 주소로 들어왔을 때 허용되는 곳 (없으면 대시보드로 되돌린다)
 // 한쪽만 고치면 **에러 없이** 어긋난다 — 여기만 넣으면 눌러도 대시보드로
 // 튕기고, TABS 에만 넣으면 주소를 직접 쳐야 들어갈 수 있다.
-const PROJECT_MENUS = [['dashboard','dashboard','대시보드'],['documents','document','문서'],['search','search','검색'],['amounts','amount','금액'],['deliverables','deliverable','산출물'],['board','board','보드'],['settings','settings','설정']]
+const PROJECT_MENUS = [['dashboard','dashboard','대시보드'],['documents','document','문서'],['search','search','검색'],['board','board','보드'],['amounts','amount','금액'],['deliverables','deliverable','산출물'],['settings','settings','설정']]
 
 export default function ProjectSidebar({ projects, activeProjectId, activeTab, onSelect, onNavigateTab, onCreate }) {
   const [collapsed, setCollapsed] = useState(() => localStorage.getItem('tasqra-rail-collapsed') === '1')
diff --git a/frontend/src/pages/WorkspacePage.jsx b/frontend/src/pages/WorkspacePage.jsx
index c454f2b..658b12a 100644
--- a/frontend/src/pages/WorkspacePage.jsx
+++ b/frontend/src/pages/WorkspacePage.jsx
@@ -38,7 +38,7 @@ import '../styles/workspace.css'
 // PROJECT_MENUS 다. 거기가 사이드바에서 눌러 들어가는 목록이고, 이 배열은
 // 주소로 들어왔을 때 허용하는 목록이다(없으면 대시보드로 되돌린다).
 // 셋 중 하나를 빠뜨리면 에러 없이 어긋난다.
-const TABS = [['dashboard','대시보드'],['documents','문서'],['search','검색'],['amounts','금액'],['deliverables','산출물'],['board','보드'],['settings','설정']]
+const TABS = [['dashboard','대시보드'],['documents','문서'],['search','검색'],['board','보드'],['amounts','금액'],['deliverables','산출물'],['settings','설정']]
 const DOCUMENT_STATES = new Set(['PROCESSING', 'REVIEW_REQUIRED', 'COMPLETED', 'FAILED'])
 
 export default function WorkspacePage({ user, onLogout, notify }) {
