# `ANL-16` 구조화 출력 검증·재시도 — Tasqra 전달용

**Tasqra 레포에 아직 올리지 않았다.** 기획을 다시 잡기로 해서 보류 중이다.
잃지 않게 여기 둔다.

| 파일 | 놓을 곳 |
|---|---|
| `structured.py` | `Tasqra/backend/app/analyzers/structured.py` |
| `README.md` | 쓰는 방법과 판단 근거. Tasqra 에 올릴 때는 안 옮겨도 된다 |

## 왜 만들었나

세현님이 LoRA 어댑터 스위칭을 고민하시는데, **형식 문제라면 학습이 필요 없다.**
스키마 검증 + 재시도로 잡힌다. `AI_INVALID_RESPONSE` 에러코드는 이미 main 에 있고
`AMT-12` 스키마도 내가 이미 올려뒀다. **재시도 장치만 없었다.**

## 검증한 것

`extract_json` 10건 통과 — 코드블록 · 머리말 · 꼬리말 · 중첩 객체 ·
**문자열 안의 중괄호** · 이스케이프된 따옴표.
금액 항목명에 괄호가 흔하다 (`"소프트웨어 라이선스(3년)"`).

## 보류 이유

2026-08-12 회의에서 **기획을 다시 잡기로 했다.** 분류(`ANL-02`)를 유지할지,
파인튜닝을 무엇에 쓸지가 미결이다. 그 결정에 따라 이 파일을 쓸 분석기가
달라진다. 결정 후에 올린다.
