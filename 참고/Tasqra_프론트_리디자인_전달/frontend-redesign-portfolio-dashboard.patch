diff --git a/backend/app/api/routes/dashboard_router.py b/backend/app/api/routes/dashboard_router.py
index b3f991e..b6d420b 100644
--- a/backend/app/api/routes/dashboard_router.py
+++ b/backend/app/api/routes/dashboard_router.py
@@ -1,5 +1,6 @@
 # =============================================================================
-# 이 파일의 책임: 프로젝트 핵심 현황과 프로젝트 캘린더 조회 엔드포인트다.
+# 이 파일의 책임: 프로젝트 단건·전역 포트폴리오 핵심 현황과 프로젝트
+#   캘린더 조회 엔드포인트를 제공한다.
 # 다른 파일과의 관계: services/dashboard_service.py 를 부르고
 #   schemas/dashboard.py 를 돌려준다.
 # Spring 비교: @RestController + @GetMapping 이다. ProjectAccess 는 인터셉터가
@@ -27,11 +28,40 @@ from datetime import date
 
 from fastapi import APIRouter, Depends, HTTPException, Query
 
-from app.dependencies import ProjectAccess, get_dashboard_service, get_project_access
-from app.schemas.dashboard import DashboardCalendarResponse, DashboardResponse
+from app.dependencies import (
+    ProjectAccess,
+    get_current_user,
+    get_dashboard_service,
+    get_project_access,
+)
+from app.models.user import User
+from app.schemas.dashboard import (
+    DashboardCalendarResponse,
+    DashboardResponse,
+    PortfolioDashboardResponse,
+)
 from app.services.dashboard_service import DashboardService
 
 router = APIRouter(prefix="/api/projects/{project_id}", tags=["dashboard"])
+portfolio_router = APIRouter(prefix="/api/portfolio", tags=["dashboard"])
+
+
+@portfolio_router.get("/dashboard", response_model=PortfolioDashboardResponse)
+def get_portfolio_dashboard(
+    recent_document_limit: int = Query(
+        5, ge=1, le=20, description="프로젝트마다 최근 문서를 몇 건까지 돌려줄지"
+    ),
+    activity_limit: int = Query(
+        20, ge=1, le=100, description="전체 프로젝트의 최근 태스크 활동 수"
+    ),
+    user: User = Depends(get_current_user),
+    service: DashboardService = Depends(get_dashboard_service),
+) -> PortfolioDashboardResponse:
+    return service.get_portfolio(
+        user_id=user.id,
+        recent_document_limit=recent_document_limit,
+        activity_limit=activity_limit,
+    )
 
 
 @router.get("/dashboard", response_model=DashboardResponse)
diff --git a/backend/app/dependencies.py b/backend/app/dependencies.py
index 36dcac0..30dd90e 100644
--- a/backend/app/dependencies.py
+++ b/backend/app/dependencies.py
@@ -322,15 +322,12 @@ def get_dashboard_repository(db: Session = Depends(get_db)) -> DashboardReposito
 # 한다. Depends(...) 는 기본값이라 함수를 정의하는 순간 평가된다.
 def get_dashboard_service(
     dashboard_repository: DashboardRepository = Depends(get_dashboard_repository),
+    project_repository: ProjectRepository = Depends(get_project_repository),
 ) -> DashboardService:
-    # 세션을 넘기지 않는다. 대시보드는 읽기만 해서 commit 할 것이 없고, 조회는
-    # 전부 리포지토리를 거친다. 쓰지 않을 의존성을 받아 두면 나중에 읽는 사람이
-    # "여기서 직접 쿼리를 날리기도 하나" 를 확인해야 한다.
-    #
-    # ProjectRepository 도 넘기지 않는다. 조회 범위가 현재 프로젝트 하나로
-    # 정해져 있어서 멤버십으로 범위를 계산할 필요가 없다 —
-    # get_amount_precedent_service 가 그것을 받는 이유와 대비된다.
-    return DashboardService(dashboard_repository)
+    # 프로젝트 단건 현황은 기존처럼 전달받은 project_id만 읽는다. 전역 포트폴리오
+    # 현황은 ProjectRepository로 현재 사용자의 멤버십 범위를 한 번 계산한 뒤,
+    # DashboardRepository의 bulk 조회로 모은다.
+    return DashboardService(dashboard_repository, project_repository)
 
 
 def get_auth_service(db: Session = Depends(get_db), users: UserRepository = Depends(get_user_repository)) -> AuthService:
diff --git a/backend/app/main.py b/backend/app/main.py
index aa70735..0f4e923 100644
--- a/backend/app/main.py
+++ b/backend/app/main.py
@@ -47,6 +47,7 @@ app.include_router(decision_schedule_router.router)
 # 프로젝트 핵심 현황(DSH-001). 열린 태스크는 tasks 테이블에서 완료되지 않은
 # TODO · IN_PROGRESS 항목을 집계한다.
 app.include_router(dashboard_router.router)
+app.include_router(dashboard_router.portfolio_router)
 # 산출물 생성 대상 미리보기(DLV-001-2). 담당 보현. 만들기(POST)는 아직 없다 —
 # 완료 판정이 "LLM 호출 전에 건수가 보인다" 이므로 미리보기를 먼저 낸다.
 app.include_router(deliverable_router.router)
diff --git a/backend/app/models/enums.py b/backend/app/models/enums.py
index f33699b..b32086b 100644
--- a/backend/app/models/enums.py
+++ b/backend/app/models/enums.py
@@ -7,9 +7,9 @@
 # 다른 파일과의 관계: models/document.py, services/* 에서 이 Enum의 .value를
 #   문자열 컬럼에 그대로 저장한다. document_type은 처음에 "값이 계속 늘어날 수
 #   있다"는 이유로 Enum 없이 자유 문자열로 두었으나, 도메인을 공공 SI·용역
-#   사업으로 좁히면서 9종으로 확정했으므로 DocumentType Enum을 둔다. DB 컬럼은
-#   여전히 String이고 값 제한은 CHECK 제약으로 넣는다(리비전 0008 예정).
-#   CREATE TYPE AS ENUM을 쓰지 않으므로 값이 늘어도 ALTER TYPE이 필요 없다.
+#   사업으로 좁히면서 신규 선택은 8종으로 확정했다. DocumentType은 과거
+#   BILLING 저장값 조회까지 포함하고, 쓰기 경로는 SelectableDocumentType 8종을 쓴다.
+#   DB 컬럼은 String이라 레거시 값을 보존하면서 화면에서 ETC로 호환할 수 있다.
 
 # Spring 비교: Java enum + @Enumerated(EnumType.STRING)과 같은 목적.
 #   차이는 여기서는 SQLAlchemy 컬럼 타입을 Enum으로 강제하지 않고 일반
@@ -95,11 +95,9 @@ class ExtractionStrategy(str, Enum):
 # =============================================================================
 # 문서 유형 — 도메인: 공공 SI · 용역 사업
 #
-# 9종 전부가 금액과 관계가 있다. 도메인을 좁힌 효과다. 범용이었을 때는
-# MANUAL 처럼 금액이 없는 유형이 섞여 있었다.
-# 업로드 시 사용자가 지정하고(document_type_source=USER), 지정하지 않으면
-# 분류 분석기가 채운다(AI). 사람이 AI 판별을 고친 비율(USER_CORRECTED)이
-# 곧 분류 오류율이 되어 별도 정답셋 없이 정확도를 잴 수 있다.
+# 사용자가 새로 선택할 수 있는 유형은 8종이다. BILLING은 과거 저장값을 읽기 위한
+# 호환 코드이며 신규 입력에서는 ETC로 통합한다. DocumentType은 조회 호환 계약,
+# SelectableDocumentType은 업로드·수정 쓰기 계약으로 역할을 분리한다.
 # =============================================================================
 class DocumentType(str, Enum):
     RFP = "RFP"                          # 제안요청서 · 입찰공고
@@ -109,9 +107,21 @@ class DocumentType(str, Enum):
     CONTRACT_CHANGE = "CONTRACT_CHANGE"  # 변경계약서 · 과업변경합의서
     REPORT = "REPORT"                    # 착수 · 주간 · 월간 · 완료보고서 · 검사조서
     MEETING_NOTES = "MEETING_NOTES"      # 회의록
-    BILLING = "BILLING"                  # 대가지급청구서 · 세금계산서
+    BILLING = "BILLING"                  # 레거시 읽기 전용 — 화면·신규 입력은 ETC
     ETC = "ETC"
 
+
+class SelectableDocumentType(str, Enum):
+    RFP = "RFP"
+    PROPOSAL = "PROPOSAL"
+    COST_SHEET = "COST_SHEET"
+    CONTRACT = "CONTRACT"
+    CONTRACT_CHANGE = "CONTRACT_CHANGE"
+    REPORT = "REPORT"
+    MEETING_NOTES = "MEETING_NOTES"
+    ETC = "ETC"
+
+
 class DocumentTypeSource(str, Enum):
     USER = "USER"                        # 업로드 시 사람이 지정
     AI = "AI"                            # 분류 분석기가 판별
diff --git a/backend/app/repositories/dashboard_repository.py b/backend/app/repositories/dashboard_repository.py
index ffa5650..372965c 100644
--- a/backend/app/repositories/dashboard_repository.py
+++ b/backend/app/repositories/dashboard_repository.py
@@ -28,12 +28,12 @@ from __future__ import annotations
 from datetime import date
 
 from sqlalchemy import Select, and_, func, or_, select
-from sqlalchemy.orm import Session
+from sqlalchemy.orm import Session, joinedload
 
 from app.models.amount import AmountItem
 from app.models.document import Document
 from app.models.schedule import ScheduleItem
-from app.models.task import Task
+from app.models.task import Task, TaskActivityLog
 
 # 승인 대기로 셀 상태. amount_items.decision 의 값이다.
 # APPROVED · EDITED 는 사람이 이미 판단한 것이고 REJECTED 는 거절한 것이라
@@ -128,6 +128,112 @@ class DashboardRepository:
         )
         return int(self._db.execute(stmt).scalar_one())
 
+    def count_document_states_by_projects(
+        self, project_ids: list[int]
+    ) -> tuple[dict[int, dict[str, int]], dict[int, dict[str, int]]]:
+        """프로젝트별 문서 상태와 검수 상태를 한 번의 묶음 조회로 센다."""
+        stmt: Select = (
+            select(
+                Document.project_id,
+                Document.status,
+                Document.review_status,
+                func.count(),
+            )
+            .where(Document.project_id.in_(project_ids))
+            .group_by(Document.project_id, Document.status, Document.review_status)
+        )
+        by_status: dict[int, dict[str, int]] = {}
+        by_review: dict[int, dict[str, int]] = {}
+        for project_id, status, review_status, count in self._db.execute(stmt).all():
+            status_counts = by_status.setdefault(int(project_id), {})
+            status_counts[status] = status_counts.get(status, 0) + int(count)
+            review_counts = by_review.setdefault(int(project_id), {})
+            review_counts[review_status] = review_counts.get(review_status, 0) + int(count)
+        return by_status, by_review
+
+    def count_document_types_by_projects(
+        self, project_ids: list[int]
+    ) -> dict[int, list[tuple[str | None, int]]]:
+        stmt: Select = (
+            select(Document.project_id, Document.document_type, func.count())
+            .where(Document.project_id.in_(project_ids))
+            .group_by(Document.project_id, Document.document_type)
+        )
+        result: dict[int, list[tuple[str | None, int]]] = {}
+        for project_id, document_type, count in self._db.execute(stmt).all():
+            result.setdefault(int(project_id), []).append((document_type, int(count)))
+        return result
+
+    def count_pending_amount_items_by_projects(
+        self, project_ids: list[int]
+    ) -> dict[int, int]:
+        stmt: Select = (
+            select(Document.project_id, func.count())
+            .select_from(AmountItem)
+            .join(Document, Document.id == AmountItem.document_id)
+            .where(
+                Document.project_id.in_(project_ids),
+                AmountItem.decision == PENDING_DECISION,
+            )
+            .group_by(Document.project_id)
+        )
+        return {
+            int(project_id): int(count)
+            for project_id, count in self._db.execute(stmt).all()
+        }
+
+    def count_open_tasks_by_projects(self, project_ids: list[int]) -> dict[int, int]:
+        stmt: Select = (
+            select(Task.project_id, func.count())
+            .where(Task.project_id.in_(project_ids), Task.status != "DONE")
+            .group_by(Task.project_id)
+        )
+        return {
+            int(project_id): int(count)
+            for project_id, count in self._db.execute(stmt).all()
+        }
+
+    def list_recent_documents_by_projects(
+        self, *, project_ids: list[int], limit_per_project: int
+    ) -> dict[int, list[Document]]:
+        """각 프로젝트의 최근 문서를 window 순위로 한 번에 가져온다."""
+        ranked = (
+            select(
+                Document.id.label("document_id"),
+                func.row_number()
+                .over(
+                    partition_by=Document.project_id,
+                    order_by=(Document.created_at.desc(), Document.id.desc()),
+                )
+                .label("document_rank"),
+            )
+            .where(Document.project_id.in_(project_ids))
+            .subquery()
+        )
+        stmt: Select = (
+            select(Document)
+            .join(ranked, ranked.c.document_id == Document.id)
+            .where(ranked.c.document_rank <= limit_per_project)
+            .order_by(Document.project_id, Document.created_at.desc(), Document.id.desc())
+        )
+        result: dict[int, list[Document]] = {}
+        for document in self._db.execute(stmt).scalars():
+            result.setdefault(int(document.project_id), []).append(document)
+        return result
+
+    def list_recent_task_activity_by_projects(
+        self, *, project_ids: list[int], limit: int
+    ) -> list[TaskActivityLog]:
+        """접근 가능한 프로젝트 전체에서 가장 최근 활동만 제한해 가져온다."""
+        stmt: Select = (
+            select(TaskActivityLog)
+            .options(joinedload(TaskActivityLog.actor))
+            .where(TaskActivityLog.project_id.in_(project_ids))
+            .order_by(TaskActivityLog.created_at.desc(), TaskActivityLog.id.desc())
+            .limit(limit)
+        )
+        return list(self._db.execute(stmt).scalars())
+
     def list_calendar_tasks(
         self, *, project_id: int, starts_on: date, ends_on: date
     ) -> list[Task]:
diff --git a/backend/app/repositories/document_repository.py b/backend/app/repositories/document_repository.py
index 2b464df..f178e50 100644
--- a/backend/app/repositories/document_repository.py
+++ b/backend/app/repositories/document_repository.py
@@ -193,6 +193,10 @@ class DocumentRepository:
             # URL에서만 쓰는 필터 값이다. 저장된 문서 유형이 아니며, 유형이 아직
             # 정해지지 않은 NULL 행을 대시보드 분포에서 그대로 열기 위해 번역한다.
             query = query.filter(Document.document_type.is_(None))
+        elif document_type == "ETC":
+            # BILLING은 과거 저장값이다. 화면에서는 ETC에 편입되므로 ETC 필터가
+            # 신규 ETC와 레거시 BILLING을 함께 반환해야 목록·통계 의미가 맞는다.
+            query = query.filter(Document.document_type.in_(("ETC", "BILLING")))
         elif document_type:
             query = query.filter(Document.document_type == document_type)
 
diff --git a/backend/app/schemas/dashboard.py b/backend/app/schemas/dashboard.py
index 9f01a03..73feaf4 100644
--- a/backend/app/schemas/dashboard.py
+++ b/backend/app/schemas/dashboard.py
@@ -1,6 +1,6 @@
 # =============================================================================
-# 이 파일의 책임: 프로젝트 핵심 현황(DSH-001)과 프로젝트 캘린더 응답 계약이다.
-#   대시보드 지표, 최근 문서, 태스크 마감·승인 일정 항목의 모양을 정한다.
+# 이 파일의 책임: 프로젝트 단건·전역 포트폴리오 현황(DSH-001)과 프로젝트
+#   캘린더 응답 계약이다. 대시보드 지표와 최근 활동 항목의 모양을 정한다.
 # 다른 파일과의 관계: services/dashboard_service.py 가 이 모델을 채워 돌려주고
 #   api/routes/dashboard_router.py 가 response_model 로 쓴다. 최근 문서 항목은
 #   models/document.py 의 Document 에서 그대로 만든다(from_attributes).
@@ -111,3 +111,29 @@ class DashboardResponse(BaseModel):
     open_tasks: int
     document_types: list[DashboardDocumentTypeCount] = Field(default_factory=list)
     recent_documents: list[DashboardRecentDocument] = Field(default_factory=list)
+
+
+
+class PortfolioProjectDashboard(BaseModel):
+    """전역 화면에서 프로젝트 하나의 집계와 최근 문서를 묶은 읽기 모델."""
+
+    project_id: int
+    dashboard: DashboardResponse
+
+
+class PortfolioTaskActivity(BaseModel):
+    """접근 가능한 프로젝트 전체에서 가져온 최근 태스크 활동 한 줄."""
+
+    model_config = ConfigDict(from_attributes=True)
+
+    id: int
+    project_id: int
+    task_id: int | None
+    task_title: str
+    event_type: str
+    created_at: datetime
+
+
+class PortfolioDashboardResponse(BaseModel):
+    projects: list[PortfolioProjectDashboard] = Field(default_factory=list)
+    recent_task_activity: list[PortfolioTaskActivity] = Field(default_factory=list)
diff --git a/backend/app/schemas/document.py b/backend/app/schemas/document.py
index a12b918..cf779d2 100644
--- a/backend/app/schemas/document.py
+++ b/backend/app/schemas/document.py
@@ -18,7 +18,7 @@ from typing import Any
 
 from pydantic import BaseModel, ConfigDict, Field, model_validator
 
-from app.models.enums import DocumentType, DocumentTypeSource
+from app.models.enums import DocumentTypeSource, SelectableDocumentType
 
 
 class DocumentUploadResponse(BaseModel):
@@ -108,14 +108,14 @@ class DocumentListItem(BaseModel):
 
 
 class DocumentTypeUpdateRequest(BaseModel):
-    document_type: DocumentType
+    document_type: SelectableDocumentType
 
 
 class DocumentTypeUpdateResponse(BaseModel):
     model_config = ConfigDict(from_attributes=True)
 
     id: int
-    document_type: DocumentType
+    document_type: SelectableDocumentType
     document_type_source: DocumentTypeSource
 
 
diff --git a/backend/app/services/dashboard_service.py b/backend/app/services/dashboard_service.py
index 8af0fc4..e10bd81 100644
--- a/backend/app/services/dashboard_service.py
+++ b/backend/app/services/dashboard_service.py
@@ -1,17 +1,16 @@
 # =============================================================================
-# 이 파일의 책임: 프로젝트 핵심 현황(DSH-001)과 프로젝트 캘린더 조회 결과를
-#   만든다. 집계 상태를 화면 묶음으로 접고, 태스크와 일정을 공통 DTO로 합친다.
+# 이 파일의 책임: 프로젝트 단건·전역 포트폴리오 핵심 현황(DSH-001)과
+#   프로젝트 캘린더 조회 결과를 만든다. 집계 상태를 화면 묶음으로 접는다.
 # 다른 파일과의 관계: repositories/dashboard_repository.py 로 조회하고 응답 계약은
 #   schemas/dashboard.py 다. api/routes/dashboard_router.py 가 부른다.
 # Spring 비교: @Service 다. 상태 묶기 규칙이 이 계층에 있는 것은
 #   amount_calculator 의 집계가 서비스에 있는 것과 같은 이유다 — 판단은 SQL 이
 #   아니라 코드에 두고 테스트할 수 있게 한다.
 #
-# 권한을 여기서 다시 확인하지 않는다
-#   라우터의 get_project_access 가 "내가 멤버인 프로젝트인가" 를 이미 판정했고,
-#   그 결과인 project_id 만 받는다. AmountPrecedentService 는 멤버십으로 범위를
-#   계산해야 해서 ProjectRepository 를 받았지만, 대시보드는 범위가 현재 프로젝트
-#   하나로 이미 정해져 있다. 판단 지점을 늘리지 않는다.
+# 권한 범위를 정하는 위치
+#   프로젝트 단건 조회는 라우터의 get_project_access가 판정한 project_id만 받는다.
+#   전역 포트폴리오는 라우터에서 인증한 user_id를 받고 ProjectRepository로 참여
+#   프로젝트 범위를 한 번 계산한다. 이후 bulk 조회는 그 ID 목록 밖을 읽지 않는다.
 #
 # 왜 화면에서 세지 않고 서버에서 세나
 #   기존 대시보드는 문서 목록을 받아 화면에서 셌다. 그런데 그 목록은
@@ -27,8 +26,10 @@ from __future__ import annotations
 import logging
 from datetime import date
 
+from app.models.document import Document
 from app.models.enums import DocumentStatus, ReviewStatus
 from app.repositories.dashboard_repository import DashboardRepository
+from app.repositories.project_repository import ProjectRepository
 from app.schemas.dashboard import (
     DashboardCalendarEvent,
     DashboardCalendarResponse,
@@ -36,6 +37,9 @@ from app.schemas.dashboard import (
     DashboardDocumentTypeCount,
     DashboardRecentDocument,
     DashboardResponse,
+    PortfolioDashboardResponse,
+    PortfolioProjectDashboard,
+    PortfolioTaskActivity,
 )
 
 logger = logging.getLogger(__name__)
@@ -63,8 +67,13 @@ REVIEW_PENDING_STATUSES = (
 
 
 class DashboardService:
-    def __init__(self, dashboard_repository: DashboardRepository) -> None:
+    def __init__(
+        self,
+        dashboard_repository: DashboardRepository,
+        project_repository: ProjectRepository,
+    ) -> None:
         self._dashboard = dashboard_repository
+        self._projects = project_repository
 
     def get_overview(
         self,
@@ -72,36 +81,97 @@ class DashboardService:
         project_id: int,
         recent_limit: int,
     ) -> DashboardResponse:
-        by_status = self._dashboard.count_documents_by_status(project_id)
-        by_review = self._dashboard.count_documents_by_review_status(project_id)
-        type_rows = self._dashboard.count_documents_by_type(project_id)
-        recent = self._dashboard.list_recent_documents(
-            project_id=project_id, limit=recent_limit
+        response = self._build_overview(
+            by_status=self._dashboard.count_documents_by_status(project_id),
+            by_review=self._dashboard.count_documents_by_review_status(project_id),
+            type_rows=self._dashboard.count_documents_by_type(project_id),
+            recent=self._dashboard.list_recent_documents(
+                project_id=project_id, limit=recent_limit
+            ),
+            pending_amounts=self._dashboard.count_pending_amount_items(project_id),
+            open_tasks=self._dashboard.count_open_tasks(project_id),
         )
-        pending_amounts = self._dashboard.count_pending_amount_items(project_id)
-        open_tasks = self._dashboard.count_open_tasks(project_id)
-
-        counts = DashboardDocumentCounts(**document_counts(by_status))
-
-        review_pending = review_pending_count(by_review)
-
         logger.info(
             "대시보드 현황 project_id=%s 문서=%d건 처리중=%d 검수대기=%d 금액승인대기=%d",
             project_id,
-            counts.total,
-            counts.processing,
-            review_pending,
-            pending_amounts,
+            response.documents.total,
+            response.documents.processing,
+            response.review_pending,
+            response.pending_amount_items,
+        )
+        return response
+
+    def get_portfolio(
+        self,
+        *,
+        user_id: int,
+        recent_document_limit: int,
+        activity_limit: int,
+    ) -> PortfolioDashboardResponse:
+        """사용자가 참여한 모든 프로젝트의 전역 현황을 고정된 수의 쿼리로 만든다."""
+        memberships = self._projects.list_for_user(user_id)
+        project_ids = [int(project.id) for project, _member in memberships]
+        if not project_ids:
+            return PortfolioDashboardResponse()
+
+        by_status, by_review = self._dashboard.count_document_states_by_projects(
+            project_ids
+        )
+        types = self._dashboard.count_document_types_by_projects(project_ids)
+        pending_amounts = self._dashboard.count_pending_amount_items_by_projects(
+            project_ids
+        )
+        open_tasks = self._dashboard.count_open_tasks_by_projects(project_ids)
+        recent_documents = self._dashboard.list_recent_documents_by_projects(
+            project_ids=project_ids,
+            limit_per_project=recent_document_limit,
+        )
+        activity = self._dashboard.list_recent_task_activity_by_projects(
+            project_ids=project_ids,
+            limit=activity_limit,
+        )
+
+        return PortfolioDashboardResponse(
+            projects=[
+                PortfolioProjectDashboard(
+                    project_id=project_id,
+                    dashboard=self._build_overview(
+                        by_status=by_status.get(project_id, {}),
+                        by_review=by_review.get(project_id, {}),
+                        type_rows=types.get(project_id, []),
+                        recent=recent_documents.get(project_id, []),
+                        pending_amounts=pending_amounts.get(project_id, 0),
+                        open_tasks=open_tasks.get(project_id, 0),
+                    ),
+                )
+                for project_id in project_ids
+            ],
+            recent_task_activity=[
+                PortfolioTaskActivity.model_validate(item) for item in activity
+            ],
         )
 
+    @staticmethod
+    def _build_overview(
+        *,
+        by_status: dict[str, int],
+        by_review: dict[str, int],
+        type_rows: list[tuple[str | None, int]],
+        recent: list[Document],
+        pending_amounts: int,
+        open_tasks: int,
+    ) -> DashboardResponse:
+        counts = DashboardDocumentCounts(**document_counts(by_status))
         return DashboardResponse(
             documents=counts,
-            review_pending=review_pending,
+            review_pending=review_pending_count(by_review),
             pending_amount_items=pending_amounts,
             open_tasks=open_tasks,
             document_types=[
                 DashboardDocumentTypeCount(document_type=document_type, count=count)
-                for document_type, count in sort_type_rows(type_rows)
+                for document_type, count in sort_type_rows(
+                    merge_legacy_document_types(type_rows)
+                )
             ],
             recent_documents=[
                 DashboardRecentDocument.model_validate(document) for document in recent
@@ -193,6 +263,21 @@ def _sum_of(counts: dict[str, int], keys: tuple[str, ...]) -> int:
     return sum(counts.get(key, 0) for key in keys)
 
 
+def merge_legacy_document_types(
+    rows: list[tuple[str | None, int]],
+) -> list[tuple[str | None, int]]:
+    """레거시 BILLING 건수를 ETC에 합쳐 사용자에게 8종 분포로 보여준다.
+
+    DB 값은 마이그레이션 없이 보존한다. 읽기 모델에서만 합치므로 기존 문서도
+    사라지지 않고, ETC 필터의 Repository 호환 규칙과 같은 의미가 된다.
+    """
+    merged: dict[str | None, int] = {}
+    for document_type, count in rows:
+        canonical = "ETC" if document_type == "BILLING" else document_type
+        merged[canonical] = merged.get(canonical, 0) + count
+    return list(merged.items())
+
+
 def sort_type_rows(rows: list[tuple[str | None, int]]) -> list[tuple[str | None, int]]:
     """많은 유형부터. 유형이 없는 칸(None)은 언제나 맨 끝이다.
 
diff --git a/backend/app/services/document_service.py b/backend/app/services/document_service.py
index d965857..af8a60e 100644
--- a/backend/app/services/document_service.py
+++ b/backend/app/services/document_service.py
@@ -26,7 +26,7 @@ from app.core.exceptions import BusinessError
 from app.core.transaction import transactional
 from app.extractors.layout import LayoutElement
 from app.models.document import Analysis, Document, OcrElement, OcrElementRevision, OcrMergeOperation, OcrStructureEvent
-from app.models.enums import AnalyzerType, DocumentType, DocumentTypeSource, ReviewStatus
+from app.models.enums import AnalyzerType, DocumentTypeSource, ReviewStatus, SelectableDocumentType
 from app.extractors.ocr_extractor import OcrExtractor
 from app.extractors.reading_order import build_reading_groups
 from app.repositories.analysis_repository import AnalysisRepository
@@ -148,7 +148,7 @@ class DocumentService:
         self,
         project_id: int,
         document_id: int,
-        document_type: DocumentType,
+        document_type: SelectableDocumentType,
     ) -> Document:
         """사용자가 현재 문서 유형을 고치고 AI 자동 갱신 대상에서 제외한다."""
         with transactional(self._db):
diff --git a/backend/app/services/extraction_service.py b/backend/app/services/extraction_service.py
index 4523bf4..318357d 100644
--- a/backend/app/services/extraction_service.py
+++ b/backend/app/services/extraction_service.py
@@ -256,9 +256,12 @@ class ExtractionService:
         if value is None or not value.strip():
             return None
         try:
-            return DocumentType(value.strip().upper())
+            document_type = DocumentType(value.strip().upper())
         except ValueError as exc:
             raise BusinessError(ErrorCode.INVALID_DOCUMENT_TYPE) from exc
+        if document_type is DocumentType.BILLING:
+            raise BusinessError(ErrorCode.INVALID_DOCUMENT_TYPE)
+        return document_type
 
     @staticmethod
     def _sanitize_filename(filename: str) -> str:
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index fb9a3ae..85ba77b 100644
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -13,6 +13,7 @@ import './styles/app.css'
 import './styles/states.css'
 import './styles/responsive.css'
 import './styles/design-refresh.css'
+import './styles/unified-ui.css'
 import { applyThemeColor, getSavedThemeColor } from './utils/theme'
 
 applyThemeColor(getSavedThemeColor())
diff --git a/frontend/src/api/dashboard.js b/frontend/src/api/dashboard.js
index 4af8726..8db9ac4 100644
--- a/frontend/src/api/dashboard.js
+++ b/frontend/src/api/dashboard.js
@@ -1,6 +1,6 @@
 // =============================================================================
-// 이 파일의 책임: 프로젝트 핵심 현황과 프로젝트 캘린더 API 호출을 감싼다.
-//   화면은 axios나 URL 계약을 모르고 이 파일의 함수만 부른다.
+// 이 파일의 책임: 프로젝트 단건·전역 포트폴리오 핵심 현황과 프로젝트
+//   캘린더 API 호출을 감싼다.
 // 다른 파일과의 관계: api/http.js 의 공통 인스턴스를 쓴다 — 토큰 첨부와 401
 //   재발급이 거기 인터셉터에 있다. 응답 필드 이름은 서버와 같은 snake_case
 //   그대로 둔다(프로젝트 합의).
@@ -27,6 +27,18 @@ import { http } from './http'
 //                        status, review_status, created_at }]
 // }
 //
+export async function getPortfolioDashboard(
+  { recentDocumentLimit = 5, activityLimit = 20 } = {},
+) {
+  const { data } = await http.get('/api/portfolio/dashboard', {
+    params: {
+      recent_document_limit: recentDocumentLimit,
+      activity_limit: activityLimit,
+    },
+  })
+  return data
+}
+
 export async function getDashboard(projectId, { recentLimit = 5 } = {}) {
   const { data } = await http.get(`/api/projects/${projectId}/dashboard`, {
     params: { recent_limit: recentLimit },
diff --git a/frontend/src/features/projects/PortfolioDashboard.css b/frontend/src/features/projects/PortfolioDashboard.css
new file mode 100644
index 0000000..8b43a2f
--- /dev/null
+++ b/frontend/src/features/projects/PortfolioDashboard.css
@@ -0,0 +1,14 @@
+.projects-home{min-width:320px;min-height:100svh;background:var(--c-bg-canvas)}.projects-home>.app-header{position:sticky;top:0;height:64px;padding-inline:max(18px,calc((100vw - 1460px)/2));border-color:var(--c-border);background:rgba(255,255,255,.95);backdrop-filter:blur(14px)}
+.portfolio-main{width:min(1460px,100%);margin:auto;padding:28px clamp(18px,3vw,44px) 64px}.portfolio-welcome{display:flex;align-items:flex-end;justify-content:space-between;gap:22px;margin-bottom:20px}.portfolio-welcome h1{margin:6px 0 5px;color:var(--c-text-strong);font-size:clamp(26px,3vw,34px);letter-spacing:-.04em}.portfolio-welcome>div>p:last-child{margin:0;color:var(--c-text-muted);font-size:13px}.portfolio-create{flex:0 0 auto;min-height:42px}.portfolio-data-notice{margin-bottom:14px;padding:11px 14px;border:1px solid #f2d3a0;border-radius:10px;background:var(--c-warning-bg);color:var(--c-warning);font-size:12px}
+.portfolio-stat-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin-bottom:14px}.portfolio-stat-card{display:flex;min-width:0;min-height:106px;align-items:center;gap:14px;padding:17px;border:1px solid var(--c-border);border-radius:var(--c-radius);background:#fff;box-shadow:var(--c-shadow-sm)}.portfolio-stat-card>svg{width:42px;height:42px;padding:10px;flex:0 0 auto;border-radius:12px;background:var(--c-accent-soft);color:var(--c-accent)}.portfolio-stat-card>div{min-width:0}.portfolio-stat-card span,.portfolio-stat-card small,.portfolio-stat-card strong{display:block}.portfolio-stat-card span{color:var(--c-text-muted);font-size:11px;font-weight:700}.portfolio-stat-card strong{margin-top:3px;color:var(--c-text-strong);font-size:25px;line-height:1.15}.portfolio-stat-card small{margin-top:4px;color:#94a3b8;font-size:10px}.portfolio-stat-card.is-danger>svg{background:var(--c-danger-bg);color:var(--c-danger)}
+.portfolio-layout{display:grid;grid-template-columns:minmax(0,2.1fr) minmax(300px,.9fr);gap:14px;align-items:start}.portfolio-projects{min-width:0;min-height:620px;padding:0;overflow:hidden}.portfolio-panel-head{display:flex;align-items:flex-start;justify-content:space-between;gap:14px;padding:17px 18px 13px}.portfolio-panel-head h2{margin:0;color:var(--c-text-strong);font-size:16px}.portfolio-panel-head p{margin:4px 0 0;color:var(--c-text-muted);font-size:11px}.portfolio-panel-head>span{color:var(--c-accent);font-size:11px;font-weight:800}.portfolio-project-tools{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:0 18px 12px;border-bottom:1px solid var(--c-border)}.portfolio-filter-tabs button{padding:8px 0;border:0;border-bottom:2px solid var(--c-accent);background:transparent;color:var(--c-accent);font-size:11px;font-weight:800}.portfolio-project-tools label{width:min(220px,45%)}.portfolio-project-tools input{width:100%;min-height:36px;padding:0 12px;border:1px solid var(--c-border);border-radius:8px;background:#fff;color:var(--c-text-strong);outline:none}.portfolio-project-tools input:focus{border-color:var(--c-accent);box-shadow:0 0 0 3px var(--c-accent-bg)}.portfolio-project-list{display:grid;gap:8px;padding:12px}
+.portfolio-project-row{display:grid;width:100%;min-width:0;grid-template-columns:42px minmax(170px,1.45fr) minmax(130px,.8fr) repeat(3,minmax(54px,.38fr)) minmax(62px,.45fr) 18px;align-items:center;gap:12px;padding:13px;border:1px solid var(--c-border);border-radius:11px;background:#fff;color:inherit;text-align:left;cursor:pointer;transition:border-color .16s,box-shadow .16s,transform .16s}.portfolio-project-row:hover{border-color:var(--c-accent-border);box-shadow:var(--c-shadow-sm);transform:translateY(-1px)}.portfolio-project-mark{display:grid;width:38px;height:38px;place-items:center;border-radius:10px;background:var(--c-accent-soft);color:var(--c-accent)}.portfolio-project-mark svg{width:19px;height:19px}.portfolio-project-name,.portfolio-project-metric,.portfolio-project-role{min-width:0}.portfolio-project-name strong,.portfolio-project-name small,.portfolio-project-metric strong,.portfolio-project-metric small,.portfolio-project-role strong,.portfolio-project-role small{display:block}.portfolio-project-name strong{overflow:hidden;color:var(--c-text-strong);font-size:12px;text-overflow:ellipsis;white-space:nowrap}.portfolio-project-name small{margin-top:5px;color:var(--c-success);font-size:9px}.portfolio-project-name small i{display:inline-block;width:5px;height:5px;margin-right:5px;border-radius:50%;background:currentColor}.portfolio-project-name small .status-archived{color:var(--c-text-muted)}.portfolio-progress small{display:flex;justify-content:space-between;color:var(--c-text-muted);font-size:9px}.portfolio-progress small b{color:var(--c-text-strong)}.portfolio-progress>i{display:block;height:5px;margin-top:7px;overflow:hidden;border-radius:99px;background:#e9edf4}.portfolio-progress>i>b{display:block;height:100%;border-radius:inherit;background:var(--c-accent)}.portfolio-project-metric small,.portfolio-project-role small{color:#94a3b8;font-size:8px}.portfolio-project-metric strong,.portfolio-project-role strong{margin-top:5px;color:var(--c-text-strong);font-size:11px}.portfolio-project-metric.is-danger strong{color:var(--c-danger)}.portfolio-project-role strong{white-space:nowrap}.portfolio-open{color:var(--c-accent);font-size:20px}.portfolio-new-row{width:calc(100% - 24px);min-height:43px;margin:0 12px 14px;border:1px dashed var(--c-accent-border);border-radius:10px;background:var(--c-accent-soft);color:var(--c-accent);font-size:12px;font-weight:800;cursor:pointer}.portfolio-new-row:hover{border-style:solid}.portfolio-empty{padding:30px;text-align:center;color:var(--c-text-muted);font-size:12px}
+.portfolio-side{display:grid;min-width:0;gap:12px}.portfolio-side>.panel{padding:0;overflow:hidden}.portfolio-attention-list{display:grid;gap:6px;padding:0 10px 10px}.portfolio-attention-list button{display:grid;min-width:0;grid-template-columns:minmax(0,1fr) auto 12px;align-items:center;gap:8px;padding:10px;border:1px solid var(--c-border);border-radius:9px;background:#fff;text-align:left;cursor:pointer}.portfolio-attention-list button:hover{border-color:var(--c-accent-border);background:var(--c-accent-soft)}.portfolio-attention-list button>span{min-width:0}.portfolio-attention-list button b,.portfolio-attention-list button small{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.portfolio-attention-list button b{color:var(--c-text-strong);font-size:10px}.portfolio-attention-list button small{margin-top:3px;color:var(--c-text-muted);font-size:8px}.portfolio-attention-list button strong{color:var(--c-text-strong);font-size:11px}.portfolio-attention-list button i{color:var(--c-accent);font-size:15px}.portfolio-attention-empty{padding:0 12px 16px;color:var(--c-text-muted);font-size:10px}.portfolio-invite-note{margin:0;padding:8px 12px;border-top:1px solid var(--c-border);background:var(--c-accent-soft);color:var(--c-accent);font-size:9px}
+.portfolio-recent ul{margin:0;padding:0 12px 10px;list-style:none}.portfolio-recent li+li{border-top:1px solid var(--c-border)}.portfolio-recent li button{display:flex;width:100%;min-width:0;align-items:center;gap:9px;padding:10px 0;border:0;background:transparent;color:inherit;text-align:left;cursor:pointer}.portfolio-recent li svg{width:25px;height:25px;padding:5px;flex:0 0 auto;border-radius:7px;background:var(--c-accent-soft);color:var(--c-accent)}.portfolio-recent li span{min-width:0;flex:1}.portfolio-recent li strong,.portfolio-recent li small{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.portfolio-recent li strong{color:var(--c-text-strong);font-size:10px}.portfolio-recent li small{margin-top:3px;color:var(--c-text-muted);font-size:8px}.portfolio-recent time{flex:0 0 auto;color:#94a3b8;font-size:8px}
+.portfolio-quick>div{display:grid;grid-template-columns:repeat(3,1fr);gap:6px;padding:0 10px 11px}.portfolio-quick>div button{display:flex;min-width:0;min-height:44px;align-items:center;justify-content:center;gap:5px;padding:6px;border:1px solid var(--c-border);border-radius:8px;background:#fff;color:var(--c-text);font-size:9px;cursor:pointer}.portfolio-quick>div button:hover:not(:disabled){border-color:var(--c-accent-border);color:var(--c-accent)}.portfolio-quick button svg{width:13px;height:13px}.portfolio-quick button:disabled{opacity:.45}
+.portfolio-chart{--chart-0:var(--c-accent);--chart-1:#57b3e8;--chart-2:#3fc08d;--chart-3:#ef6a70;--chart-4:#f1ad4e}.portfolio-chart__body{display:flex;align-items:center;gap:20px;padding:3px 18px 18px}.portfolio-donut{display:grid;width:116px;height:116px;flex:0 0 auto;place-items:center;border-radius:50%}.portfolio-donut>span{display:grid;width:74px;height:74px;place-items:center;align-content:center;border-radius:50%;background:#fff}.portfolio-donut strong,.portfolio-donut small{display:block;text-align:center}.portfolio-donut strong{color:var(--c-text-strong);font-size:15px}.portfolio-donut small{color:var(--c-text-muted);font-size:8px}.portfolio-chart ul{min-width:0;flex:1;margin:0;padding:0;list-style:none}.portfolio-chart li{display:grid;grid-template-columns:7px minmax(0,1fr) auto;align-items:center;gap:7px;padding:3px 0;font-size:9px}.portfolio-chart li i{width:7px;height:7px;border-radius:50%}.portfolio-chart li .chart-0{background:var(--chart-0)}.portfolio-chart li .chart-1{background:var(--chart-1)}.portfolio-chart li .chart-2{background:var(--chart-2)}.portfolio-chart li .chart-3{background:var(--chart-3)}.portfolio-chart li .chart-4{background:var(--chart-4)}.portfolio-chart li span{color:var(--c-text-muted)}.portfolio-chart li strong{color:var(--c-text-strong)}
+.projects-error{width:min(900px,calc(100% - 36px));margin:40px auto}
+@media(max-width:1180px){.portfolio-project-row{grid-template-columns:42px minmax(150px,1.3fr) minmax(120px,.8fr) repeat(2,minmax(48px,.35fr)) 18px}.portfolio-project-row>.portfolio-project-metric:nth-of-type(4),.portfolio-project-role{display:none}.portfolio-layout{grid-template-columns:minmax(0,1.65fr) minmax(290px,.85fr)}}
+@media(max-width:900px){.portfolio-stat-grid{grid-template-columns:repeat(2,1fr)}.portfolio-layout{grid-template-columns:1fr}.portfolio-projects{min-height:0}.portfolio-side{grid-template-columns:1fr 1fr}.portfolio-chart,.portfolio-recent{grid-row:span 2}}
+@media(max-width:650px){.portfolio-main{padding:22px 14px 48px}.portfolio-welcome{align-items:flex-start;flex-direction:column}.portfolio-create{width:100%}.portfolio-stat-grid{grid-template-columns:1fr 1fr;gap:8px}.portfolio-stat-card{min-height:90px;padding:13px}.portfolio-stat-card>svg{width:36px;height:36px}.portfolio-project-tools{align-items:stretch;flex-direction:column}.portfolio-project-tools label{width:100%}.portfolio-project-row{grid-template-columns:38px minmax(0,1fr) auto}.portfolio-project-row>.portfolio-progress,.portfolio-project-row>.portfolio-project-metric,.portfolio-project-row>.portfolio-project-role{display:none}.portfolio-project-mark{width:36px;height:36px}.portfolio-open{display:block}.portfolio-side{grid-template-columns:1fr}.portfolio-chart,.portfolio-recent{grid-row:auto}}
+@media(max-width:390px){.portfolio-stat-grid{grid-template-columns:1fr}.portfolio-stat-card{min-height:82px}}
diff --git a/frontend/src/features/projects/PortfolioDashboard.jsx b/frontend/src/features/projects/PortfolioDashboard.jsx
new file mode 100644
index 0000000..4d091e7
--- /dev/null
+++ b/frontend/src/features/projects/PortfolioDashboard.jsx
@@ -0,0 +1,186 @@
+// =============================================================================
+// 이 파일의 책임: 로그인 직후 모든 참여 프로젝트의 실제 현황을 한 화면에 모은다.
+// 다른 파일과의 관계: portfolio dashboard API를 한 번 조회해 기존 프로젝트 목록과
+//   결합하고, 각 프로젝트 작업공간 경로로 이동한다.
+// Spring 비교: 여러 프로젝트의 읽기 모델을 소비하는 전용 Dashboard View다.
+// =============================================================================
+
+import { useQuery } from '@tanstack/react-query'
+import { useMemo, useState } from 'react'
+import { useNavigate } from 'react-router-dom'
+import { getPortfolioDashboard } from '../../api/dashboard'
+import { formatNumber } from '../../utils/format'
+import './PortfolioDashboard.css'
+
+const ACTIVITY_LABELS = {
+  CREATED: '태스크 생성',
+  UPDATED: '태스크 수정',
+  STATUS_CHANGED: '상태 변경',
+  DELETED: '태스크 삭제',
+  AUTO_NOTE: '자동 메모',
+}
+
+export default function PortfolioDashboard({ user, projects, invitations, onCreate }) {
+  const navigate = useNavigate()
+  const [query, setQuery] = useState('')
+  const portfolioQuery = useQuery({
+    queryKey: ['portfolio-dashboard', user.id],
+    queryFn: () => getPortfolioDashboard({ recentDocumentLimit: 5, activityLimit: 20 }),
+    staleTime: 0,
+    refetchOnMount: 'always',
+  })
+  const dashboardByProject = useMemo(
+    () => new Map((portfolioQuery.data?.projects ?? []).map(item => [Number(item.project_id), item.dashboard])),
+    [portfolioQuery.data],
+  )
+  const rows = projects.map(project => ({
+    project,
+    dashboard: dashboardByProject.get(Number(project.id)),
+  }))
+  const summary = rows.reduce((total, row) => {
+    const data = row.dashboard
+    total.documents += data?.documents?.total ?? 0
+    total.openTasks += data?.open_tasks ?? 0
+    total.failed += data?.documents?.failed ?? 0
+    total.review += data?.review_pending ?? 0
+    total.amounts += data?.pending_amount_items ?? 0
+    total.completed += data?.documents?.completed ?? 0
+    total.extracted += data?.documents?.extracted ?? 0
+    total.processing += data?.documents?.processing ?? 0
+    return total
+  }, { documents:0, openTasks:0, failed:0, review:0, amounts:0, completed:0, extracted:0, processing:0 })
+
+  const filteredRows = rows.filter(({ project }) => project.name.toLocaleLowerCase().includes(query.trim().toLocaleLowerCase()))
+  const recentWork = useMemo(() => buildRecentWork(projects, portfolioQuery.data), [projects, portfolioQuery.data])
+  const waitingTotal = summary.review + summary.amounts + summary.failed + invitations.length
+  const loading = portfolioQuery.isPending
+  const today = new Intl.DateTimeFormat('ko-KR', { month:'long', day:'numeric', weekday:'long' }).format(new Date())
+
+  return <main className="portfolio-main">
+    <section className="portfolio-welcome">
+      <div><p className="eyebrow">WORKSPACE OVERVIEW</p><h1>{user.name}님, 좋은 하루예요.</h1><p>{today} · 전체 프로젝트 현황과 지금 확인할 일을 한눈에 살펴보세요.</p></div>
+      <button className="primary portfolio-create" onClick={onCreate}>＋ 새 프로젝트</button>
+    </section>
+
+    {portfolioQuery.isError && <div className="portfolio-data-notice">전체 프로젝트 현황을 불러오지 못했습니다. 잠시 후 다시 시도해 주세요.</div>}
+
+    <section className="portfolio-stat-grid" aria-label="전체 프로젝트 요약">
+      <StatCard icon="folder" label="전체 프로젝트" value={projects.length} note="참여 중인 프로젝트"/>
+      <StatCard icon="document" label="등록 문서" value={loading ? null : summary.documents} note="전체 프로젝트 합계"/>
+      <StatCard icon="task" label="열린 태스크" value={loading ? null : summary.openTasks} note="완료 전 태스크"/>
+      <StatCard icon="alert" label="확인 필요" value={loading ? null : waitingTotal} note="검수·승인·실패·초대" tone={waitingTotal ? 'danger' : ''}/>
+    </section>
+
+    <div className="portfolio-layout">
+      <section className="portfolio-projects panel">
+        <header className="portfolio-panel-head"><div><h2>내 프로젝트</h2><p>프로젝트를 선택하면 기존 작업 공간으로 이동합니다.</p></div><span>{filteredRows.length}개</span></header>
+        <div className="portfolio-project-tools"><div className="portfolio-filter-tabs"><button className="is-active">전체 {projects.length}</button></div><label><span className="sr-only">프로젝트 검색</span><input value={query} onChange={event => setQuery(event.target.value)} placeholder="프로젝트 검색"/></label></div>
+        <div className="portfolio-project-list">{filteredRows.map(({ project, dashboard }, index) => <ProjectRow key={project.id} project={project} dashboard={dashboard} marker={index % 5} onOpen={() => navigate(`/projects/${project.id}/dashboard`)}/>)}</div>
+        {filteredRows.length === 0 && <div className="portfolio-empty">검색 조건에 맞는 프로젝트가 없습니다.</div>}
+        <button className="portfolio-new-row" onClick={onCreate}>＋ 새 프로젝트 만들기</button>
+      </section>
+
+      <aside className="portfolio-side">
+        <AttentionCard invitations={invitations} onOpen={(projectId, tab, queryString='') => navigate(`/projects/${projectId}/${tab}${queryString}`)} rows={rows}/>
+        <RecentWork items={recentWork} onOpen={navigate}/>
+        <QuickStart projects={projects} onCreate={onCreate} onOpen={navigate}/>
+        <ProcessingChart summary={summary}/>
+      </aside>
+    </div>
+  </main>
+}
+
+function StatCard({ icon, label, value, note, tone='' }) {
+  return <article className={`portfolio-stat-card ${tone ? `is-${tone}` : ''}`}><Icon name={icon}/><div><span>{label}</span><strong>{value === null ? '—' : formatNumber(value)}</strong><small>{note}</small></div></article>
+}
+
+function ProjectRow({ project, dashboard, marker, onOpen }) {
+  const documents = dashboard?.documents
+  const completion = documents?.total ? Math.round((documents.completed / documents.total) * 100) : 0
+  return <button className="portfolio-project-row" onClick={onOpen}>
+    <span className={`portfolio-project-mark marker-${marker}`}><Icon name="folder"/></span>
+    <span className="portfolio-project-name"><strong>{project.name}</strong><small><i className={`status-${project.status?.toLowerCase()}`}/>{projectStatus(project.status)}</small></span>
+    <span className="portfolio-progress"><small>문서 처리율 <b>{dashboard ? `${completion}%` : '—'}</b></small><i><b style={{ width:`${completion}%` }}/></i></span>
+    <span className="portfolio-project-metric"><small>문서</small><strong>{dashboard ? formatNumber(documents?.total ?? 0) : '—'}</strong></span>
+    <span className="portfolio-project-metric"><small>열린 태스크</small><strong>{dashboard ? formatNumber(dashboard.open_tasks ?? 0) : '—'}</strong></span>
+    <span className={`portfolio-project-metric ${(dashboard?.documents?.failed ?? 0) > 0 ? 'is-danger' : ''}`}><small>처리 실패</small><strong>{dashboard ? formatNumber(documents?.failed ?? 0) : '—'}</strong></span>
+    <span className="portfolio-project-role"><small>역할</small><strong>{roleLabel(project.role)}</strong></span>
+    <span className="portfolio-open">›</span>
+  </button>
+}
+
+function AttentionCard({ invitations, rows, onOpen }) {
+  const definitions = [
+    ['OCR 검수', data => data?.review_pending ?? 0, 'documents', '?document_state=REVIEW_REQUIRED'],
+    ['금액 승인', data => data?.pending_amount_items ?? 0, 'amounts', ''],
+    ['처리 실패', data => data?.documents?.failed ?? 0, 'documents', '?document_state=FAILED'],
+  ]
+  const items = rows.flatMap(({ project, dashboard }) => definitions
+    .map(([label, countOf, tab, query]) => ({ project, label, count:countOf(dashboard), tab, query }))
+    .filter(item => item.count > 0))
+    .sort((a,b) => b.count - a.count || a.project.name.localeCompare(b.project.name, 'ko'))
+
+  return <section className="portfolio-attention panel">
+    <header className="portfolio-panel-head"><div><h2>지금 확인할 일</h2><p>프로젝트별 항목을 선택해 바로 확인하세요.</p></div></header>
+    {items.length ? <div className="portfolio-attention-list">{items.map(item => <button key={`${item.project.id}-${item.label}`} onClick={() => onOpen(item.project.id,item.tab,item.query)}><span><b>{item.project.name}</b><small>{item.label}</small></span><strong>{formatNumber(item.count)}건</strong><i>›</i></button>)}</div> : <div className="portfolio-attention-empty">현재 확인할 항목이 없습니다.</div>}
+    {invitations.length > 0 && <p className="portfolio-invite-note">새 프로젝트 초대 {invitations.length}건은 상단 알림에서 확인할 수 있습니다.</p>}
+  </section>
+}
+
+function RecentWork({ items, onOpen }) {
+  return <section className="portfolio-recent panel"><header className="portfolio-panel-head"><div><h2>최근 작업</h2><p>전체 프로젝트의 최근 문서와 태스크 활동입니다.</p></div></header>{items.length ? <ul>{items.slice(0,5).map(item => <li key={item.key}><button onClick={() => onOpen(item.path)}><Icon name={item.kind === 'document' ? 'document' : 'task'}/><span><strong>{item.title}</strong><small>{item.projectName} · {item.description}</small></span><time>{relativeDate(item.createdAt)}</time></button></li>)}</ul> : <div className="portfolio-empty">아직 최근 작업이 없습니다.</div>}</section>
+}
+
+function QuickStart({ projects, onCreate, onOpen }) {
+  const first = projects[0]
+  return <section className="portfolio-quick panel"><header className="portfolio-panel-head"><div><h2>빠른 시작</h2></div></header><div><button disabled={!first} onClick={() => first && onOpen(`/projects/${first.id}/documents`)}><Icon name="upload"/>문서 업로드</button><button onClick={onCreate}><Icon name="folder"/>프로젝트</button><button disabled={!first} onClick={() => first && onOpen(`/projects/${first.id}/board`)}><Icon name="task"/>보드 열기</button></div></section>
+}
+
+function ProcessingChart({ summary }) {
+  const known = summary.completed + summary.extracted + summary.processing + summary.failed
+  const waiting = Math.max(0, summary.documents - known)
+  const values = [summary.completed, summary.extracted, summary.processing, summary.failed, waiting]
+  let cursor = 0
+  const stops = summary.documents > 0
+    ? values.map((value,index) => { const start=cursor; cursor += value / summary.documents * 100; return `var(--chart-${index}) ${start}% ${cursor}%` }).join(',')
+    : ''
+  const background = summary.documents > 0 ? `conic-gradient(${stops})` : 'var(--c-border)'
+  const chartRows = [['처리 완료',summary.completed],['추출 완료',summary.extracted],['처리 중',summary.processing],['처리 실패',summary.failed],['대기·기타',waiting]]
+  return <section className="portfolio-chart panel"><header className="portfolio-panel-head"><div><h2>문서 처리 현황</h2><p>전체 프로젝트의 현재 처리 단계입니다.</p></div></header><div className="portfolio-chart__body"><div className={`portfolio-donut ${summary.documents ? '' : 'is-empty'}`} style={{ background }}><span><strong>{formatNumber(summary.documents)}</strong><small>전체 문서</small></span></div><ul>{chartRows.map(([label,value],index) => <li key={label}><i className={`chart-${index}`}/><span>{label}</span><strong>{formatNumber(value)}</strong></li>)}</ul></div></section>
+}
+
+function buildRecentWork(projects, portfolio) {
+  const items = []
+  const projectById = new Map(projects.map(project => [Number(project.id), project]))
+  ;(portfolio?.projects ?? []).forEach(item => {
+    const project = projectById.get(Number(item.project_id))
+    if (!project) return
+    ;(item.dashboard?.recent_documents ?? []).forEach(document => items.push({ key:`document-${project.id}-${document.id}`,kind:'document',title:document.filename,projectName:project.name,description:'문서 업로드',createdAt:document.created_at,path:`/projects/${project.id}/documents/${document.id}` }))
+  })
+  ;(portfolio?.recent_task_activity ?? []).forEach(activity => {
+    const project = projectById.get(Number(activity.project_id))
+    if (!project) return
+    items.push({ key:`activity-${project.id}-${activity.id}`,kind:'task',title:activity.task_title || '태스크',projectName:project.name,description:ACTIVITY_LABELS[activity.event_type] ?? activity.event_type,createdAt:activity.created_at,path:`/projects/${project.id}/board${activity.task_id ? `?task_id=${activity.task_id}` : ''}` })
+  })
+  return items.filter(item => item.createdAt).sort((a,b) => new Date(b.createdAt) - new Date(a.createdAt)).slice(0,12)
+}
+
+function relativeDate(value) {
+  const diff = Date.now() - new Date(value).getTime()
+  if (diff < 60_000) return '방금'
+  if (diff < 3_600_000) return `${Math.max(1,Math.floor(diff/60_000))}분 전`
+  if (diff < 86_400_000) return `${Math.floor(diff/3_600_000)}시간 전`
+  if (diff < 604_800_000) return `${Math.floor(diff/86_400_000)}일 전`
+  return new Intl.DateTimeFormat('ko-KR',{month:'numeric',day:'numeric'}).format(new Date(value))
+}
+function projectStatus(status){ return status === 'ARCHIVED' ? '보관됨' : status === 'COMPLETED' ? '완료' : '진행 중' }
+function roleLabel(role){ return role === 'OWNER' ? '소유자' : role === 'EDITOR' ? '편집자' : '뷰어' }
+
+function Icon({ name }) {
+  const common={viewBox:'0 0 24 24',fill:'none',stroke:'currentColor',strokeWidth:'1.8','aria-hidden':true}
+  if(name==='folder') return <svg {...common}><path d="M3 7a2 2 0 0 1 2-2h5l2 3h7a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2Z"/></svg>
+  if(name==='document') return <svg {...common}><path d="M6 3h8l4 4v14H6zM14 3v5h5M9 12h6M9 16h6"/></svg>
+  if(name==='task') return <svg {...common}><circle cx="12" cy="12" r="9"/><path d="m8 12 2.5 2.5L16 9"/></svg>
+  if(name==='alert') return <svg {...common}><path d="M10.3 4.2 2.9 17a2 2 0 0 0 1.7 3h14.8a2 2 0 0 0 1.7-3L13.7 4.2a2 2 0 0 0-3.4 0ZM12 9v4M12 17h.01"/></svg>
+  return <svg {...common}><path d="M12 16V4m0 0L7 9m5-5 5 5M4 15v5h16v-5"/></svg>
+}
diff --git a/frontend/src/hooks/useInvitationsQuery.js b/frontend/src/hooks/useInvitationsQuery.js
index 7979f98..a926225 100644
--- a/frontend/src/hooks/useInvitationsQuery.js
+++ b/frontend/src/hooks/useInvitationsQuery.js
@@ -1,3 +1,9 @@
+// =============================================================================
+// 이 파일의 책임: 내 프로젝트 초대·최근 초대 대상 조회와 초대 응답 mutation을 묶는다.
+// 다른 파일과의 관계: project API를 호출하고 프로젝트·포트폴리오 캐시를 갱신한다.
+// Spring 비교: React Query 기반 조회 Service와 mutation 이벤트 핸들러에 해당한다.
+// =============================================================================
+
 import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
 import { acceptInvitation, declineInvitation, listMyInvitations, listRecentInvitees } from '../api/project'
 
@@ -21,6 +27,9 @@ export function useInvitationsQuery(userId, notify) {
     onSuccess: (_, values) => {
       queryClient.setQueryData(invitationsKey, current => current?.filter(item => item.id !== values.id))
       queryClient.invalidateQueries({ queryKey: ['projects'] })
+      if (values.action === 'accept') {
+        queryClient.invalidateQueries({ queryKey: ['portfolio-dashboard', userId] })
+      }
       notify?.('success', values.action === 'accept' ? '초대 수락 완료' : '초대 거절 완료', values.action === 'accept' ? '프로젝트에 참여했습니다.' : '프로젝트 초대를 거절했습니다.')
     },
     onError: error => notify?.('error', '초대 처리 실패', error.message),
diff --git a/frontend/src/pages/AuthPage.jsx b/frontend/src/pages/AuthPage.jsx
index 2d6f8ee..2887c64 100644
--- a/frontend/src/pages/AuthPage.jsx
+++ b/frontend/src/pages/AuthPage.jsx
@@ -1,3 +1,9 @@
+// =============================================================================
+// 이 파일의 책임: 로그인과 회원가입 폼을 같은 브랜드 화면에서 제공한다.
+// 다른 파일과의 관계: auth API와 세션 훅을 연결하고 성공 후 보호 라우트로 이동한다.
+// Spring 비교: 로그인 DTO를 받는 인증 Controller의 입력 View에 해당한다.
+// =============================================================================
+
 import { useState } from 'react'
 import { Link, useLocation, useNavigate } from 'react-router-dom'
 import { login, signup } from '../api/auth'
@@ -31,15 +37,28 @@ export default function AuthPage({ mode, onAuthenticated, notify }) {
   }
 
   const isSignup = mode === 'signup'
-  return <main className="auth-shell"><form className="auth-card" onSubmit={submit}>
-    <Link to="/" className="logo-link"><Logo/></Link><div><p className="eyebrow">DOCUMENT WORKSPACE</p><h1>{isSignup ? '회원가입' : '로그인'}</h1></div>
-    {!isSignup && location.state?.loginRequired && <div className="login-notice"><strong>로그인이 필요한 기능입니다.</strong><span>프로젝트 작업을 계속하려면 로그인하세요.</span></div>}
-    {!isSignup && location.state?.signupComplete && <div className="success-box"><strong>회원가입이 완료되었습니다.</strong><span>등록한 아이디와 비밀번호로 로그인하세요.</span></div>}
-    {isSignup && <input name="name" placeholder="표시 이름" required/>}
-    <input name="login_id" placeholder="아이디" minLength="3" pattern="[a-zA-Z0-9_.-]+" required/>
-    {isSignup && <input name="email" type="email" placeholder="이메일" required/>}
-    <input name="password" type="password" placeholder="비밀번호 (8자 이상)" minLength="8" required/>
-    <button className="primary" disabled={busy}>{busy ? '처리 중...' : isSignup ? '가입 완료' : '로그인'}</button>
-    <Link className="link auth-link" to={isSignup ? '/login' : '/signup'}>{isSignup ? '로그인으로 돌아가기' : '계정 만들기'}</Link>
-  </form></main>
+  return <main className="auth-shell">
+    <section className="auth-brand" aria-label="Tasqra 소개">
+      <Link to="/" className="auth-brand__logo"><Logo/></Link>
+      <div className="auth-brand__copy"><p className="eyebrow">DOCUMENT TO PROJECT</p><h1>문서의 맥락을 잃지 않고<br/>실행까지 연결하세요.</h1><p>OCR 검수, AI 분석, 프로젝트 업무와 산출물을 하나의 흐름으로 관리합니다.</p></div>
+      <div className="auth-brand__visual" aria-hidden="true"><span>T</span><i/><i/><b>DOC</b><b>AI</b><b>TASK</b></div>
+      <ul><li>원문과 분석 근거 연결</li><li>프로젝트별 안전한 권한 관리</li><li>검토와 변경 이력 보존</li></ul>
+    </section>
+    <section className="auth-form-side">
+      <form className="auth-card" onSubmit={submit}>
+        <Link to="/" className="auth-mobile-logo"><Logo/></Link>
+        <div className="auth-card__heading"><p className="eyebrow">WELCOME TO TASQRA</p><h2>{isSignup ? '새 계정 만들기' : '다시 만나 반가워요.'}</h2><p>{isSignup ? '팀의 문서 업무를 정리할 계정을 만드세요.' : '계정으로 로그인하고 오늘의 프로젝트를 확인하세요.'}</p></div>
+        {!isSignup && location.state?.loginRequired && <div className="login-notice"><strong>로그인이 필요한 기능입니다.</strong><span>프로젝트 작업을 계속하려면 로그인하세요.</span></div>}
+        {!isSignup && location.state?.signupComplete && <div className="success-box"><strong>회원가입이 완료되었습니다.</strong><span>등록한 아이디와 비밀번호로 로그인하세요.</span></div>}
+        <div className="auth-fields">
+          {isSignup && <label><span>표시 이름</span><input name="name" placeholder="이름을 입력하세요" autoComplete="name" required/></label>}
+          <label><span>아이디</span><input name="login_id" placeholder="아이디를 입력하세요" autoComplete="username" minLength="3" pattern="[a-zA-Z0-9_.-]+" required/></label>
+          {isSignup && <label><span>이메일</span><input name="email" type="email" placeholder="name@example.com" autoComplete="email" required/></label>}
+          <label><span>비밀번호</span><input name="password" type="password" placeholder="8자 이상 입력하세요" autoComplete={isSignup ? 'new-password' : 'current-password'} minLength="8" required/></label>
+        </div>
+        <button className="primary auth-submit" disabled={busy}>{busy ? '처리 중...' : isSignup ? '계정 만들기' : '로그인'}</button>
+        <p className="auth-switch">{isSignup ? '이미 계정이 있으신가요?' : '아직 계정이 없으신가요?'} <Link to={isSignup ? '/login' : '/signup'}>{isSignup ? '로그인' : '회원가입'}</Link></p>
+      </form>
+    </section>
+  </main>
 }
diff --git a/frontend/src/pages/DocumentDetailPage.jsx b/frontend/src/pages/DocumentDetailPage.jsx
index ceecd36..bbdf8ef 100644
--- a/frontend/src/pages/DocumentDetailPage.jsx
+++ b/frontend/src/pages/DocumentDetailPage.jsx
@@ -22,7 +22,7 @@ import DocumentReviewTab from '../features/document-detail/DocumentReviewTab'
 import DecisionScheduleReviewPanel from '../features/decision-schedule/DecisionScheduleReviewView'
 import ProjectSidebar from '../features/projects/ProjectSidebar'
 import { useProjectsQuery } from '../hooks/useProjectsQuery'
-import { DOCUMENT_TYPES } from '../utils/documentType'
+import { DOCUMENT_TYPES, LEGACY_BILLING_DOCUMENT_TYPE, normalizeDocumentTypeValue } from '../utils/documentType'
 import '../styles/document-detail-page.css'
 import '../styles/document-detail-updates.css'
 
@@ -33,6 +33,20 @@ function getDocumentListUrl(projectId, candidate) {
   return candidate === fallback || candidate?.startsWith(`${fallback}?`) ? candidate : fallback
 }
 
+function DocumentTypeCorrection({ document, pending, onSave }) {
+  const canonicalType = normalizeDocumentTypeValue(document.document_type) || 'ETC'
+  const [selectedType, setSelectedType] = useState(canonicalType)
+  const isLegacyBilling = document.document_type === LEGACY_BILLING_DOCUMENT_TYPE
+
+  useEffect(() => { setSelectedType(canonicalType) }, [canonicalType])
+
+  return <section className="detail-card document-type-correction" aria-labelledby="document-type-correction-title">
+    <h2 id="document-type-correction-title">문서 유형 수정</h2>
+    <p>{isLegacyBilling ? '기존 BILLING 유형은 현재 기타(ETC)에 포함됩니다. 저장하면 현재 8종 기준으로 정리됩니다.' : document.document_type_source === 'AI' ? 'AI 자동 분류 결과입니다.' : '사용자가 수정한 유형입니다.'} 변경한 값은 이후 AI 재분석에서도 유지됩니다.</p>
+    <div className="document-type-correction-controls"><label htmlFor="document-type-correction">문서 유형</label><select id="document-type-correction" value={selectedType} disabled={pending} onChange={event => setSelectedType(event.target.value)}>{DOCUMENT_TYPES.map(([value, label]) => <option value={value} key={value}>{label}</option>)}</select><button type="button" className="primary" disabled={pending || (!isLegacyBilling && selectedType === canonicalType)} onClick={() => onSave(selectedType)}>{pending ? '저장 중...' : isLegacyBilling ? 'ETC로 정리' : '유형 저장'}</button></div>
+  </section>
+}
+
 export default function DocumentDetailPage({ user, onLogout, notify }) {
   const { projectId, documentId } = useParams()
   const location = useLocation()
@@ -91,15 +105,7 @@ export default function DocumentDetailPage({ user, onLogout, notify }) {
     <ProjectSidebar projects={projects} activeProjectId={projectId} activeTab="documents" onSelect={selected => navigate(`/projects/${selected.id}/dashboard`)} onNavigateTab={key => navigate(`/projects/${projectId}/${key}`)} onCreate={() => navigate('/projects')}/>
     <div className="standalone-workspace-content"><div className="document-detail-shell">
       <DocumentHeader document={document} canEdit={canEdit} busy={deleteMutation.isPending || downloadMutation.isPending || retryMutation.isPending} onBack={() => navigate(documentListUrl)} onDownload={() => downloadMutation.mutate()} onRetry={() => retryMutation.mutate()} onDelete={() => setDeleteOpen(true)}/>
-      {canEdit && ['AI', 'USER_CORRECTED'].includes(document.document_type_source) && <section className="detail-card" aria-labelledby="document-type-correction-title">
-        <h2 id="document-type-correction-title">문서 유형 수정</h2>
-        <p>{document.document_type_source === 'AI' ? 'AI 자동 분류 결과입니다.' : '사용자가 수정한 유형입니다.'} 변경한 값은 이후 AI 재분석에서도 유지됩니다.</p>
-        <label htmlFor="document-type-correction">문서 유형</label>
-        <select id="document-type-correction" value={document.document_type ?? ''} disabled={documentTypeMutation.isPending} onChange={event => documentTypeMutation.mutate(event.target.value)}>
-          {DOCUMENT_TYPES.map(([value, label]) => <option value={value} key={value}>{label}</option>)}
-        </select>
-        {documentTypeMutation.isPending && <span role="status"> 저장 중...</span>}
-      </section>}
+      {canEdit && (['AI', 'USER_CORRECTED'].includes(document.document_type_source) || document.document_type === LEGACY_BILLING_DOCUMENT_TYPE) && <DocumentTypeCorrection document={document} pending={documentTypeMutation.isPending} onSave={documentType => documentTypeMutation.mutate(documentType)}/>}
       <nav className="document-detail-tabs">{TABS.map(([key, label]) => <button className={activeTab === key ? 'active' : ''} key={key} onClick={() => setParams({ tab: key }, { state: { documentListUrl } })}>{label}</button>)}</nav>
       <main className="document-tab-body">
         {analysisRunning && <section className="detail-card" role="status"><strong>AI 분석: {job.stage}</strong>{job.total_units > 0 && <p>현재 단계 {job.completed_units}/{job.total_units}</p>}<p>화면을 닫아도 분석은 계속됩니다.</p></section>}
diff --git a/frontend/src/pages/LandingPage.jsx b/frontend/src/pages/LandingPage.jsx
index 41ec4f9..0162dca 100644
--- a/frontend/src/pages/LandingPage.jsx
+++ b/frontend/src/pages/LandingPage.jsx
@@ -1,14 +1,81 @@
+// =============================================================================
+// 이 파일의 책임: Tasqra의 공개 랜딩에서 제품 가치와 실제 작업 흐름을 소개한다.
+// 다른 파일과의 관계: AppHeader의 공개 내비게이션과 로그인·회원가입 라우트로 연결된다.
+// Spring 비교: 인증 없이 접근 가능한 소개용 Controller View에 해당한다.
+// =============================================================================
+
 import { Link } from 'react-router-dom'
 import AppHeader from '../components/common/AppHeader'
 import Logo from '../components/common/Logo'
 import '../styles/landing.css'
 
-const FEATURES = [['01','프로젝트 문서 관리','프로젝트별로 문서를 모으고 구성원의 접근 권한을 관리합니다.'],['02','OCR 검수','원본 위치와 추출 텍스트를 비교하고 수정해 결과를 검수합니다.'],['03','AI 액션 아이템','문서에서 할 일과 일정을 추출하고 승인 후 실제 작업으로 연결합니다.']]
+const TRUST_ITEMS = [
+  ['⌁', '원문 근거 연결'],
+  ['✓', 'OCR 결과 직접 검수'],
+  ['♙', '프로젝트별 팀 권한'],
+  ['◷', '검토 이력 기록'],
+]
+
+const FEATURES = [
+  ['01', '정확한 OCR', 'PDF와 이미지, 문서 파일을 읽고 원문과 나란히 검수합니다.'],
+  ['02', 'AI 문서 분석', '핵심 요약과 요구사항, 금액과 일정을 구조화합니다.'],
+  ['03', '프로젝트 실행', '검토한 결과를 태스크와 프로젝트 업무로 연결합니다.'],
+  ['04', '협업과 추적', '권한과 처리 상태, 변경 이력을 한곳에서 관리합니다.'],
+]
+
+const STEPS = [
+  ['01', '문서를 정확하게 읽고', '다양한 형식의 문서에서 텍스트를 추출하고 원문 위치와 함께 검수합니다.'],
+  ['02', '핵심을 구조화하고', 'AI 분석 결과를 사람이 확인한 뒤 프로젝트 정보와 승인 항목으로 정리합니다.'],
+  ['03', '실행까지 연결합니다', '확정된 일정과 금액, 할 일을 보드와 산출물로 끊김 없이 이어갑니다.'],
+]
 
 export default function LandingPage({ user, onLogout, notify }) {
-  return <div className="landing"><AppHeader user={user} onLogout={onLogout} notify={notify}/><main>
-    <section className="hero"><p className="eyebrow">DOCUMENT TO PROJECT</p><h1>문서를 올리면<br/>프로젝트가 정리됩니다.</h1><p>OCR과 AI가 문서를 읽고, 함께 검수한 결과를 프로젝트의 실행 가능한 작업으로 연결합니다.</p><div><a className="primary" href="#features">기능 둘러보기</a><Link className="secondary" to={user ? '/projects' : '/login'} state={!user ? { loginRequired: true } : undefined}>{user ? '내 프로젝트 열기' : '작업 공간 열기'}</Link></div></section>
-    <section id="features" className="feature-section"><div className="section-title"><p className="eyebrow">CORE FEATURES</p><h2>Tasqra에서 할 수 있는 일</h2></div><div className="feature-grid">{FEATURES.map(([number,title,description]) => <article key={number}><span>{number}</span><h3>{title}</h3><p>{description}</p></article>)}</div></section>
-    <section className="login-cta"><div><p className="eyebrow">PROJECT WORKSPACE</p><h2>{user ? `${user.name}님, 프로젝트 작업을 이어가세요.` : '프로젝트 작업을 시작하려면 로그인이 필요합니다.'}</h2><p>{user ? '참여 중인 프로젝트와 새 초대를 확인할 수 있습니다.' : '계정으로 로그인하거나 새 계정을 만든 뒤 문서 작업을 시작하세요.'}</p></div><div><Link className="primary" to={user ? '/projects' : '/login'} state={!user ? { loginRequired: true } : undefined}>{user ? '내 프로젝트' : '로그인하고 시작하기'}</Link>{!user && <Link className="secondary" to="/signup">회원가입</Link>}</div></section>
-  </main><footer><Logo/><span>문서 기반 프로젝트 작업 도구</span></footer></div>
+  const workspacePath = user ? '/projects' : '/login'
+  return <div className="landing">
+    <AppHeader user={user} onLogout={onLogout} notify={notify}/>
+    <main>
+      <section className="landing-hero">
+        <div className="landing-hero__copy">
+          <p className="eyebrow">DOCUMENT INTELLIGENCE WORKSPACE</p>
+          <h1>흩어진 문서를<br/>실행 가능한 <strong>프로젝트로</strong></h1>
+          <p className="landing-hero__description">AI가 문서의 핵심을 분석하고, 검토가 필요한 지점을 알려주며 프로젝트 실행까지 연결합니다.</p>
+          <div className="landing-hero__actions">
+            <Link className="primary" to={user ? '/projects' : '/signup'}>{user ? '내 대시보드 열기' : '무료로 시작하기'}</Link>
+            <a className="secondary" href="#workflow">작동 방식 보기</a>
+          </div>
+          <div className="landing-hero__notes"><span>원문 근거 연결</span><span>프로젝트별 권한</span><span>검토 이력 기록</span></div>
+        </div>
+        <HeroFlow/>
+      </section>
+
+      <section className="landing-trust" aria-label="신뢰 기능">
+        {TRUST_ITEMS.map(([icon, label]) => <div key={label}><i aria-hidden="true">{icon}</i><span>{label}</span></div>)}
+      </section>
+
+      <section id="features" className="landing-features">
+        <div className="landing-section-heading"><p className="eyebrow">CORE FEATURES</p><h2>문서 업무에 필요한 흐름을<br/>하나의 공간에 담았습니다.</h2></div>
+        <div className="landing-feature-grid">{FEATURES.map(([number, title, description]) => <article key={number}><span>{number}</span><div><h3>{title}</h3><p>{description}</p></div></article>)}</div>
+      </section>
+
+      <section id="workflow" className="landing-workflow">
+        <div className="landing-section-heading"><p className="eyebrow">ONE CONNECTED WORKFLOW</p><h2>읽는 순간부터 실행까지,<br/>하나의 흐름으로 연결합니다.</h2><p>OCR 검수부터 AI 분석, 태스크 관리와 산출물 생성까지 업무 맥락을 잃지 않습니다.</p></div>
+        <div className="landing-step-grid">{STEPS.map(([number, title, description]) => <article key={number}><span>{number}</span><h3>{title}</h3><p>{description}</p></article>)}</div>
+      </section>
+
+      <section className="landing-cta"><div><p className="eyebrow">PROJECT WORKSPACE</p><h2>{user ? `${user.name}님, 오늘의 프로젝트를 이어가세요.` : '문서에서 실행까지, 지금 연결해 보세요.'}</h2><p>{user ? '전체 프로젝트 현황과 지금 확인할 업무를 한눈에 볼 수 있습니다.' : '계정을 만들고 팀의 문서 업무를 하나의 흐름으로 정리하세요.'}</p></div><div><Link className="primary" to={workspacePath} state={!user ? { loginRequired: true } : undefined}>{user ? '메인 대시보드' : '로그인하고 시작하기'}</Link>{!user && <Link className="secondary" to="/signup">회원가입</Link>}</div></section>
+    </main>
+    <footer><Logo/><span>문서 기반 프로젝트 작업 도구</span><small>© Tasqra</small></footer>
+  </div>
+}
+
+function HeroFlow() {
+  return <div className="hero-flow" aria-label="문서에서 실행으로 이어지는 Tasqra 업무 흐름">
+    <div className="hero-flow__glow"/>
+    <div className="hero-flow__orbit hero-flow__orbit--one"/><div className="hero-flow__orbit hero-flow__orbit--two"/>
+    <div className="hero-flow__core"><span>T</span><strong>Tasqra</strong><small>DOCUMENT TO ACTION</small></div>
+    <div className="hero-flow__card hero-flow__card--document"><i>▤</i><div><strong>문서 업로드</strong><small>원문 · OCR</small></div></div>
+    <div className="hero-flow__card hero-flow__card--analysis"><i>✦</i><div><strong>AI 분석</strong><small>요약 · 분류 · 추출</small></div></div>
+    <div className="hero-flow__card hero-flow__card--project"><i>▦</i><div><strong>프로젝트 실행</strong><small>태스크 · 산출물</small></div></div>
+    <svg className="hero-flow__lines" viewBox="0 0 700 500" aria-hidden="true"><path d="M145 185 C250 150 260 230 326 242"/><path d="M375 238 C450 180 500 168 568 190"/><path d="M368 278 C430 330 475 360 555 350"/></svg>
+  </div>
 }
diff --git a/frontend/src/pages/ProjectsPage.jsx b/frontend/src/pages/ProjectsPage.jsx
index 4e5ad40..2bec988 100644
--- a/frontend/src/pages/ProjectsPage.jsx
+++ b/frontend/src/pages/ProjectsPage.jsx
@@ -1,19 +1,26 @@
+// =============================================================================
+// 이 파일의 책임: 로그인 직후 전역 메인 대시보드를 열고 프로젝트 생성을 조정한다.
+// 다른 파일과의 관계: PortfolioDashboard에 실제 프로젝트 데이터를 전달하고 생성 후
+//   기존 프로젝트 작업공간으로 이동한다.
+// Spring 비교: 전역 대시보드 View와 프로젝트 생성 흐름을 조립하는 MVC Controller다.
+// =============================================================================
+
 import { useState } from 'react'
-import { Navigate, useNavigate } from 'react-router-dom'
+import { useNavigate } from 'react-router-dom'
 import AppHeader from '../components/common/AppHeader'
 import LoadingState from '../components/common/LoadingState'
+import PortfolioDashboard from '../features/projects/PortfolioDashboard'
 import ProjectCreateModal from '../features/projects/ProjectCreateModal'
-import ProjectSidebar from '../features/projects/ProjectSidebar'
 import { useInvitationsQuery } from '../hooks/useInvitationsQuery'
 import { useProjectsQuery } from '../hooks/useProjectsQuery'
-import { getRecentProjectId, setRecentProjectId } from '../utils/recentProject'
+import { setRecentProjectId } from '../utils/recentProject'
 import '../styles/projects.css'
 
 export default function ProjectsPage({ user, onLogout, notify }) {
   const [creating, setCreating] = useState(false)
   const navigate = useNavigate()
   const { projects, loading, error, createMutation } = useProjectsQuery(notify)
-  const { recentInvitees } = useInvitationsQuery(user?.id, notify)
+  const invitationData = useInvitationsQuery(user?.id, notify)
 
   async function submit(values) {
     try {
@@ -24,20 +31,9 @@ export default function ProjectsPage({ user, onLogout, notify }) {
     } catch { /* 공통 토스트에서 처리 */ }
   }
 
-  if (loading) return <div className="project-screen"><AppHeader user={user} onLogout={onLogout} notify={notify}/><LoadingState label="프로젝트를 불러오는 중..."/></div>
-  if (!error && projects.length) {
-    const recentId = getRecentProjectId(user?.id)
-    const target = projects.find(project => String(project.id) === recentId) ?? projects[0]
-    return <Navigate to={`/projects/${target.id}/dashboard`} replace/>
-  }
-
-  return <div className="app-frame"><AppHeader user={user} onLogout={onLogout} notify={notify}/>
-    <div className="workspace-shell">
-      <ProjectSidebar projects={[]} onCreate={() => setCreating(true)} onSelect={() => {}} onNavigateTab={() => {}}/>
-      <main className="project-empty-main">
-        {error ? <div className="error-state">{error.message}</div> : <section className="project-empty-state"><span aria-hidden="true">＋</span><h1>아직 참여 중인 프로젝트가 없습니다.</h1><p>새 프로젝트를 만들어 문서와 작업을 한곳에서 관리해 보세요.</p><button className="primary" onClick={() => setCreating(true)}>새 프로젝트 만들기</button></section>}
-      </main>
-    </div>
-    <ProjectCreateModal open={creating} recentInvitees={recentInvitees} pending={createMutation.isPending} onClose={() => setCreating(false)} onSubmit={submit}/>
+  return <div className="projects-home">
+    <AppHeader user={user} onLogout={onLogout} notify={notify}/>
+    {loading ? <LoadingState label="프로젝트를 불러오는 중..."/> : error ? <main className="projects-error"><div className="error-state">{error.message}</div></main> : <PortfolioDashboard user={user} projects={projects} invitations={invitationData.invitations} onCreate={() => setCreating(true)}/>}
+    <ProjectCreateModal open={creating} recentInvitees={invitationData.recentInvitees} pending={createMutation.isPending} onClose={() => setCreating(false)} onSubmit={submit}/>
   </div>
 }
diff --git a/frontend/src/styles/auth-extras.css b/frontend/src/styles/auth-extras.css
index 0df88a2..469a61a 100644
--- a/frontend/src/styles/auth-extras.css
+++ b/frontend/src/styles/auth-extras.css
@@ -1 +1 @@
-.login-notice{display:grid;gap:4px;padding:14px;background:#eef3ff;border:1px solid #c8d6fb;border-radius:10px;color:#284aa8}.login-notice span{font-size:13px}.logo-link{text-decoration:none}.auth-link{text-align:center;text-decoration:none}
+.logo-link{text-decoration:none}.auth-link{text-align:center;text-decoration:none}
diff --git a/frontend/src/styles/auth.css b/frontend/src/styles/auth.css
index 83d304e..a51281e 100644
--- a/frontend/src/styles/auth.css
+++ b/frontend/src/styles/auth.css
@@ -1 +1,8 @@
-.auth-shell{min-height:100vh;display:grid;place-items:center}.auth-card{width:min(420px,92vw);background:white;padding:38px;border:1px solid #dce3ee;border-radius:20px;box-shadow:0 18px 55px #25365a18;display:grid;gap:14px}.auth-card h1{margin:7px 0 0;color:#111827}.auth-card input{border:1px solid #d5deeb;border-radius:10px;padding:12px;background:white}.success-box{display:grid;gap:4px;padding:14px;background:#eefbf3;border:1px solid #bce7ca;border-radius:10px;color:#15753a}.success-box span{font-size:13px}
+.auth-shell{display:grid;min-width:320px;min-height:100svh;grid-template-columns:minmax(360px,.95fr) minmax(420px,1.05fr);background:var(--c-bg-canvas)}
+.auth-brand{position:relative;isolation:isolate;display:flex;min-width:0;overflow:hidden;flex-direction:column;padding:44px clamp(34px,5vw,82px);background:linear-gradient(145deg,var(--tasqra-hero-from),var(--tasqra-hero-to));color:var(--tasqra-on-hero)}.auth-brand::before{content:'';position:absolute;z-index:-1;top:20%;right:-24%;width:540px;height:540px;border:1px solid var(--tasqra-hero-line);border-radius:50%;box-shadow:0 0 80px var(--tasqra-hero-glow)}.auth-brand::after{content:'';position:absolute;z-index:-1;right:6%;bottom:14%;width:330px;height:330px;border:1px dashed var(--tasqra-hero-line);border-radius:50%}.auth-brand__logo{text-decoration:none}.auth-brand__logo .logo{color:var(--tasqra-on-hero)}.auth-brand__copy{max-width:600px;margin:auto 0}.auth-brand__copy .eyebrow{color:var(--tasqra-on-hero-muted)}.auth-brand__copy h1{margin:14px 0;color:var(--tasqra-on-hero);font-size:clamp(36px,4.4vw,58px);line-height:1.18;letter-spacing:-.05em;word-break:keep-all}.auth-brand__copy>p:last-child{max-width:530px;color:var(--tasqra-on-hero-muted);font-size:17px;line-height:1.75;word-break:keep-all}.auth-brand>ul{display:flex;flex-wrap:wrap;gap:10px 18px;margin:0;padding:0;list-style:none;color:var(--tasqra-on-hero-muted);font-size:12px}.auth-brand li::before{content:'✓';margin-right:6px;color:var(--tasqra-on-hero)}
+.auth-brand__visual{position:absolute;right:8%;bottom:18%;width:270px;height:180px;opacity:.32}.auth-brand__visual>span{position:absolute;top:50%;left:50%;display:grid;width:82px;height:82px;place-items:center;border:1px solid var(--tasqra-hero-line);border-radius:50%;background:var(--tasqra-hero-fill);font-size:36px;font-weight:900;transform:translate(-50%,-50%)}.auth-brand__visual>i{position:absolute;inset:10px;border:1px solid var(--tasqra-hero-line);border-radius:50%;transform:rotate(18deg)}.auth-brand__visual>i:nth-of-type(2){inset:35px -20px;transform:rotate(-22deg)}.auth-brand__visual>b{position:absolute;padding:7px 9px;border:1px solid var(--tasqra-hero-line);border-radius:8px;background:var(--tasqra-hero-fill);font-size:9px}.auth-brand__visual>b:nth-of-type(1){top:10px;left:0}.auth-brand__visual>b:nth-of-type(2){top:20px;right:0}.auth-brand__visual>b:nth-of-type(3){right:18px;bottom:3px}
+.auth-form-side{display:grid;min-width:0;place-items:center;padding:48px 24px}.auth-card{display:grid;width:min(440px,100%);gap:0;padding:40px;border:1px solid var(--c-border);border-radius:22px;background:#fff;box-shadow:var(--c-shadow-lg);animation:auth-enter .42s ease both}.auth-mobile-logo{display:none}.auth-card__heading h2{margin:8px 0 8px;color:var(--c-text-strong);font-size:31px;letter-spacing:-.04em}.auth-card__heading>p:last-child{margin:0 0 24px;color:var(--c-text-muted);font-size:14px;line-height:1.6}.auth-fields{display:grid;gap:15px}.auth-fields label{display:grid;gap:7px;color:var(--c-text-strong);font-size:13px;font-weight:700}.auth-card input{width:100%;min-height:46px;padding:0 13px;border:1px solid var(--c-border);border-radius:10px;background:#fff;color:var(--c-text-strong);outline:none;transition:border-color .16s,box-shadow .16s,transform .16s}.auth-card input::placeholder{color:#94a3b8}.auth-card input:focus{border-color:var(--c-accent);box-shadow:0 0 0 4px var(--c-accent-bg);transform:translateY(-1px)}.auth-submit{width:100%;min-height:48px;margin-top:22px}.auth-switch{margin:18px 0 0;text-align:center;color:var(--c-text-muted);font-size:13px}.auth-switch a{color:var(--c-accent);font-weight:800;text-decoration:none}.login-notice,.success-box{display:grid;gap:4px;margin:0 0 18px;padding:13px 14px;border-radius:10px;font-size:13px}.success-box{border:1px solid #bce7ca;background:#eefbf3;color:#15753a}.login-notice{border:1px solid var(--c-accent-border);background:var(--c-accent-soft);color:var(--c-accent-strong)}.login-notice span,.success-box span{font-size:12px}
+@keyframes auth-enter{from{opacity:0;transform:translateY(12px)}to{opacity:1;transform:translateY(0)}}
+@media(max-width:860px){.auth-shell{grid-template-columns:1fr}.auth-brand{display:none}.auth-form-side{min-height:100svh;padding:30px 18px;background:radial-gradient(circle at 50% 5%,var(--c-accent-bg),transparent 34%),var(--c-bg-canvas)}.auth-card{padding:32px 25px}.auth-mobile-logo{display:inline-flex;width:max-content;margin-bottom:30px;text-decoration:none}}
+@media(max-width:420px){.auth-form-side{padding:0}.auth-card{min-height:100svh;align-content:center;border:0;border-radius:0;box-shadow:none}.auth-card__heading h2{font-size:28px}}
+@media(prefers-reduced-motion:reduce){.auth-card{animation:none}}
diff --git a/frontend/src/styles/landing.css b/frontend/src/styles/landing.css
index cc7cec3..0740ee9 100644
--- a/frontend/src/styles/landing.css
+++ b/frontend/src/styles/landing.css
@@ -1 +1,16 @@
-.landing{min-height:100vh;background:#f7f9fd;color:#111827}.landing-header{height:84px;display:flex;align-items:center;padding:0 max(28px,calc((100vw - 1280px)/2));background:white;border-bottom:1px solid #e0e6ef}.landing-header nav{margin-left:auto;display:flex;align-items:center;gap:28px}.landing-header a{text-decoration:none;color:#536077;font-weight:700}.landing-header a.primary{color:white}.hero{min-height:600px;padding:100px max(28px,calc((100vw - 1280px)/2));background:radial-gradient(circle at 80% 30%,#e9efff 0,transparent 34%),white}.hero h1{max-width:760px;margin:18px 0 24px;font-size:clamp(48px,7vw,86px);line-height:1.08;letter-spacing:-.055em}.hero>p:not(.eyebrow){max-width:650px;color:#647187;font-size:20px;line-height:1.8}.hero>div,.login-cta>div:last-child{display:flex;gap:12px;margin-top:34px}.hero a,.login-cta a{text-decoration:none}.secondary{padding:12px 20px;border:1px solid #ccd6e6;border-radius:10px;background:white;color:#253149;font-weight:700}.feature-section{padding:100px max(28px,calc((100vw - 1280px)/2))}.section-title h2{font-size:40px}.feature-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:22px;margin-top:38px}.feature-grid article{min-height:260px;padding:30px;border:1px solid #dbe2ed;border-radius:18px;background:white}.feature-grid article>span{color:#355ad9;font-weight:800}.feature-grid h3{margin-top:52px;font-size:24px}.feature-grid p{color:#6a778d;line-height:1.7}.login-cta{margin:0 max(28px,calc((100vw - 1280px)/2)) 100px;padding:46px;display:flex;align-items:center;justify-content:space-between;border-radius:22px;background:#18233c;color:white}.login-cta h2{font-size:30px}.login-cta p:not(.eyebrow){color:#bac5da}.landing footer{height:110px;padding:0 max(28px,calc((100vw - 1280px)/2));display:flex;align-items:center;gap:24px;border-top:1px solid #dce3ee;background:white;color:#758197}@media(max-width:800px){.landing-header nav>a:not(.primary){display:none}.hero{min-height:520px;padding-top:70px}.feature-grid{grid-template-columns:1fr}.login-cta{align-items:flex-start;flex-direction:column}.login-cta>div:last-child{flex-wrap:wrap}}
+.landing{min-width:0;min-height:100vh;overflow-x:clip;background:#fff;color:var(--c-text)}
+.landing main{min-width:0;overflow:hidden}.landing .app-header{position:sticky;top:0;height:72px;padding-inline:max(22px,calc((100vw - 1320px)/2));border-color:var(--c-border);background:rgba(255,255,255,.92);backdrop-filter:blur(16px)}
+.landing-hero{display:grid;width:min(1320px,100%);min-height:620px;margin:auto;grid-template-columns:minmax(360px,.82fr) minmax(0,1.18fr);align-items:center;gap:clamp(30px,4vw,72px);padding:54px clamp(22px,4vw,58px) 48px;background:#fff}
+.landing-hero__copy{position:relative;z-index:2;max-width:540px}.landing-hero__copy h1{margin:15px 0 0;color:var(--c-text-strong);font-size:clamp(42px,4.8vw,66px);font-weight:850;line-height:1.12;letter-spacing:-.055em;word-break:keep-all}.landing-hero__copy h1 strong{color:var(--c-accent)}
+.landing-hero__description{max-width:500px;margin:21px 0 0;color:var(--c-text-muted);font-size:18px;line-height:1.75;word-break:keep-all}.landing-hero__actions{display:flex;gap:10px;margin-top:27px}.landing-hero__actions a,.landing-cta a{display:inline-flex;min-height:44px;align-items:center;justify-content:center;padding:0 19px;text-decoration:none}.landing-hero__notes{display:flex;flex-wrap:wrap;gap:8px 17px;margin-top:20px;color:var(--c-text-muted);font-size:13px;font-weight:650}.landing-hero__notes span::before{content:'✓';margin-right:6px;color:var(--c-accent);font-weight:900}
+.hero-flow{position:relative;isolation:isolate;width:100%;height:520px;min-width:0;overflow:visible}.hero-flow__glow{position:absolute;z-index:-2;inset:8% 0 0 5%;border-radius:50%;background:radial-gradient(circle,var(--c-accent-bg),transparent 68%)}.hero-flow__orbit{position:absolute;z-index:-1;top:50%;left:50%;border:1px solid var(--c-accent-border);border-radius:50%;transform:translate(-50%,-50%);opacity:.75}.hero-flow__orbit--one{width:330px;height:330px}.hero-flow__orbit--two{width:440px;height:440px;border-style:dashed;animation:hero-spin 32s linear infinite}.hero-flow__core{position:absolute;top:50%;left:50%;display:grid;width:158px;height:158px;place-items:center;align-content:center;border:1px solid rgba(255,255,255,.28);border-radius:50%;background:linear-gradient(145deg,var(--tasqra-hero-from),var(--tasqra-hero-to));color:var(--tasqra-on-hero);box-shadow:0 25px 60px rgba(22,35,74,.24),0 0 55px var(--tasqra-hero-glow);transform:translate(-50%,-50%)}.hero-flow__core span{display:grid;width:50px;height:50px;place-items:center;border:1px solid var(--tasqra-hero-line);border-radius:15px;background:var(--tasqra-hero-fill);font-size:25px;font-weight:900}.hero-flow__core strong{margin-top:7px;font-size:17px}.hero-flow__core small{margin-top:2px;color:var(--tasqra-on-hero-muted);font-size:7px;letter-spacing:.16em}
+.hero-flow__card{position:absolute;z-index:3;display:flex;min-width:174px;align-items:center;gap:11px;padding:13px 15px;border:1px solid var(--c-border);border-radius:14px;background:rgba(255,255,255,.94);box-shadow:0 16px 34px rgba(15,23,42,.1);backdrop-filter:blur(10px);animation:hero-float 5s ease-in-out infinite}.hero-flow__card i{display:grid;width:38px;height:38px;place-items:center;border-radius:11px;background:var(--c-accent-soft);color:var(--c-accent);font-style:normal;font-weight:800}.hero-flow__card strong,.hero-flow__card small{display:block}.hero-flow__card strong{color:var(--c-text-strong);font-size:13px}.hero-flow__card small{margin-top:3px;color:var(--c-text-muted);font-size:10px}.hero-flow__card--document{top:104px;left:2%}.hero-flow__card--analysis{top:88px;right:1%;animation-delay:-1.4s}.hero-flow__card--project{right:5%;bottom:64px;animation-delay:-2.8s}.hero-flow__lines{position:absolute;z-index:0;inset:0;width:100%;height:100%;overflow:visible}.hero-flow__lines path{fill:none;stroke:var(--c-accent-border);stroke-width:2;stroke-dasharray:7 8;animation:hero-dash 10s linear infinite}
+.landing-trust{display:grid;width:min(1320px,calc(100% - 44px));margin:auto;grid-template-columns:repeat(4,minmax(0,1fr));border-block:1px solid var(--c-border);background:linear-gradient(90deg,var(--c-bg-subtle),var(--c-accent-soft),var(--c-bg-subtle))}.landing-trust>div{display:flex;min-width:0;min-height:82px;align-items:center;justify-content:center;gap:10px;padding:15px;border-right:1px solid var(--c-border);color:var(--c-text-strong);font-size:14px;font-weight:750}.landing-trust>div:last-child{border-right:0}.landing-trust i{display:grid;width:36px;height:36px;place-items:center;border-radius:10px;background:#fff;color:var(--c-accent);font-style:normal;box-shadow:var(--c-shadow-sm)}
+.landing-features,.landing-workflow{padding:96px max(22px,calc((100vw - 1200px)/2))}.landing-section-heading{max-width:720px}.landing-section-heading h2{margin:10px 0 0;color:var(--c-text-strong);font-size:clamp(30px,3.5vw,46px);line-height:1.25;letter-spacing:-.045em}.landing-section-heading>p:last-child{margin:14px 0 0;color:var(--c-text-muted);font-size:16px;line-height:1.75}.landing-feature-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:13px;margin-top:42px}.landing-feature-grid article{display:grid;min-width:0;min-height:190px;align-content:space-between;padding:25px;border:1px solid var(--c-border);border-radius:var(--c-radius-lg);background:#fff;box-shadow:var(--c-shadow-sm);transition:transform .2s,border-color .2s,box-shadow .2s}.landing-feature-grid article:hover{transform:translateY(-5px);border-color:var(--c-accent-border);box-shadow:var(--c-shadow)}.landing-feature-grid article>span{display:grid;width:38px;height:38px;place-items:center;border-radius:11px;background:var(--c-accent-soft);color:var(--c-accent);font-size:12px;font-weight:850}.landing-feature-grid h3{margin:30px 0 7px;color:var(--c-text-strong);font-size:18px}.landing-feature-grid p{margin:0;color:var(--c-text-muted);font-size:14px;line-height:1.65;word-break:keep-all}
+.landing-workflow{background:var(--c-bg-canvas)}.landing-step-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:16px;margin-top:38px}.landing-step-grid article{min-height:238px;padding:28px;border:1px solid var(--c-border);border-radius:var(--c-radius-lg);background:#fff}.landing-step-grid article>span{display:grid;width:40px;height:40px;place-items:center;border-radius:12px;background:var(--c-accent);color:#fff;font-size:12px;font-weight:850;box-shadow:0 8px 18px var(--c-accent-bg)}.landing-step-grid h3{margin:38px 0 9px;color:var(--c-text-strong);font-size:21px}.landing-step-grid p{margin:0;color:var(--c-text-muted);line-height:1.7;word-break:keep-all}
+.landing-cta{display:flex;width:min(1200px,calc(100% - 44px));margin:0 auto 96px;align-items:center;justify-content:space-between;gap:28px;padding:46px;border:1px solid var(--tasqra-hero-line);border-radius:24px;background:linear-gradient(135deg,var(--tasqra-hero-from),var(--tasqra-hero-to));color:var(--tasqra-on-hero);box-shadow:0 22px 50px rgba(15,23,42,.14)}.landing-cta h2{margin:9px 0;color:var(--tasqra-on-hero);font-size:30px}.landing-cta p:not(.eyebrow){margin:0;color:var(--tasqra-on-hero-muted)}.landing-cta>div:last-child{display:flex;flex:0 0 auto;gap:9px}.landing-cta .secondary{border-color:var(--tasqra-hero-line);background:var(--tasqra-hero-fill);color:var(--tasqra-on-hero)}.landing footer{display:flex;min-height:110px;align-items:center;gap:22px;padding:20px max(22px,calc((100vw - 1200px)/2));border-top:1px solid var(--c-border);color:var(--c-text-muted)}.landing footer small{margin-left:auto}
+@keyframes hero-spin{to{transform:translate(-50%,-50%) rotate(360deg)}}@keyframes hero-float{0%,100%{transform:translateY(0)}50%{transform:translateY(-9px)}}@keyframes hero-dash{to{stroke-dashoffset:-120}}
+@media(max-width:980px){.landing-hero{min-height:auto;grid-template-columns:1fr;padding-top:72px}.landing-hero__copy{max-width:680px;margin:auto;text-align:center}.landing-hero__description{margin-inline:auto}.landing-hero__actions,.landing-hero__notes{justify-content:center}.hero-flow{width:min(700px,100%);height:500px;margin:auto}.landing-feature-grid{grid-template-columns:repeat(2,1fr)}}
+@media(max-width:700px){.landing .app-header{height:64px}.landing-hero{padding:52px 18px 20px}.landing-hero__copy{text-align:left}.landing-hero__copy h1{font-size:clamp(38px,11vw,50px)}.landing-hero__actions{display:grid;justify-content:stretch}.landing-hero__notes{display:grid;grid-template-columns:1fr 1fr;justify-content:start}.hero-flow{height:390px}.hero-flow__orbit--one{width:245px;height:245px}.hero-flow__orbit--two{width:330px;height:330px}.hero-flow__core{width:125px;height:125px}.hero-flow__core span{width:42px;height:42px}.hero-flow__card{min-width:138px;padding:10px}.hero-flow__card i{width:32px;height:32px}.hero-flow__card--document{top:42px;left:0}.hero-flow__card--analysis{top:64px;right:-12px}.hero-flow__card--project{right:0;bottom:24px}.hero-flow__lines{display:none}.landing-trust{width:100%;grid-template-columns:1fr 1fr}.landing-trust>div{justify-content:flex-start;border-bottom:1px solid var(--c-border)}.landing-features,.landing-workflow{padding:70px 18px}.landing-feature-grid,.landing-step-grid{grid-template-columns:1fr}.landing-feature-grid article{min-height:165px}.landing-cta{width:calc(100% - 36px);margin-bottom:70px;padding:30px 24px;align-items:flex-start;flex-direction:column}.landing-cta>div:last-child{width:100%;flex-wrap:wrap}.landing-cta>div:last-child a{flex:1}.landing footer{padding-inline:18px}.landing footer>span{display:none}}
+@media(max-width:430px){.hero-flow__card--analysis{display:none}.hero-flow__card--document{left:-8px}.landing-trust{grid-template-columns:1fr}.landing-trust>div{border-right:0}.landing-feature-grid{grid-template-columns:1fr}}
+@media(prefers-reduced-motion:reduce){.hero-flow__orbit,.hero-flow__card,.hero-flow__lines path{animation:none}}
diff --git a/frontend/src/styles/projects.css b/frontend/src/styles/projects.css
index 329e1e7..dc8b5e7 100644
--- a/frontend/src/styles/projects.css
+++ b/frontend/src/styles/projects.css
@@ -1 +1 @@
-.global-header{height:92px;padding:0 40px;display:flex;align-items:center;border-bottom:1px solid #dce3ee;background:white}.user-chip{margin-left:auto;display:flex;align-items:center;gap:12px}.user-chip i{display:grid;place-items:center;width:38px;height:38px;border-radius:50%;background:#edf1ff;color:#355ad9;font-style:normal;font-weight:800}.user-chip button{border:0;background:none;color:#6b778c}.project-main{padding:55px 44px;max-width:1500px;margin:auto}.project-title{display:flex;align-items:center;justify-content:space-between}.project-title h1{font-size:36px;color:#101827;margin-bottom:12px}.project-title p{color:#6d7a90}.create-project{display:grid;grid-template-columns:1fr 1.5fr auto auto;gap:10px;background:white;border:1px solid #dce3ee;border-radius:15px;padding:18px;margin:25px 0}.create-project input{border:1px solid #d5deeb;border-radius:10px;padding:12px;background:white}.create-project button:not(.primary){border:1px solid #d5deeb;background:white;border-radius:10px}.project-cards{display:grid;grid-template-columns:repeat(3,1fr);gap:22px;margin-top:30px}.project-card{text-align:left;min-height:265px;padding:28px;background:white;border:1px solid #d7e0ec;border-radius:18px;cursor:pointer;color:#111827}.project-card:hover{border-color:#8ca5f5;box-shadow:0 10px 30px #29417712}.project-card>div{display:flex;justify-content:space-between;align-items:center}.project-card p{height:58px;color:#68768c;line-height:1.6}.project-card dl{display:flex;gap:38px;padding:20px 0;border-block:1px solid #e4e9f1}.project-card dt{font-size:12px;color:#768399}.project-card dd{margin:6px 0 0;font-weight:800}.project-card small{display:block;margin-top:18px;color:#7b879a}.project-card--new{border-style:dashed;display:grid;place-items:center;align-content:center;gap:12px;text-align:center;color:#536177}.project-card--new b{font-size:32px;color:#355ad9}@media(max-width:900px){.global-header{padding:0 18px}.project-main{padding:24px}.project-cards{grid-template-columns:1fr}.create-project{grid-template-columns:1fr}}
+.project-empty-main{display:grid;place-items:center;padding:30px}.project-empty-state{width:min(620px,100%);text-align:center}.project-empty-state>span{display:grid;width:54px;height:54px;margin:0 auto 18px;place-items:center;border-radius:16px;font-size:28px}.project-empty-state h1{color:var(--c-text-strong)}.project-empty-state p{color:var(--c-text-muted)}
diff --git a/frontend/src/styles/unified-ui.css b/frontend/src/styles/unified-ui.css
new file mode 100644
index 0000000..3e6d7df
--- /dev/null
+++ b/frontend/src/styles/unified-ui.css
@@ -0,0 +1,68 @@
+/* =============================================================================
+   책임: 공개 화면에서 이어지는 Tasqra 시각 언어를 모든 프로젝트 업무 화면에 적용한다.
+   관계: 기존 기능별 CSS 뒤에서 레이아웃·색·반응형만 보정하며 데이터/상호작용은 바꾸지 않는다.
+   Spring 비교: Controller 로직은 두고 공통 ViewResolver 테마를 마지막에 적용하는 층이다.
+   ============================================================================= */
+
+:root{--ui-control-h:40px;--ui-panel-radius:14px;--ui-panel-shadow:0 2px 9px rgba(15,23,42,.035);--ui-hover-shadow:0 10px 28px rgba(15,23,42,.07)}
+body{color:var(--c-text);background:var(--c-bg-canvas)}
+button,input,select,textarea{font:inherit}button{transition:border-color .16s,background-color .16s,color .16s,box-shadow .16s,transform .16s}
+.primary{min-height:var(--ui-control-h);padding:9px 16px;border:1px solid var(--c-accent);border-radius:9px;background:var(--c-accent);color:#fff;box-shadow:0 6px 16px var(--c-accent-bg)}
+.primary:hover:not(:disabled){border-color:var(--c-accent-strong);background:var(--c-accent-strong);transform:translateY(-1px)}
+.link{color:var(--c-accent)}
+.secondary{border-color:var(--c-border-strong);border-radius:9px;color:var(--c-text-strong)}
+.eyebrow{color:var(--c-accent);font-size:10px;font-weight:850;letter-spacing:.14em}
+.panel{min-width:0;padding:20px;border:1px solid var(--c-border);border-radius:var(--ui-panel-radius);background:#fff;box-shadow:var(--ui-panel-shadow)}
+.panel-head{min-width:0;gap:14px}.panel-head>div{min-width:0}.panel-head h2{margin:0;color:var(--c-text-strong);font-size:16px;letter-spacing:-.015em}.panel-head>div>p{margin:4px 0 0;color:var(--c-text-muted);font-size:11.5px;line-height:1.5}.panel-head>span{flex:0 0 auto;color:var(--c-accent);font-size:11px;font-weight:800}
+.page-heading{align-items:flex-end;gap:20px;margin-bottom:22px}.page-heading>div{min-width:0}.page-heading h1{margin:4px 0 5px;color:var(--c-text-strong);font-size:clamp(26px,3vw,34px);letter-spacing:-.04em}.page-heading p{max-width:780px;margin:0;color:var(--c-text-muted);font-size:13px;line-height:1.6}
+input,select,textarea{color:var(--c-text-strong)}
+input:focus,select:focus,textarea:focus{border-color:var(--c-accent)!important;outline:3px solid var(--c-accent-bg)!important;outline-offset:0}
+
+/* Global and project header */
+.app-header{height:64px;border-color:var(--c-border);background:rgba(255,255,255,.96);backdrop-filter:blur(14px)}
+.app-header__brand .logo{font-size:19px;font-weight:850;letter-spacing:-.035em}.app-header__brand .logo span{width:34px;height:34px;border-radius:10px;background:var(--c-accent)}
+.app-header__nav{gap:8px}.app-header__nav>a{min-height:38px;padding:9px 12px!important;border-radius:8px;color:var(--c-text)!important;font-size:13px}.app-header__nav>a:hover{background:var(--c-bg-hover);color:var(--c-text-strong)!important}.app-header__nav>a.primary{color:#fff!important}.app-header__nav>a.primary:hover{background:var(--c-accent-strong)}
+.app-frame>.app-header .app-header__nav>a:first-child{display:inline-flex;align-items:center}.app-header__context{font-size:11px}.app-header__context strong{color:var(--c-text-strong)}
+.header-icon-button,.theme-header-button,.profile-button,.logout-button{height:38px;border-color:var(--c-border);border-radius:9px;background:#fff;box-shadow:none}.header-icon-button,.theme-header-button{width:38px}.profile-button i{width:30px;height:30px}.logout-button{padding:8px 11px;font-size:12px}.header-popover{top:46px;border-color:var(--c-border);border-radius:var(--c-radius);box-shadow:var(--c-shadow-lg)}
+
+/* Workspace shell and rail */
+.app-frame{min-width:320px;overflow-x:clip;background:var(--c-bg-canvas)}
+.workspace-main{width:100%;max-width:1480px;padding:30px clamp(20px,3vw,44px) 64px}
+.project-sidebar{box-shadow:6px 0 24px rgba(6,14,33,.06)}
+.project-sidebar__marker{background:var(--c-accent)!important;box-shadow:0 0 0 4px var(--c-accent-bg)!important}.project-sidebar__marker.marker-1{opacity:.86}.project-sidebar__marker.marker-2{opacity:.72}.project-sidebar__marker.marker-3{opacity:.58}.project-sidebar__marker.marker-4{opacity:.44}
+.project-sidebar__list .project-sidebar__project-button{color:var(--tasqra-rail-muted)}.project-sidebar__project.is-open>.project-sidebar__project-button{background:var(--tasqra-rail-active);color:var(--tasqra-rail-text)}
+.project-sidebar__list .project-sidebar__menus button.is-active{border-color:var(--c-accent-border);background:var(--tasqra-rail-active);box-shadow:inset 3px 0 0 var(--c-accent)}
+.project-sidebar__create{box-shadow:none}.project-sidebar__collapse:focus-visible,.project-sidebar button:focus-visible{outline-color:var(--c-accent-border)}
+
+/* Project dashboard */
+.dashboard-summary-grid--compact{gap:11px}.dashboard-summary-card{box-shadow:var(--ui-panel-shadow);transition:border-color .16s,box-shadow .16s,transform .16s}.dashboard-summary-card.is-clickable:hover{border-color:var(--c-accent-border);background:#fff;box-shadow:var(--ui-hover-shadow);transform:translateY(-2px)}.dashboard-summary-card>span{color:var(--c-text-muted);font-size:11px}.dashboard-summary-card>strong{color:var(--c-text-strong);font-variant-numeric:tabular-nums}.dashboard-summary-card.is-emphasis{border-color:#f2c9cc;background:var(--c-danger-bg)}
+.dashboard-top-grid,.dashboard-distribution-grid{gap:14px}.dashboard-next-actions{border-color:var(--tasqra-hero-line);background:linear-gradient(135deg,var(--tasqra-hero-from),var(--tasqra-hero-to));box-shadow:0 14px 32px rgba(15,23,42,.13)}.dashboard-attention-row>button:hover:not(:disabled){border-color:var(--tasqra-hero-line);background:var(--tasqra-hero-fill)}
+.dashboard-task-card{border-color:var(--c-border);background:#fff!important;box-shadow:var(--c-shadow-sm)}.dashboard-task-card.task-type-development,.dashboard-task-card.task-type-design,.dashboard-task-card.task-type-infra,.dashboard-task-card.task-type-document{border-top-color:var(--c-accent)}.dashboard-task-type{background:var(--c-accent-soft)!important;color:var(--c-accent)!important}
+.dashboard-status-list button:hover,.dashboard-type-item:not(:disabled):hover{background:var(--c-accent-soft)}.dashboard-type-item:not(:disabled):hover,.dashboard-type-item:not(:disabled):focus-visible{outline-color:var(--c-accent)}.dashboard-type-bar>i,.dashboard-status-list button>i>b{background:var(--c-accent)}
+.project-calendar{border-radius:var(--ui-panel-radius)}.project-calendar__controls button:hover,.project-calendar__detail li button:hover{border-color:var(--c-accent-border);background:var(--c-accent-soft)}.project-calendar__day.is-selected{box-shadow:inset 0 0 0 2px var(--c-accent)}.project-calendar__date small,.project-calendar__more{color:var(--c-accent)}
+
+/* Documents */
+.table-panel{overflow:hidden}.document-filter-bar{display:flex;min-width:0;flex-wrap:wrap;gap:10px;padding:12px 20px;border-color:var(--c-border);background:var(--c-bg-subtle)}.document-filter-bar label{min-width:0;flex:1 1 220px}.document-filter-bar select{width:100%;min-width:0;min-height:var(--ui-control-h);border-color:var(--c-border);color:var(--c-text-strong)}.document-filter-bar>button{margin-left:0;border-color:var(--c-border);color:var(--c-accent)}
+.document-list .document-row{gap:12px;border-color:var(--c-border)}.document-row:hover{background:var(--c-bg-subtle)}.document-main-action:hover strong{color:var(--c-accent)}.file-icon,.detail-file-icon{background:var(--c-accent-soft);color:var(--c-accent)}.type-pill{background:var(--c-accent-soft);color:var(--c-accent)}.document-open{border-color:var(--c-border);color:var(--c-accent)}.document-open:hover{border-color:var(--c-accent-border);background:var(--c-accent-soft)}.upload-drop-hint,.drop-zone{border-color:var(--c-accent-border);background:linear-gradient(180deg,#fff,var(--c-accent-soft));color:var(--c-accent)}.upload-queue,.processing-document{border-color:var(--c-border);background:var(--c-accent-soft)}.processing-spinner{border-color:var(--c-accent-border);border-top-color:var(--c-accent)}
+
+/* Search */
+.search-modes button,.search-scope button{border-color:var(--c-border);border-radius:999px;background:#fff;color:var(--c-text)}.search-modes button.is-active,.search-scope button.active{border-color:var(--c-accent);background:var(--c-accent-soft);color:var(--c-accent);box-shadow:inset 0 0 0 1px var(--c-accent-border)}
+.search-panel{border-color:var(--tasqra-hero-line);background:linear-gradient(135deg,var(--tasqra-hero-from),var(--tasqra-hero-to));box-shadow:0 14px 32px rgba(15,23,42,.12)}.search-panel--precedent{background:#fff;color:var(--c-text);box-shadow:var(--ui-panel-shadow)}.search-input{min-height:46px;border-color:var(--tasqra-hero-line)}.search-result-body{border-color:var(--c-border);border-radius:11px;box-shadow:var(--ui-panel-shadow)}.search-result-body:hover{border-color:var(--c-accent-border);background:var(--c-bg-subtle)}.search-doc-title:hover{color:var(--c-accent)}.search-result-hit{background:var(--c-accent-soft)}.precedent-input:focus{border-color:var(--c-accent);outline-color:var(--c-accent-bg)}.precedent-submit{background:var(--c-accent)}
+
+/* Amount, deliverables, board and settings */
+.amount-total-grid,.deliverable-count-grid{gap:11px}.amount-total-card,.deliverable-count-card{border-color:var(--c-border);border-radius:var(--ui-panel-radius);box-shadow:var(--ui-panel-shadow)}.amount-total-card.is-emphasis{border-color:var(--c-accent);background:var(--c-accent-soft);box-shadow:inset 0 0 0 1px var(--c-accent-border)}.amount-scope-list>div{border-color:var(--c-border);background:var(--c-bg-subtle)}.amount-filter.is-active,.deliverable-content-view.is-active{border-color:var(--c-accent-strong);background:var(--c-accent-strong)}
+.deliverable-kind,.deliverable-format{border-color:var(--c-border);border-radius:10px}.deliverable-kind:hover,.deliverable-format:hover{border-color:var(--c-accent-border)}.deliverable-kind.is-active,.deliverable-format.is-active{border-color:var(--c-accent);background:var(--c-accent-soft);box-shadow:inset 0 0 0 1px var(--c-accent-border)}.deliverable-card{border-color:var(--c-border);box-shadow:var(--ui-panel-shadow)}
+.task-board-heading>div:last-child button{border-color:var(--c-border);border-radius:9px;color:var(--c-accent);box-shadow:var(--c-shadow-sm)}.task-board-heading>div:last-child .primary,.task-dialog>footer .primary{border-color:var(--c-accent);background:var(--c-accent)}.task-board.board>section{border-color:var(--c-border);border-radius:var(--ui-panel-radius);background:#eef2f7}.task-column.is-drag-over{border-color:var(--c-accent);background:var(--c-accent-soft);box-shadow:inset 0 0 0 2px var(--c-accent-border)}.task-board-card{border-color:var(--c-border);border-top-color:var(--c-accent)!important;box-shadow:var(--c-shadow-sm)}.task-board-card button{color:var(--c-accent)}.task-board-card.is-calendar-target{outline-color:var(--c-accent-border);background:linear-gradient(120deg,#fff,var(--c-accent-soft))}
+.settings-layout{gap:14px}.settings-panel{box-shadow:var(--ui-panel-shadow)}.project-info-form input,.project-info-form textarea,.project-info-form select,.invite input,.invite select,.member-list select{border-color:var(--c-border)}.member-list i{background:var(--c-accent-soft);color:var(--c-accent)}.danger-zone{border-color:#fecaca;background:#fff}.danger-button{border-color:#fecaca;background:var(--c-danger-bg);color:var(--c-danger)}
+
+/* Dialogs and detailed pages */
+.dialog-backdrop{background:rgba(6,13,29,.56);backdrop-filter:blur(4px)}.project-dialog,.task-dialog,.confirm-dialog,.reocr-dialog{border:1px solid var(--c-border);border-radius:var(--c-radius-lg);box-shadow:0 30px 90px rgba(6,13,29,.28)}.project-dialog>header{border-color:var(--c-border)}.document-detail-page>.app-header{position:sticky;top:0}.document-page-header,.detail-card{border-color:var(--c-border);box-shadow:var(--ui-panel-shadow)}.document-detail-tabs button.active{background:var(--c-accent-soft);color:var(--c-accent)}
+
+@media(max-width:1180px){.document-list .document-row{grid-template-columns:minmax(0,1.4fr) 180px 145px}.document-actions{grid-column:1/-1;justify-content:flex-end;padding-top:9px;border-top:1px solid var(--c-border)}.document-open{min-width:0}.deliverable-kind-group,.deliverable-format-group{grid-template-columns:repeat(2,1fr)}}
+@media(max-width:900px){.app-frame>.app-header{position:sticky;height:60px}.app-frame>.app-header .app-header__nav>a:first-child{display:block}.workspace-main{padding:22px 16px 48px}.project-sidebar{position:relative!important;padding:10px 14px;box-shadow:none}.project-sidebar__heading{display:none}.project-sidebar__list{display:flex;gap:6px;overflow-x:auto}.project-sidebar__project{display:flex;flex:0 0 auto;align-items:center}.project-sidebar__project-button{width:auto!important;max-width:200px}.project-sidebar__menus{display:flex;margin:0 0 0 5px;padding:0 0 0 6px;border-left:1px solid var(--tasqra-hero-line)}.project-sidebar__list .project-sidebar__menus button{width:38px;min-height:38px;padding:0;justify-content:center}.project-sidebar__menus .project-sidebar__text{display:none}.project-sidebar__create{display:none}.dashboard-summary-grid--compact{grid-template-columns:repeat(2,minmax(0,1fr))}.dashboard-top-grid,.dashboard-distribution-grid,.settings-layout{grid-template-columns:1fr}.task-board.board{grid-template-columns:1fr}.amount-total-grid{grid-template-columns:repeat(2,1fr)}.header-popover{max-height:calc(100svh - 92px);overflow-y:auto}}
+@media(max-width:700px){.app-header__context span{max-width:26vw}.app-header__context i{display:none}.app-header__context strong{overflow:hidden;text-overflow:ellipsis}.page-heading{align-items:flex-start;flex-direction:column}.page-heading>.primary{width:100%}.document-list .document-row{grid-template-columns:minmax(0,1fr) auto}.document-state-stack,.document-secondary-meta{grid-column:1;grid-auto-flow:row}.document-actions{grid-column:1/-1;grid-row:auto;justify-content:stretch}.document-actions .document-open{display:block;flex:1}.invite{grid-template-columns:1fr}.invite .primary{width:100%}.member-list li{align-items:flex-start;flex-wrap:wrap}.member-list li>div{min-width:calc(100% - 54px)}.member-list select,.member-list li>button{margin-left:50px}.deliverable-card-actions{width:100%;flex-wrap:wrap}.deliverable-card-actions button{flex:1}.search-form{flex-direction:column}.search-form .primary{width:100%}.project-calendar__head{align-items:flex-start}}
+@media(max-width:520px){.workspace-main{padding-inline:12px}.panel{padding:16px}.dashboard-summary-grid--compact,.amount-total-grid,.deliverable-kind-group,.deliverable-format-group,.deliverable-count-grid{grid-template-columns:1fr}.dashboard-summary-grid--compact .dashboard-summary-card{min-height:86px}.app-header__context span{display:none}.document-filter-bar{padding:12px}.task-board-heading>div:last-child{width:100%;flex-direction:column}.task-board-heading>div:last-child button{width:100%}.project-sidebar__project-button{max-width:145px}.project-sidebar__menus button{width:36px!important}.settings-panel{padding:16px}}
+@media(prefers-reduced-motion:reduce){button,.dashboard-summary-card,.task-board-card{transform:none!important}}
+
+
+.document-type-correction{margin-top:14px}.document-type-correction>h2{margin:0;color:var(--c-text-strong);font-size:16px}.document-type-correction>p{margin:6px 0 14px;color:var(--c-text-muted);font-size:12px;line-height:1.6}.document-type-correction-controls{display:grid;grid-template-columns:auto minmax(220px,1fr) auto;align-items:center;gap:10px}.document-type-correction-controls label{color:var(--c-text-strong);font-size:12px;font-weight:750}.document-type-correction-controls select{min-height:40px;padding:8px 10px;border:1px solid var(--c-border);border-radius:9px;background:#fff}.document-type-correction-controls button{min-height:40px}@media(max-width:620px){.document-type-correction-controls{grid-template-columns:1fr}.document-type-correction-controls button{width:100%}}
diff --git a/frontend/src/utils/documentType.js b/frontend/src/utils/documentType.js
index 97271d0..7e0c12c 100644
--- a/frontend/src/utils/documentType.js
+++ b/frontend/src/utils/documentType.js
@@ -1,24 +1,14 @@
 // =============================================================================
-// 이 파일의 책임: 문서 유형(documents.document_type) 값을 화면 문구로 바꾼다.
-//   업로드 모달과 대시보드 유형 분포가 같은 표기를 쓰게 하는 것이 목적이다.
-// 다른 파일과의 관계: features/documents/DocumentUploadModal.jsx 의 선택 목록과
-//   features/dashboard/DashboardView.jsx 의 유형 분포가 이 목록을 쓴다.
-//   utils/documentStatus.js 와 같은 자리다 — 상태값 표기를 한 곳에 모아 화면마다
-//   다르게 보이는 일을 막는 파일.
-// Spring 비교: 코드값 → 표시명 매핑을 담은 공용 CodeTable 이나 MessageSource 에
-//   해당한다. 상태를 갖지 않는 순수 함수만 둔다.
-//
-// 이 파일을 따로 만든 이유
-//   전에는 이 목록이 DocumentUploadModal.jsx 안에만 있었다. 대시보드가 유형
-//   분포를 보여주려고 목록을 복사하면, 나중에 한쪽 문구만 고쳐서 업로드 화면과
-//   대시보드가 같은 유형을 다르게 부르게 된다. 에러가 나지 않아 알아채기 어렵다.
-//   백엔드 값 목록은 models/enums.py 의 DocumentType 9종이다.
+// 이 파일의 책임: 문서 유형의 8종 선택 목록과 레거시 BILLING 표시 호환을 관리한다.
+// 다른 파일과의 관계: 업로드·상세 수정·목록 필터·대시보드 배지가 같은 canonical
+//   값과 라벨을 사용하도록 한곳에서 변환한다.
+// Spring 비교: 레거시 코드 alias까지 처리하는 공용 CodeTable/Converter에 해당한다.
 // =============================================================================
 
-// [값, 표시명]. 순서는 업로드 모달의 선택 순서이기도 하다 — 문서가 만들어지는
-// 흐름(제안요청서 → 제안서 → 산출내역서 → 계약서 → ...)에 맞춰 둔 것이다.
 export const UNCLASSIFIED_DOCUMENT_TYPE = '__UNCLASSIFIED__'
+export const LEGACY_BILLING_DOCUMENT_TYPE = 'BILLING'
 
+// 사용자가 새로 선택하고 저장할 수 있는 유형은 8종이다.
 export const DOCUMENT_TYPES = [
   ['RFP', '제안요청서·입찰공고'],
   ['PROPOSAL', '제안서·기술제안서'],
@@ -27,38 +17,35 @@ export const DOCUMENT_TYPES = [
   ['CONTRACT_CHANGE', '변경계약서·과업변경합의서'],
   ['REPORT', '보고서·검사조서'],
   ['MEETING_NOTES', '회의록'],
-  ['BILLING', '대가지급청구서·세금계산서'],
-  ['ETC', '기타'],
+  ['ETC', '기타 (세금계산서·대가지급청구서 포함)'],
 ]
 
 const LABELS = Object.fromEntries(DOCUMENT_TYPES)
 const FILTER_VALUES = new Set([...DOCUMENT_TYPES.map(([value]) => value), UNCLASSIFIED_DOCUMENT_TYPE])
 
+/** 과거 BILLING 저장값을 현재 8종 계약의 ETC로 읽는다. */
+export function normalizeDocumentTypeValue(documentType) {
+  return documentType === LEGACY_BILLING_DOCUMENT_TYPE ? 'ETC' : documentType
+}
+
 export function isSupportedDocumentTypeFilter(documentType) {
-  return FILTER_VALUES.has(documentType)
+  return FILTER_VALUES.has(normalizeDocumentTypeValue(documentType))
 }
 
-/** URL query의 문서 유형 필터를 저장 가능한 9종 또는 미분류 값으로 제한한다. */
+/** 과거 BILLING URL도 ETC 필터로 복원해 신규·레거시 값을 함께 조회한다. */
 export function normalizeDocumentTypeFilter(documentType) {
-  return isSupportedDocumentTypeFilter(documentType) ? documentType : ''
+  const normalized = normalizeDocumentTypeValue(documentType)
+  return FILTER_VALUES.has(normalized) ? normalized : ''
 }
 
-/** 필터 값을 화면 문구로. URL 전용 미분류 값은 실제 저장값 null과 같은 뜻이다. */
 export function getDocumentTypeFilterLabel(documentType) {
   if (documentType === UNCLASSIFIED_DOCUMENT_TYPE) return '미분류'
   return getDocumentTypeLabel(documentType)
 }
 
-/** 문서 유형 값을 표시명으로. null·빈 값은 "미분류" 다.
- *
- * document_type 은 nullable 이다. 업로드할 때 유형을 고르지 않으면 비어 있고
- * AI 분류가 채우기 전까지 그대로다. "미분류" 는 유형 이름이 아니라 유형이 없는
- * 상태를 가리킨다 — ETC(기타)와 다르다. 기타는 사람이 고른 값이다.
- *
- * 목록에 없는 값이 오면 값 자체를 그대로 보여준다. 임의로 "기타" 로 바꾸면
- * 백엔드에 유형이 추가됐을 때 화면에서 그 사실이 감춰진다.
- */
+/** null은 미분류, 레거시 BILLING은 ETC 표시명으로 보여준다. */
 export function getDocumentTypeLabel(documentType) {
   if (!documentType) return '미분류'
-  return LABELS[documentType] ?? documentType
+  const normalized = normalizeDocumentTypeValue(documentType)
+  return LABELS[normalized] ?? documentType
 }
