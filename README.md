# PDF Brief AI — 산출물 보관소

교육과정 미니프로젝트 **PDF Brief AI** 의 문서 산출물을 모아둔 저장소입니다.
소스코드는 별도 저장소([OCR_MiniProject](https://github.com/ParkSehyeon1009/OCR_MiniProject))에 있습니다.

- **발표** 8/7(금) · **코드 프리즈** 8/5(수)
- **팀** 김보현(팀장) · 최재정 · 박세현

---

## 폴더 구성

### `산출물/` — 제출 대상

| 파일 | 설명 |
|---|---|
| `화면정의서.md` | 화면 6종 정의. 요소·연동 API·상태별 표시·동작 규칙·에러 코드 포함 |
| `화면정의서.html` | **PDF 변환용.** 브라우저로 열고 `Ctrl+P` → `PDF로 저장` |
| `캡처/` | 화면 캡처 7장 (`HOME` `LIST` `LIST2` `DTL` `UPL` `CMP` `NOTFOUND`) |
| `화면흐름도.svg` | 화면 간 이동 경로. 화면정의서에 삽입 |
| `단위테스트결과서.csv` | **66항목** — 화면 흐름 관점 |
| `통합테스트시나리오.csv` | **44항목** — 기능 연동 관점 |
| `테스트문서_작성안내.md` | 단위·통합 테스트의 관점 구분 기준 |

### `관리/` — 진행 관리

| 파일 | 설명 |
|---|---|
| `WBS(작업분할구조도)_v1.xlsx` | **정본.** 시트 4개 — WBS · 산출물목록 · 이슈관리 · 결정사항 |
| `WBS_반영대기_통합.md` | 이슈·결정사항의 상세 서술(현상·원인·조치). 반영 근거 문서 |
| `남은작업_정리.md` / `.html` | 잔여 작업, 측정 방법, 발표 소재 |

### `참고/` — 회의·설계 자료

의사결정 이력과 발표 근거로 남겨둔 자료입니다.

| 파일 | 용도 |
|---|---|
| `API_계약서.md` | 엔드포인트 6종 + 에러 코드 11종 규약 |
| `mockup_layouts.html` | 레이아웃 3안 목업 — 결정사항 13의 근거 |
| `도메인_제안_및_검증계획.md` | 초기 도메인 제안 및 검증 계획 |
| `D1_결정사항_체크리스트.md` | 1일차 결정 안건 |
| `D4_팀회의_자료.md` · `D4_차장님_보고자료.md` | 4일차 회의·보고 자료 |
| `문서수집_가이드.md` | 카테고리별 수집처와 검수 기준 |
| `발표자료_골격.md` | 발표 목차 5장 구성안 |
| `OCR_제안_스토리.md` | OCR 엔진 선정 서술 |
| `단위테스트_상세체크리스트.csv` | 상세 회귀 체크리스트(제출용 아님) |
| `Docker_설치_가이드.md` | 팀 공유용 설치 절차. 운영 메뉴얼 원천 |

### `도구/`

| 파일 | 용도 |
|---|---|
| `_md2html.py` | 마크다운 → 인쇄용 HTML 변환 |

---

## 사용법

### 마크다운을 PDF로 만들기

```bash
python 도구/_md2html.py 산출물/화면정의서.md "화면 정의서"
```

생성된 `.html` 을 브라우저에서 열고 `Ctrl+P` → 대상을 `PDF로 저장` 으로 지정합니다.
A4 여백·표 테두리·페이지 나눔이 인쇄용으로 설정되어 있습니다.
캡처 이미지는 상대 경로(`캡처/…`)로 연결되므로 **`.html` 을 `산출물/` 안에서 열어야** 이미지가 보입니다.

### CSV를 Excel에서 열기

CSV는 **UTF-8 BOM · 쉼표 구분** 으로 저장되어 있어 더블클릭만 해도 한글이 깨지지 않습니다
(결정사항 29). 제출 양식(xlsx)이 정해져 있다면 데이터 행을 복사해 양식에 붙여넣으면 됩니다.

> **주의** — Excel이 CSV를 A열에 몰아서 여는 경우가 있습니다. 그대로 저장하면 열 구분이
> 사라지고 인코딩이 CP949로 바뀝니다(ISS-037). 저장 시 형식을 **`CSV UTF-8(쉼표로 분리)`** 로
> 지정해 주세요.

---

## 화면 구성 (6종)

| ID | 화면 | 경로 | 담당 |
|---|---|---|---|
| `HOME` | 홈 (서비스 소개) | `/` | 최재정·김보현 |
| `LIST` | 문서 목록 + 상세 패널 | `/documents` | 김보현 |
| `DTL` | 문서 상세 (전체 화면) | `/documents/{id}` | 김보현 |
| `UPL` | 문서 업로드 및 분석 | `/upload` | 최재정 |
| `CMP` | OCR 엔진 비교 | `/ocr-compare` | 박세현 |
| `NF` | 페이지 없음 (404) | 정의되지 않은 모든 경로 | 박세현 |

---

## 테스트 구성

**단위테스트는 화면 흐름**, **통합테스트는 기능 연동** 관점으로 나눠 작성했습니다.

### 단위테스트 66항목 — 전 항목 판정 완료

| ID | 대상 | 항목 |
|---|---|---|
| `UT-PDF-01-001` | 파일 선택 · 업로드 실행 · 업로드 검증 | 11 |
| `UT-PDF-01-002` | 추출 방식 판정 · 추출 품질 | 9 |
| `UT-PDF-02-001` | AI 요약 · 카테고리 분류 · 분석 선택 · 분석 이력 | 9 |
| `UT-PDF-03-001` | 목록 조회 · 검색 · 필터 · 페이징 | 11 |
| `UT-PDF-03-002` | 상세 패널 · 전체 화면 상세 · 잘못된 주소 | 8 |
| `UT-PDF-04-001` | 요약 다운로드 · 다운로드 제한 | 5 |
| `UT-PDF-04-002` | 문서 삭제 | 5 |
| `UT-PDF-05-001` | OCR 엔진 비교 | 4 |
| `UT-PDF-06-001` | 실행 환경 | 4 |

### 통합테스트 44항목

| ID | 기능 | 항목 | 판정 |
|---|---|---|---|
| `IT-PDF-01` | 문서 업로드 · 텍스트 추출 연동 | 4 | 2 |
| `IT-PDF-02` | AI 분석 연동 | 5 | 4 |
| `IT-PDF-03` | 문서 조회 · 검색 연동 | 6 | 6 |
| `IT-PDF-04` | 요약 다운로드 연동 | 7 | 5 |
| `IT-PDF-05` | 문서 삭제 연동 | 4 | 3 |
| `IT-PDF-06` | OCR 엔진 비교 | 6 | 3 |
| `IT-PDF-07` | 오류 응답 일관성 | 6 | 1 |
| `IT-PDF-08` | 실행 환경 · 설정 | 6 | 5 |

미판정 항목은 `결함/이상 내용` 열에 사유와 담당자를 적어두었습니다.

---

## 이슈·결정사항

`WBS(작업분할구조도)_v1.xlsx` 의 시트에 기록합니다.

- **이슈관리 46건** — 담당자별로 나누지 않은 단일 표. 발표 '04 수행경과' 트러블슈팅의 원천 자료
- **결정사항 29건** — 결정 내용과 함께 **미채택 대안과 그 이유**를 남깁니다.
  발표 '03 수행절차 및 방법'에 그대로 사용합니다.

> 담당자별로 표를 따로 만들지 않습니다. 두 표로 나뉘어 ID가 중복된 사례가 있었습니다(ISS-037).
