# 기능명세서 갱신 목록 — 2026-08-24

`관리/기능명세서.md`(설계 근거판)는 **이미 고쳤다.** 이 문서는 **구글 시트(원본
`산출물/기능명세서_v5_세분화.xlsm`)에 사람이 옮겨 적을 목록**이다. xlsm 은 손으로
갱신하므로 머지된 PR 을 자동으로 따라오지 못한다.

옮긴 뒤 **`python 도구/spec_sync.py`** 를 돌려 어긋남이 사라지는지 확인한다.

## 내 담당 — 상태를 바꿀 것

| ID | 작업명 | 지금 시트 | 바꿀 값 | 근거 |
|---|---|---|---|---|
| `DLV-001-1` | 산출물 조건 선택 | 미구현 | **구현됨** | PR #52·#53 |
| `DLV-001-2` | 생성 대상 미리보기 | 미구현 | **구현됨** | PR #54 — 여섯 건수가 다 찼다 |
| `DLV-002-1` | 주간 보고서 생성 | 미구현 | **부분 구현** | PR #58 — 표는 되고 **개요(LLM)가 없다** |
| `DLV-002-2` | 프로젝트 현황 한 장 생성 | 미구현 | **부분 구현** | PR #58 — 같은 개요 자리가 빈다 |
| `DLV-003-1` | 결정사항 대장 생성 | 미구현 | **부분 구현** | PR #58 — 생성 경로는 되고 재료(`ANL-002-2`)가 없다 |
| `DLV-003-2` | 다음 회의 안건 생성 | 미구현 | **부분 구현** | PR #58 — 같은 이유 |
| `DLV-003-3` | 생성 이력·다운로드 | 미구현 | **구현됨** | PR #58 |
| `DLV-003-4` | 갱신 필요 판정 | 미구현 | **구현됨** | PR #58 |
| `SYS-002-4` | 임베딩 모델 서빙 경로 확정 | **미결** | **구현됨** | `관리/서빙경로_결정_SYS-002-4.md` |
| `DSH-001` | 프로젝트 핵심 현황 | 미구현 | **부분 구현** | PR #34·#51 — 유형 분포 이동까지 됐다 |
| `SYS-003-1` | 오류코드 체계·요청 로깅 | 부분 구현 | **부분 구현 유지** | PR #55·#58 로 채웠지만 워커 `request_id` 가 남았다 |

**`미결` 이 사라진다.** `SYS-002-4` 가 마지막 미결이었다.

## 내 담당 — 오늘 것이 아니지만 전부터 어긋나 있던 것

같이 고치면 `spec_sync` 가 깨끗해진다. 전부 md 쪽이 맞다(코드로 확인된 것들이다).

| ID | 작업명 | 지금 시트 | 바꿀 값 | 근거 |
|---|---|---|---|---|
| `SRH-002-3` | 유사 사업 단가 선례 검색 | 미구현 | **구현됨** | PR #32 · 화면까지 완료 |
| `SRH-003` | 키워드 검색 | 미구현 | **구현됨** | PR #38 · 리비전 `0016`(`pg_trgm`) |
| `SRH-004` | 키워드·의미 결합(하이브리드) | 미구현 | **구현됨** | PR #39 · RRF `k=60` |
| `RAG-002-1` | 프롬프트 컨텍스트 조립 | 미구현 | **부분 구현** | PR #40 — 조립 로직은 있고 **부르는 곳이 없다** |

`SRH-002-1`(검색 결과 재순위)은 **담당과 우선순위가 시트와 다르다** — 시트는
`P3 · 김보현`, md 는 `P2 · 박세현`이다. 2026-08-19 에 세현님으로 이관된 것을 md 에만
반영해 뒀다. 시트도 맞추면 된다.

## 내 담당 — 설명 문구도 고칠 것

| ID | 무엇 |
|---|---|
| `DLV-001-1` | 출력 형식이 **셋(XLSX·HTML·MD) → 넷(+PDF)** 이다. 리비전 `0021` 이 CHECK 를 넓혔다 |

## 남의 담당 — 알려야 할 것 (내가 고치지 않는다)

| ID | 작업명 | 담당 | 지금 시트 | 실제 |
|---|---|---|---|---|
| `TSK-001-1` | 태스크 CRUD | 최재정 | 미구현 | 리비전 `0019` + 화면까지 있다 (PR #50) |
| `TSK-001-2` | 칸반 상태 변경 | 최재정 | 미구현 | 드래그로 상태가 바뀌고 `completed_at` 이 기록된다 (PR #50) |
| `TSK-003-3` | 활동 기록 조회 | 최재정, 김보현 | 미구현 | `task_activity_logs` 와 `GET /tasks/activity` 가 있다 (리비전 `0020`) |
| `SRH-002-1` | 검색 결과 재순위 | 박세현 | 미구현 | `app/rerank/` 가 의미 검색·하이브리드 **양쪽에** 붙었다 (PR #49). 기본값은 꺼짐 |
| `DOC-004-1` | 문서 목록·필터 조회 | 최재정 | 부분 구현 | 유형 필터가 화면에 붙었다 (PR #51). **처리상태 필터·페이징·N+1 은 그대로 남았다** |

> 남의 항목은 **상태를 내가 바꾸지 않는다.** 판정 근거를 본인이 갖고 있고, 내가
> 못 본 완료 조건이 있을 수 있다. 위 사실만 전달한다.

## 주의 — "구현됨" 으로 올리지 않은 것들의 이유

같은 실수를 8/20 에 한 번 했다(`DLV-001-2` 를 구현됨으로 잘못 올렸다). **완료 판정
문장을 그대로 읽고** 내가 만든 것에 맞춰 해석하지 않는다.

| ID | 판정문에서 못 채운 부분 |
|---|---|
| `DLV-002-1` | "**LLM 호출은 개요 1회**" — 개요 문장이 없다 |
| `DLV-002-2` | 판정문에 LLM 이 없지만 같은 개요 자리가 빈다. 함께 올린다 |
| `DLV-003-1` | "결정 상태별로 표가 **정확히** 생성된다" — 실제 결정 데이터로 확인 못 했다 |
| `DLV-003-2` | "PENDING 상태 항목만" — 같은 이유 |
| `DSH-001` | "**각 항목에서** 관련 화면으로 이동" — 금액 승인 대기가 남았다 |
| `SYS-003-1` | "요청 단위 추적" — 워커 로그가 요청과 이어붙지 않는다 |
