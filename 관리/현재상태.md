# 현재 상태 — 2026-08-20

**새 세션은 이 파일만 읽는다.** 다른 문서는 필요할 때 `grep` 으로 그 절만 본다.
아래 "언제 무엇을 읽나" 를 지키면 큰 문서를 통째로 읽지 않아도 된다.

## 지금 어디까지 왔나

**검색 파트가 거의 끝났다.** 내 담당(김보현 37건) 중 검색 사슬은 —

| 기능 | 상태 |
|---|---|
| 의미 검색 `SRH-001` | 구현됨 |
| 키워드 검색 `SRH-003` | **구현됨** (8/20 · PR #38) |
| 키워드·의미 결합 `SRH-004` | **구현됨** (8/20 · PR #39 · RRF) |
| 근거 스니펫 `SRH-002-2` | 구현됨 |
| 단가 선례 `SRH-002-3` | 구현됨 |
| 텍스트 정규화·청킹 `RAG-001-1` / 임베딩 저장 `-2` / 재인덱싱 `-3` | 구현됨 |
| 프롬프트 컨텍스트 조립 `RAG-002-1` | **부분 구현** — 로직만. 부르는 곳 없음 |
| 검색 품질 측정 `RAG-002-2` | 부분 구현 |
| 검색 결과 재순위 `SRH-002-1` | **박세현 담당** (8/19 이관) |
| 문서 기반 질의응답 `CHAT-001` | 미구현 · P3 · 팀이 후순위 합의 |
| **임베딩 모델 서빙 경로 `SYS-002-4`** | **미결** — 유일한 미결 P1 |

전체: 구현됨 29 · 부분 구현 24 · 미구현 60 · 검토중 1 · 미결 1 (115건)

**산출물(`DLV`) 9건은 전부 내 담당이고 전부 미구현이다. P1 이 7개다.**
8/20 에 `decisions`·`schedule_items`·`deliverables` **ORM 모델을 만들어** 그중
P1 넷(`DLV-001-2`·`003-1`·`003-2`·`003-3`)의 선행이 풀렸다. 화면 시안도 있다
(`산출물/UI시안_산출물_v1.html` · 네이비).

**임베딩 학습은 8/20 에 종료됐다.** 최종 결과로 90% 경계선이 문서 9.9건 → **16.2건**
으로 옮겨졌다(인수인계 9-G ⑤-b). 문서 제한에서 `k=5` 가 92.1% 라 **리랭커를
`top-5` 로 받아도 상한이 90% 를 넘는다** — 세현님 메모리 제약에 직접 도움이 된다.

## 다음에 할 일 — 대기 이유와 함께

| | 무엇 | 왜 대기 |
|---|---|---|
| 1 | 기능명세서 xlsm 에 `RAG-002-1` → `부분 구현` | 사용자가 시트에서 고쳐야 한다 |
| 2 | API v2 시트에 추가분 붙이기 | `관리/API_v2_구현시트_추가분_0820.tsv` (탭 구분) |
| 3 | `API_계약서_v2.md` 에 검색 합치기 | `API_계약_RAG_검색_초안.md` 의 **정정 절**을 옮긴다 |
| 4 | 하이브리드 품질 측정 | 세현님·재정님이 하이브리드+리랭커 테스트 중. 결과와 맞춘다 |
| 5 | `SYS-002-4` + 문서 선택 단계 | **리랭커까지 돌린 뒤 결정**(사용자 판단) |
| 6 | 컨텍스트 조립을 서비스에 붙이기 | `CHAT-001` 과 함께. 응답 형식이 먼저 정해져야 한다 |
| 7 | 금액 파트 (`AMT-002-1` 등 P1) | 한 번에 해야 해서 시간이 필요하다 |
| 8 | 산출물 **만들기** (`DLV-002-1`) | 미리보기(`DLV-001-2`)는 8/20 에 만들었다. 다음은 실제 파일 생성 |

**세현님께 물어볼 것** — BM25 에 형태소 분석기를 붙였는가. 붙였으면 우리 트라이그램을
바꿀 근거가 된다. (8/20 확인: 아직 BM25 를 안 쓰고 계신다)

## 반드시 기억할 함정 셋

1. **지표 정의를 확인하지 않고 숫자를 해석하지 않는다.** 같은 자료로 3.4배가 난다 —
   `accuracy@k`(첫 정답 순위가 k 이하면 1) 대 표준 `recall@k`. 우리 도구는 앞의 것이다.
2. **`similarity` 의 뜻이 `match_kind` 에 따라 다르다.** `vector`·`both` 는 코사인,
   `keyword` 는 트라이그램(조사가 붙으면 1 미만). 무조건 "유사도 N%" 로 쓰면 안 된다.
3. **검색이 0건이면 청크의 `embedding_model` 을 먼저 본다.** 지금 쓰는 모델과 다르면
   조용히 0건이다. 에러가 아니다.

## 언제 무엇을 읽나 — 통째로 읽지 않는다

| 문서 | 줄 | 언제 |
|---|---|---|
| **이 파일** | 100 | **항상. 먼저** |
| `정정_이전대화방_인식오류.md` | 150 | 새 세션에서 한 번. 짧다 |
| `작업일지_0820.md` | 190 | 8/20 에 무엇을 왜 했는지 |
| `인수인계.md` | 1,960 | **`grep -n '^## '` 로 목차만** 보고 필요한 절만. `9-B`(지표 정의)·`9-G`(90% 의 수학)·`9-I`(도구 종료)가 아직 유효하다. 나머지는 이력이다 |
| `기능명세서.md` | 867 | 특정 기능을 `grep` 으로. 통째로 읽지 않는다 |
| `API_계약_RAG_검색_초안.md` | 732 | **정정 절(맨 앞)만.** 그 아래는 8/11 설계 기록이다 |
| `작업일지_0818-0819.md` | 2,649 | **읽지 않는다.** 요점이 인수인계 9-x 에 옮겨져 있다 |
| `RAG_임베딩모델_*` 9개 | 5,600 | **읽지 않는다.** 모델 선정은 끝났다(`dragonkue/BGE-m3-ko`). 근거를 물으면 그때만 |
| `요구사항정의서` · `데이터구조_설계` · `화면정의서` | 1,755 | 그 주제를 다룰 때만 |

> **파일을 `보관/` 으로 옮기지 않았다.** 다른 문서가 30곳 넘게 참조하고 있어 경로를
> 바꾸면 링크가 깨진다. 대신 **읽지 않을 목록**으로 관리한다.

## 도구 — 무엇이 있는지만 안다

| | 언제 |
|---|---|
| `도구/spec_sync.py` | 기능명세서 xlsx·xlsm ↔ md 어긋남 검사. **분포 숫자는 손으로 세지 말고 이 출력을 옮긴다** |
| `도구/xlsx_read.py` | 엑셀을 표준 라이브러리로 읽기(`.xlsm` 됨, `.xlsb` 안 됨) |
| `도구/check_jsx.py` | `node_modules` 가 없을 때 JSX·CSS 검사. `--all` 로 검사기 자체도 확인 |
| `도구/check_hybrid_quality.py` | 세 검색 방식의 정답 순위 비교(회귀·개선) |
| `도구/embed_server.py` + `docker-compose.override.yml` | 실제 임베딩 붙이기. **둘은 한 쌍** |
| `도구/embed-test/` | **모델 선정용. 역할 종료**(인수인계 9-I). 진단 도구 3개만 계속 쓴다 |
| `관리/git_명령어_모음.md` | 사용자에게 명령어를 줄 때 참고 |
