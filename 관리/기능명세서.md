# Tasqra 기능명세서 — v5 기준 통합판

| | |
|---|---|
| 기준 | `산출물/기능명세서_v5_세분화.xlsx` (v5 · 2026-08-18 구현 확인분) |
| 기능 수 | **115건** · 13개 영역 |
| 상태 | 구현됨 26 · 부분 구현 22 · 미구현 65 · 검토중 1 · 미결 1 |
| 우선순위 | P0 30 · P1 49 · P2 25 · P3 11 |
| 담당 | 최재정 58 · 김보현 38 · 박세현 13 · 최재정, 김보현 4 · 박세현, 김보현 2 |
| 통합 | 2026-08-19 |

## 0. 이 문서가 무엇인가

기능 정의가 두 곳에 갈라져 있었다.

| | ID 체계 | 문제 |
|---|---|---|
| `산출물/기능명세서_v5_세분화.xlsx` | 3단 (`DSH-001` · `SRH-002-3`) | 표라서 **설계 근거를 담을 자리가 없다** |
| `관리/기능명세서.md` (구판) | 2단 (`VIS-01` · `RAG-04`) | 제품명이 `DocFlow` 로 남아 있고 **ID 가 낡았다** |

같은 기능이 다른 ID 로 존재해서 서로 다른 기능으로 읽힐 위험이 있었다. 실제로
대시보드가 구판에서는 `VIS-01`, v5 에서는 `DSH-001` 이다.

**v5 를 뼈대로 삼고 구판의 설계 근거를 그 위에 얹었다.** v5 가 최신 구현 확인분을
반영하고 담당·상태가 갈리는 단위로 세분화되어 있어서다.

### 영역이 재편됐다 — 단순한 이름 변경이 아니다

| 구판 | v5 | 무엇이 바뀌었나 |
|---|---|---|
| `VIS` 가시성 (9건) | **`DSH` 대시보드 + `SRH` 검색** | `VIS` 접두어가 없어졌다. 지표는 `DSH`, 검색은 `SRH` 로 갈렸다 |
| `RAG` 검색·근거 (12건) | **`RAG` 파이프라인 5 + `SRH` 검색 6 + `CHAT` 1** | 만드는 쪽(청킹·임베딩)과 쓰는 쪽(검색)을 나눴다 |
| — | **`REC` AI 추천 (신규)** | 구판에 없던 영역 |
| 나머지 영역 | 접두어 유지 · 3단으로 세분화 | `AMT-01` → `AMT-001-1`·`AMT-001-2` 처럼 쪼개졌다 |

전수 대응은 **부록 A** 에 있다.

### v5 xlsx 는 8/18 기준이다 — 8/19 머지분을 반영했다

v5 는 *2026-08-18 구현 확인분* 이다. 그 뒤 8/19 에 PR 일곱 건(#29~#35)이 머지됐다.
표만 믿으면 **이미 만든 것을 안 만든 것으로 읽는다.** 아래 세 건을 이 문서에서 고쳤다.

| 기능 | v5 (8/18) | 이 문서 (8/19) | 근거 |
|---|---|---|---|
| `DSH-001` 프로젝트 핵심 현황 | 미구현 | **부분 구현** | PR #34. 지표는 서버 집계로 동작한다. 유형 분포·금액 승인 대기에서 이동이 안 돼 완료 판정을 일부 못 만족 |
| `SRH-002-3` 유사 사업 단가 선례 검색 | 미구현 | **부분 구현** | PR #32. API 는 되고 화면이 없어 "표시된다" 를 못 만족 |
| `AMT-001-1` 금액 항목 추출 | 미구현 | 미구현 (비고만 추가) | PR #31 로 저장 스키마만 준비됐다. 추출 로직은 없다 |

`DOC`·`REV`·`SYS` 영역도 8/19 에 PR #30·#33·#35 가 머지됐다. 재정님 영역이라
**상태를 내가 단정하지 않았다** — 확인 후 갱신할 항목이다.

### v5 가 담지 않은 것을 남겼다

v5 의 부제가 *"적용 페이지별 **사용자** 기능 중심"* 이다. 그래서 사용자에게 화면으로
보이지 않는 **기반·품질 항목이 빠져 있다** — `N+1 방지` · `FK 인덱스` ·
`request_id 로깅` · `Fake AI Client` 같은 것들이다.

이것들을 v5 로 갈아치우면서 잃으면 안 된다. **17절에 따로 보존했다.**

### 개별 ID 1:1 대응표를 전부 만들지는 않았다

이름으로 자동 대조해 봤더니 구판 153건 중 **확실한 대응 80 · 애매 28 · 미대응 45**
였다. 애매한 것에는 명백히 틀린 짝이 섞여 있었다 — `DOC-09 문서 상세` →
`DOC-006-1 문서 삭제`, `RAG-04 의미 검색` → `RAG-002-2 검색 품질 측정`(실제로는
`SRH-001`).

**틀린 대응표는 없는 것보다 나쁘다.** 그래서 부록 A 에는 **눈으로 확인한 것만**
넣었다. 실제로 커밋·PR·작업일지에서 인용되는 `RAG`·`VIS` 계열은 전수 확인했다.
나머지 구판 ID 는 부록 B 에 "어느 영역으로 갔는지" 까지만 적었다.

---

## 1. 읽는 방법

### 상태

| 값 | 뜻 |
|---|---|
| `구현됨` | 코드가 있고 완료 판정을 만족한다 |
| `부분 구현` | 일부만 동작한다. 무엇이 빠졌는지는 완료 판정과 비교해서 본다 |
| `미구현` | 코드가 없다 |
| `검토중` | 할지 말지 정하지 않았다 |
| `미결` | 하기로 했으나 방식이 정해지지 않았다 |

### 우선순위

| 값 | 뜻 |
|---|---|
| `P0` | 없으면 제품이 성립하지 않는다 |
| `P1` | 시연에 필요하다 |
| `P2` | 있으면 좋다 |
| `P3` | 이번 범위 밖 |

### 담당

v5 는 이름을 그대로 쓴다. 구판은 `세현`·`재정`·`보현` 처럼 줄여 썼다.

| v5 표기 | 구판 표기 | 맡은 영역 (주로) |
|---|---|---|
| 최재정 | `재정` | 계정·권한 · 프로젝트 · 문서 입력·처리 · OCR 검수 · 일괄 처리 |
| 김보현 | `보현` | 검색·근거 · 금액 · 가시성 · 산출물 |
| 박세현 | `세현` | 분석 · AI 추천 |

`최재정, 김보현` 처럼 둘이 적힌 항목은 화면과 서버를 나눠 맡은 것이다.

### 완료 판정 기준

**"만들었다" 가 아니라 "무엇을 확인하면 됐다고 하는가" 를 적는다.** 이것이 없으면
구현 여부를 사람마다 다르게 판단한다. 판정은 가능하면 실행해서 확인할 수 있는
형태로 쓴다.

---

## 2. 제품이 무엇을 만드는가 — 흐름

```
문서 입력 → 추출·OCR 검수 → 분석(분석기 4개 병렬) → 조각 → 산출물
   세현        재정                  보현            보현     보현
 (그릇)      (입력 품질)                          (조각)   (출력물)
```

**태스크 보드는 중간 산물이고 출력물은 문서다.** 미니 프로젝트에서 우리가 손으로
만든 것(이슈 46건 · 결정사항 29건 · 주간 보고 · WBS)을 자동으로 만드는 것이
`DLV` 영역이다.

---

## 3. 요약

| 영역 | 접두어 | 기능 | 구현됨 | 부분 | 미구현 | 주 담당 |
|---|---|---|---|---|---|---|
| 계정·권한 | `AUTH` | 6 | 5 | 0 | 1 | 최재정 |
| 프로젝트 | `PRJ` | 13 | 8 | 5 | 0 | 최재정 |
| 가시성 | `DSH` | 2 | 0 | 1 | 1 | 김보현 |
| 문서 입력·처리 | `DOC` | 11 | 0 | 9 | 2 | 최재정 |
| 일괄 처리 | `BAT` | 6 | 0 | 0 | 6 | 최재정 |
| OCR 검수 | `REV` | 15 | 4 | 2 | 9 | 최재정 |
| 분석 | `ANL` | 11 | 0 | 4 | 7 | 박세현 |
| 태스크·협업 | `TSK` | 10 | 0 | 0 | 10 | 김보현 |
| 금액 | `AMT` | 10 | 0 | 0 | 10 | 김보현 |
| 산출물 | `DLV` | 9 | 0 | 0 | 9 | 김보현 |
| 검색·근거 | `CHAT·RAG·SRH` | 12 | 5 | 2 | 5 | 김보현 |
| AI 추천 | `REC` | 1 | 0 | 0 | 1 | 박세현 |
| 공통·기반 | `SYS` | 9 | 4 | 1 | 4 | 최재정 |
| **합계** | | **115** | **26** | **24** | **65** | |

---

## 4. 계정·권한 `AUTH`

| 기능 ID | 기능명 | 설명 | 상태 · 우선 · 담당 | 선행 | 완료 판정 기준 |
|---|---|---|---|---|---|
| `AUTH-001` | 회원가입 및 계정 생성 <br>※ 비밀번호는 해시로만 저장한다. | 로그인 ID, 이메일, 표시 이름, 비밀번호를 입력해 계정을 생성한다. | 구현됨 · P0 · 최재정 | - | 유효한 정보로 계정이 생성되며 중복 ID·이메일과 잘못된 입력은 명확한 오류로 차단된다. |
| `AUTH-002-1` | 로그인 | 아이디·비밀번호로 인증하고 access·refresh 토큰을 발급한다. | 구현됨 · P0 · 최재정 | AUTH-001 | 올바른 계정으로 로그인되며 틀린 자격은 401을 반환한다. |
| `AUTH-002-2` | 내 정보 조회 | 유효 토큰으로 본인 정보를 반환한다. | 구현됨 · P0 · 최재정 | AUTH-002-1 | 만료·무효 토큰은 401을 반환한다. |
| `AUTH-002-3` | 토큰 갱신 <br>※ 리비전 0004 refresh_tokens | refresh 토큰으로 access 토큰을 재발급한다. | 구현됨 · P0 · 최재정 | AUTH-002-1 | 유효한 refresh만 갱신되고 만료 시 재로그인으로 유도된다. |
| `AUTH-002-4` | 로그아웃 <br>※ revoked_at 기록 | 현재 세션의 refresh 토큰을 무효화한다. | 구현됨 · P0 · 최재정 | AUTH-002-1 | 로그아웃 후 해당 refresh 토큰은 재사용할 수 없다. |
| `AUTH-003` | SSO 로그인 <br>※ 후순위 기능 | 외부 통합 인증을 이용해 계정으로 로그인한다. | 미구현 · P3 · 최재정 | - | 지원 SSO 공급자로 인증한 사용자가 정상 로그인되고 계정 연결 상태가 유지된다. |

### 설계 근거 (구판에서 옮김)

> 아래 글은 **구판 ID 기준**으로 쓴 기록이다. 그때의 판단을 그대로 남기는 것이
> 목적이라 ID 를 바꾸지 않았다. v5 ID 대응은 **부록 A · B** 를 본다.

**`AUTH-05` 가 핵심이다.** 개별 라우터에서 검사하면 누락된다.

> **Spring 비교** — `get_current_user` 의존성은 Spring Security 의
> `SecurityFilterChain` + `@AuthenticationPrincipal` 에 해당한다. FastAPI 는
> 필터가 아니라 **의존성 주입으로** 같은 일을 한다.

---

## 5. 프로젝트 `PRJ`

| 기능 ID | 기능명 | 설명 | 상태 · 우선 · 담당 | 선행 | 완료 판정 기준 |
|---|---|---|---|---|---|
| `PRJ-001-1` | 프로젝트 생성 | 새 프로젝트를 생성한다. | 구현됨 · P0 · 최재정 | AUTH-002-1 | 생성자가 자동으로 OWNER 멤버로 등록된다. |
| `PRJ-001-2` | 내 프로젝트 목록 조회 | 내가 참여한 프로젝트만 조회한다. | 구현됨 · P0 · 최재정 | PRJ-001-1 | 내가 멤버인 프로젝트만 표시된다. |
| `PRJ-002-1` | 받은 초대 목록 조회 | 받은 초대의 프로젝트·초대자·역할·상태를 조회한다. | 구현됨 · P1 · 최재정 | AUTH-002-1 | 현재 사용자에게 온 초대만 표시된다. |
| `PRJ-002-2` | 초대 수락 | 받은 초대를 수락한다. | 구현됨 · P1 · 최재정 | PRJ-002-1 | 수락한 사용자는 해당 프로젝트 멤버가 된다. |
| `PRJ-002-3` | 초대 거절 | 받은 초대를 거절한다. | 구현됨 · P1 · 최재정 | PRJ-002-1 | 거절한 초대는 상태 이력으로 보존된다. |
| `PRJ-003-1` | 프로젝트 정보 수정 | 프로젝트 이름·설명 등 상세 정보를 수정한다. | 구현됨 · P0 · 최재정 | PRJ-001-1 | EDITOR 이상만 수정 가능하고 VIEWER는 403을 받는다. |
| `PRJ-003-2` | 프로젝트 상태 변경 <br>※ projects.status | 진행 중·보관됨 상태를 변경한다. | 구현됨 · P0 · 최재정 | PRJ-001-1 | 보관 시 목록에서 구분 표시되고 문서는 유지된다. |
| `PRJ-003-3` | 프로젝트 삭제 | 프로젝트를 삭제한다. | 구현됨 · P0 · 최재정 | PRJ-001-1 | OWNER만 가능하며 문서·태스크가 함께 정리된다. |
| `PRJ-004-1` | 멤버 초대 <br>※ TOCTOU: 유니크 제약으로 체크·실행 일원화 | 이메일과 역할을 지정해 멤버를 초대한다. | 부분 구현 · P0 · 최재정 | PRJ-001-1 | 이미 멤버면 409를 반환한다. |
| `PRJ-004-2` | 초대 취소 | 보낸 초대를 취소한다. | 부분 구현 · P0 · 최재정 | PRJ-004-1 | OWNER만 취소할 수 있고 취소 후 재사용되지 않는다. |
| `PRJ-004-3` | 멤버 역할 변경 <br>※ 금액 열람 권한은 AMT-003-1과 함께 정책 확정 필요 | 멤버의 OWNER/EDITOR/VIEWER 역할을 변경한다. | 부분 구현 · P0 · 최재정 | PRJ-004-1 | 역할별 허용 동작 표대로 권한이 즉시 반영된다. |
| `PRJ-004-4` | 멤버 제외 | 프로젝트에서 멤버를 제외한다. | 부분 구현 · P0 · 최재정 | PRJ-004-1 | 제외된 사용자는 즉시 프로젝트 범위 밖 데이터에 접근할 수 없다. |
| `PRJ-004-5` | 최근 초대 사용자 재선택 <br>※ UX 편의 기능 | 최근에 초대했던 사용자를 목록에서 다시 선택한다. | 부분 구현 · P2 · 최재정 | PRJ-004-1 | 최근 초대 이력이 선택 목록에 노출된다. |

### 설계 근거 (구판에서 옮김)

> 아래 글은 **구판 ID 기준**으로 쓴 기록이다. 그때의 판단을 그대로 남기는 것이
> 목적이라 ID 를 바꾸지 않았다. v5 ID 대응은 **부록 A · B** 를 본다.

### 권한 표 (`PRJ-07`)

| 동작 | OWNER | EDITOR | VIEWER |
|---|:-:|:-:|:-:|
| 문서 업로드 · 삭제 | O | O | — |
| OCR 검수 | O | O | — |
| 제안 승인 · 태스크 편집 | O | O | — |
| **산출물 생성** | O | O | — |
| 문서 · 태스크 · 산출물 조회 | O | O | O |
| 멤버 초대 · 권한 변경 | O | — | — |
| 프로젝트 삭제 | O | — | — |
| **금액 항목 조회** | O | O | **미결 — `AMT-11`** |

---

## 6. 가시성 `DSH`

| 기능 ID | 기능명 | 설명 | 상태 · 우선 · 담당 | 선행 | 완료 판정 기준 |
|---|---|---|---|---|---|
| `DSH-001` | 프로젝트 핵심 현황 <br>※ **PR #34(8/19) 로 서버 집계 구현.** 남은 것 둘 — 문서 유형 분포 칸에서 걸러진 목록으로 이동 불가(`DocumentsView` 에 유형 필터가 없다. 서버는 `document_type` 파라미터를 이미 받는다) · 금액 승인 대기는 이동할 화면이 없다. 열린 태스크는 `decisions` ORM 모델이 없어 `null` 로 낸다(0 이 아니다) | 문서 수, 처리 중 문서, 열린 태스크, 승인 대기, 문서 유형 분포, 최근 문서를 요약한다. | **부분 구현** · P1 · 김보현 | PRJ-001-1 | 최신 데이터가 지표·목록에 반영되고 각 항목에서 관련 화면으로 이동할 수 있다. |
| `DSH-002` | 승인 대기 및 활동 타임라인 <br>※ v4 에서 이 행의 열이 한 칸 밀려 있었다(선행 기능 칸에 완료 판정 기준이 들어감). 바로잡았다 | 승인 대기 제안, 이번 주 활동, 제안 종류별 채택률, 프로젝트 활동 이력을 보여준다. | 미구현 · P1 · 김보현 | DSH-001 | 승인 대기 건수·활동 이력이 종류·시각별로 표시되고 관련 화면으로 이동할 수 있다. |

### 설계 근거 (구판에서 옮김)

> 아래 글은 **구판 ID 기준**으로 쓴 기록이다. 그때의 판단을 그대로 남기는 것이
> 목적이라 ID 를 바꾸지 않았다. v5 ID 대응은 **부록 A · B** 를 본다.

**`VIS-07` 의 성격을 바꿨다.** 검색 엔진 자체는 `RAG-04`(P1) 가 만들므로
`VIS-07` 은 **그 결과를 `VIS-06` 검색 화면에 얹는 UI** 로 좁힌다.
`RAG` 영역이 신설되면서 같은 기능이 두 번 정의된 상태였다
(`VIS-07` P3 · `RAG-04` P1). 번호는 재사용하지 않으므로 지우지 않고 성격만 바꿨다.

**`P3` 에서 `P2` 로 올렸다.** `RAG-04` 가 P1 인데 사용자가 그 결과를 볼 수단이
P3 이면 P1 기능이 화면 없이 남는다. 선행인 `VIS-06`(P2) 과 같은 층위로 맞췄다.

---

## 7. 문서 입력·처리 `DOC`

| 기능 ID | 기능명 | 설명 | 상태 · 우선 · 담당 | 선행 | 완료 판정 기준 |
|---|---|---|---|---|---|
| `DOC-001` | 파일 업로드 및 안전성 검증 <br>※ 기본 최대 크기 20MB. 중복검사 해시는 SHA-256 | PDF·DOCX·HWPX·JPG·PNG 파일을 다중 업로드하고 형식·크기·빈 파일·페이지·문자 제한·중복(SHA-256)·파일명 안전성을 검증한다. | 부분 구현 · P0 · 최재정 | PRJ-001-1 | 유효한 파일만 등록되고 실패 파일은 원인별 오류를 표시하며 내부 저장명 충돌·경로 조작이 없다. |
| `DOC-002` | 문서 추출·OCR 및 추출 전략 <br>※ DOCX·HWPX 포함, 이미지 OCR 포함 | 형식·전략에 따라 텍스트 레이어·OCR·하이브리드 추출기를 선택하고 방법을 기록한다. | 부분 구현 · P0 · 최재정 | DOC-001 | AUTO·TEXT_ONLY·TEXT_WITH_IMAGE_OCR 전략과 실제 추출 방법이 기록된다. |
| `DOC-003-1` | 처리 모드 선택 | 업로드 시 일반·검수·일괄 모드를 선택한다. | 미구현 · P0 · 최재정 | DOC-001 | 모드가 저장되고 기본값은 일반이며 검수 모드는 검수 대기 상태로 시작한다. |
| `DOC-003-2` | 비동기 처리 및 진행 상태 조회 <br>※ 큐 실행과 상태·오류 기록은 동작한다(documents.status, processing_error). 남은 것은 DOC-003-1 모드 연동 | 업로드를 비동기 큐로 실행하고 단계별 진행·오류를 표시한다. | 부분 구현 · P0 · 최재정 | DOC-003-1, SYS-002-2 | 업로드 요청이 즉시 문서 ID를 반환하고 처리 단계·완료·실패 상태를 지속 확인할 수 있다. |
| `DOC-004-1` | 문서 목록·필터 조회 <br>※ 성능 판정 기준 미달 — N+1 3곳. (1) _build_list_row 가 get_latest_by_type 을 문서마다 2회 + active_ocr_char_count 로 extracted_text 지연로딩, size=20 에 약 62쿼리 (2)(3) document_router 51·84행과 _ordered_ocr_elements 의 page.elements. 처방: IN 배치 + joinedload / selectinload | 프로젝트 문서 목록을 페이징 조회하고 유형·처리상태로 필터링한다. | 부분 구현 · P0 · 최재정 | PRJ-001-1 | 현재 프로젝트 문서만 표시되고 필터가 정상 동작하며 조회 성능이 문서 수에 비례해 악화되지 않는다. |
| `DOC-004-2` | 문서 유형 지정·자동 판별 | 업로드 시 문서 유형을 지정하거나 자동 판별한다. | 부분 구현 · P1 · 최재정 | DOC-001 | 9종 문서 유형과 유형 출처(사용자/AI/AI수정)가 저장된다. |
| `DOC-005-1` | 문서 원문·메타정보 조회 | 요약, 추출 원문, 추출 방식, OCR 엔진, 페이지·글자 수, 신뢰도, 처리 시간을 조회하고 본문을 검색·복사한다. | 부분 구현 · P0 · 최재정 | DOC-002 | 문서의 원문과 처리 메타정보가 정확히 표시되고 본문 검색·복사가 정상 동작한다. |
| `DOC-005-2` | 원본·요약 다운로드 | 업로드 당시 원본 파일과 요약본을 다운로드한다. | 부분 구현 · P0 · 최재정 | DOC-005-1 | 업로드 당시 파일명으로 원본·요약 다운로드가 정상 동작한다. |
| `DOC-006-1` | 문서 삭제 <br>※ cascade | 문서를 삭제한다. | 부분 구현 · P0 · 최재정 | DOC-005-1 | 하위 데이터(추출텍스트·분석·OCR요소·금액·결정·일정)가 함께 정리된다. |
| `DOC-006-2` | 재분석 실행 <br>※ analyses 1:N | 문서를 다시 분석한다. | 부분 구현 · P1 · 최재정 | DOC-005-1, ANL-003-2 | 재분석 결과가 기존 이력을 덮어쓰지 않고 새 행으로 쌓인다. |
| `DOC-006-3` | 문서 버전 이력·변경점 비교 <br>※ 후순위 | 문서 변경 버전을 누적해 과거 결과와 비교한다. | 미구현 · P3 · 최재정 | DOC-006-1 | 이전 버전과의 변경점을 확인할 수 있다. |

### 설계 근거 (구판에서 옮김)

> 아래 글은 **구판 ID 기준**으로 쓴 기록이다. 그때의 판단을 그대로 남기는 것이
> 목적이라 ID 를 바꾸지 않았다. v5 ID 대응은 **부록 A · B** 를 본다.

**`DOC-06` 이 본프로젝트 최우선이다.** 현재는 업로드 요청 스레드에서 OCR 을 직접
돌린다. 다중 사용자에서 바로 워커가 고갈된다.

### `DOC-16` 의 목적이 바뀌었다

원래는 **분석기를 고르는 기준**을 만들기 위한 기능이었다. 그 계획(`ANL-04`
분석기 선택 규칙)이 폐기됐으므로 목적이 셋으로 줄었다.

| 쓰이는 곳 | |
|---|---|
| 목록 필터 · 대시보드 유형 분포 | `documents.document_type` |
| **프롬프트 힌트** | 계약서라고 알려주면 금액 분석기가 계약금액에 집중한다 |
| 산출물 분류 | 주간 보고서에서 "회의록 3건 · 계약서 2건" |

**분석기를 껐다 켜지는 않는다.** 유형을 몰라도 분석은 전부 돌아간다.

---

## 8. 일괄 처리 `BAT`

| 기능 ID | 기능명 | 설명 | 상태 · 우선 · 담당 | 선행 | 완료 판정 기준 |
|---|---|---|---|---|---|
| `BAT-001-1` | 다중 파일 업로드 <br>※ 브라우저 순차 호출로 데모 충분 | 여러 파일을 한 번에 등록한다. | 미구현 · P1 · 최재정 | DOC-001 | 파일별 성공·실패가 독립적으로 기록된다. |
| `BAT-001-2` | 처리 대기열 조회 | 파일별 대기·처리중·검수필요·완료·실패 상태와 실패 사유를 표시한다. | 미구현 · P1 · 최재정 | BAT-001-1 | 사용자가 전체 처리 현황을 확인할 수 있다. |
| `BAT-002-1` | 서버 일괄 작업 실행·복구 <br>※ batch_jobs / batch_items. OCR Worker 수 제한 포함 | 브라우저 종료와 무관하게 배치를 실행하고 중단 후 복구한다. | 미구현 · P2 · 최재정 | DOC-003-2 | 서버 작업이 중단 후에도 복구되며 진행률이 자원 한도 안에서 갱신된다. |
| `BAT-002-2` | 실패 재시도 | 실패한 항목만 재시도한다. | 미구현 · P2 · 최재정 | BAT-002-1 | 실패 항목만 독립적으로 재시도할 수 있다. |
| `BAT-002-3` | 실시간 진행률 (SSE) | 일괄 처리 진행률을 실시간으로 표시한다. | 미구현 · P2 · 최재정 | BAT-002-1 | 진행률이 실시간으로 갱신된다. |
| `BAT-002-4` | 로컬 폴더 자동 감시 <br>※ 별도 설치형. 후순위·범위 밖 가능성 | 지정 폴더에 파일이 추가되면 자동으로 업로드한다. | 미구현 · P3 · 최재정 | BAT-002-1 | 감시 폴더에 파일 추가 시 자동 등록된다. |

### 설계 근거 (구판에서 옮김)

> 아래 글은 **구판 ID 기준**으로 쓴 기록이다. 그때의 판단을 그대로 남기는 것이
> 목적이라 ID 를 바꾸지 않았다. v5 ID 대응은 **부록 A · B** 를 본다.

**`BAT-01` 은 브라우저가 순차 호출하는 방식으로 데모에 충분하다.** `BAT-03` 은
운영용이고 `DOC-06` 큐가 전제다. `batch_jobs.default_document_type` 으로 배치
단위 유형을 지정한다 (`DOC-16`).

---

## 9. OCR 검수 `REV`

| 기능 ID | 기능명 | 설명 | 상태 · 우선 · 담당 | 선행 | 완료 판정 기준 |
|---|---|---|---|---|---|
| `REV-001-1` | OCR 박스 표시 및 신뢰도 구분 <br>※ 박스·신뢰도 조회 API(GET review)와 화면은 있다. 신뢰도 구간 시각 구분은 미확인 | 페이지 이미지 위에 OCR 박스를 표시하고 신뢰도별로 구분한다. | 부분 구현 · P1 · 최재정 | DOC-002 | 확대·축소와 무관하게 박스가 정확히 표시되고 신뢰도 구간이 시각적으로 구분된다. |
| `REV-001-2` | 낮은 신뢰도 요소 모아보기 | 임계치 미만 박스만 순회하며 검수한다. | 미구현 · P1 · 최재정 | REV-001-1 | 낮은 신뢰도 박스만 목록으로 순회할 수 있다. |
| `REV-002-1` | 박스 텍스트 수정 | OCR 박스의 텍스트를 수정한다. | 구현됨 · P1 · 최재정 | REV-001-1 | 수정 후 original_text는 보존되고 text만 바뀐다. |
| `REV-002-2` | 박스 이동·크기 조정 | 박스 위치·크기를 조정한다. | 미구현 · P2 · 최재정 | REV-002-1 | 크기만 바꾸면 텍스트는 유지된다. |
| `REV-002-3` | 박스 생성·삭제·복원 | 박스를 새로 만들거나 논리 삭제·복원한다. | 미구현 · P2 · 최재정 | REV-002-1 | 삭제는 논리 삭제로 처리되고 복원할 수 있다. |
| `REV-002-4` | 박스 병합·분리 | 여러 박스를 합치거나 하나를 나눈다. | 미구현 · P2 · 최재정 | REV-002-2 | 병합·분리 후에도 원본과 수정값이 보존된다. |
| `REV-002-5` | 선택 영역 재OCR | 선택한 영역을 다시 OCR 처리한다. | 미구현 · P2 · 최재정 | REV-002-1 | 사용자 확인 후에만 재OCR 결과가 반영된다. source=RE_OCR |
| `REV-003-1` | 단락 지정·병합·분리 | OCR 요소의 단락을 지정·병합·분리한다. | 미구현 · P2 · 최재정 | REV-001-1 | OCR 재실행 없이 단락 구조가 반영된다. |
| `REV-003-2` | 읽기 순서 변경 <br>※ 지정순서 > 지정단락 > XY-Cut — RAG-001-1 청킹 입력 순서와 직결 | 요소의 읽기 순서를 수동으로 바꾼다. | 미구현 · P2 · 최재정 | REV-003-1 | 사용자 지정 순서가 자동 결과(XY-Cut)를 덮지 않는다. |
| `REV-003-3` | 표 행·열 구조 보정 <br>※ 후순위 | 표의 행·열 구조를 수동으로 보정한다. | 미구현 · P3 · 최재정 | REV-003-1 | 보정된 표 구조가 텍스트 재조립에 반영된다. |
| `REV-004-1` | 검수 완료 처리 <br>※ offset 기록 불필요. 완료 직후 RAG-001-3 재인덱싱이 큐에 걸린다 | 검수를 완료하고 확정 텍스트를 재조립한다. | 구현됨 · P1 · 최재정 | REV-002-1 | 완료 시 is_confirmed=true, 확정자·시각·리비전이 기록되고 텍스트가 같은 트랜잭션에서 갱신된다. |
| `REV-004-2` | 수정 이력 조회·되돌리기 <br>※ 이력 조회(GET history)는 있고 되돌리기는 미확인 | OCR 수정 이력을 조회하고 이전 버전으로 되돌린다. | 부분 구현 · P2 · 최재정 | REV-002-1 | ocr_element_revisions로 이력이 남고 되돌리기가 가능하다. |
| `REV-004-3` | 분석 제외·복원 <br>※ is_excluded 컬럼 | 특정 OCR 요소를 분석 대상에서 제외하거나 복원한다. | 구현됨 · P1 · 최재정 | REV-001-1 | 제외된 요소는 확정 텍스트·분석에 포함되지 않으며 복원 시 다시 포함된다. |
| `REV-004-4` | 동시 수정 충돌 방지 <br>※ ocr_elements.version | 같은 박스를 동시에 수정하면 충돌을 차단한다. | 구현됨 · P1 · 최재정 | REV-002-1 | 같은 박스를 두 사용자가 수정하면 나중 요청이 409를 받는다. |
| `REV-004-5` | 분석 결과 최신성 표시 <br>※ v4 누락분 보완. 판정 재료는 이미 있다 — extracted_texts.text_version, documents.ocr_revision, analyses 의 ocr_revision 을 대조하면 된다. 청크도 같은 방식(ix_chunk_stale)을 쓴다 | OCR 확정 후 분석이 오래됐음을 사용자에게 알린다. | 미구현 · P1 · 최재정 | REV-004-1 | OCR을 고친 뒤 화면에 '분석을 다시 실행해 주세요' 배너가 표시된다. |

### 설계 근거 (구판에서 옮김)

> 아래 글은 **구판 ID 기준**으로 쓴 기록이다. 그때의 판단을 그대로 남기는 것이
> 목적이라 ID 를 바꾸지 않았다. v5 ID 대응은 **부록 A · B** 를 본다.

데이터 요구사항은 `OCR-DB 설계안` 을 **변경 없이 채택**했다.

### 1단계 — 표시와 기본 수정

### 2단계 — 영역과 구조 편집

**`REV-13` 의 우선순위 규칙**을 코드에 못박는다.

```
사용자 지정 순서 > 사용자 지정 단락 > 자동 Recursive XY-Cut 결과
```

**`REV-03` 이 가장 자주 깨지는 지점이다.** 프론트가 보는 이미지와 OCR 좌표 기준
이미지가 다르면 박스가 어긋난다. `document_pages.image_path` 를 기준본으로 고정한다.

**금액 정확도가 검수에 달려 있다.** 숫자는 OCR 오인식에 가장 취약해서 `1` 을 `7` 로
읽으면 전부 틀린다. 문장은 오탈자가 있어도 요약이 대충 맞지만 금액은 아니다.

---

## 10. 분석 `ANL`

| 기능 ID | 기능명 | 설명 | 상태 · 우선 · 담당 | 선행 | 완료 판정 기준 |
|---|---|---|---|---|---|
| `ANL-001-1` | 문서 요약 | 문서를 요약한다. | 부분 구현 · P0 · 박세현 | DOC-002 | 요약문과 토큰·지연시간이 함께 기록된다. |
| `ANL-001-2` | 문서 유형 분류 | 문서를 지정된 문서 유형으로 자동 분류한다. | 부분 구현 · P0 · 박세현 | DOC-004-2 | 분류 결과와 판단 근거(reason)가 함께 생성된다. 사용자 지정 유형은 자동 분류로 덮어쓰지 않는다. |
| `ANL-002-1` | 액션 아이템 추출 | 문서에서 액션 아이템을 구조화해 추출한다. | 미구현 · P1 · 박세현 | ANL-001-1 | 정의된 스키마의 배열이 생성되고 근거·확신도가 함께 붙는다. 문서에 없는 값은 만들지 않는다. |
| `ANL-002-2` | 결정사항 추출 <br>※ DLV-003-2의 재료 | 회의록 등에서 결정된 사항을 추출한다. | 미구현 · P1 · 박세현 | ANL-001-1 | 결론이 안 난 항목은 PENDING으로 남아 회의 안건 재료가 된다. |
| `ANL-002-3` | 일정·기한 추출 <br>※ DLV-002-1·DLV-002-2의 재료 | 문서의 날짜·기한을 추출한다. | 미구현 · P1 · 박세현 | ANL-001-1 | 날짜 형식을 원문과 대조해 정확도를 측정할 수 있다. |
| `ANL-003-1` | 분석 진행률 표시 | 분석기별 진행 상태를 표시한다. | 부분 구현 · P1 · 박세현 | ANL-001-1 | 분석기가 추가돼도 진행률 표시 로직(화면 코드)을 고치지 않는다. |
| `ANL-003-2` | 분석 이력·버전 조회 <br>※ v4 누락분 보완 — ocr_revision 추적 명시 | 재분석 결과와 모델·프롬프트 버전, 실행 메타정보를 기록·조회한다. | 부분 구현 · P0 · 박세현 | ANL-001-1 | 최신·과거 결과가 구분 조회되고 어느 OCR 리비전으로 분석했는지 함께 기록된다. |
| `ANL-004-1` | LLM 재시도·폴백 <br>※ 재시도·폴백의 유일한 소유 항목 (SYS-003에는 포함하지 않음) | 일시 실패 시 재시도하고 지속 실패 시 대체 모델을 쓴다. | 미구현 · P2 · 박세현 | ANL-001-1 | 일시 오류는 재시도되고 지속 실패는 대체 경로로 처리된다. |
| `ANL-004-2` | 평가셋 기반 정확도 측정 <br>※ 박세현님 모델 비교에 직접 사용 | 동일 평가셋으로 모델·프롬프트별 정확도를 측정한다. | 미구현 · P1 · 김보현 | ANL-002-1 | 정답 데이터와 대조한 정확도가 수치로 나오고 모델·프롬프트를 바꿔도 같은 자로 잰다. |
| `ANL-004-3` | 교정 분석기 <br>※ 후순위 | 문서의 오탈자·비문을 교정한다. | 미구현 · P3 · 박세현 | ANL-001-1 | 교정 전/후가 함께 표시된다. |
| `ANL-004-4` | 번역 분석기 <br>※ 후순위 | 문서를 다른 언어로 번역한다. | 미구현 · P3 · 박세현 | ANL-001-1 | 번역 결과가 원문과 함께 표시된다. |

### 설계 근거 (구판에서 옮김)

> 아래 글은 **구판 ID 기준**으로 쓴 기록이다. 그때의 판단을 그대로 남기는 것이
> 목적이라 ID 를 바꾸지 않았다. v5 ID 대응은 **부록 A · B** 를 본다.

### 분석기는 4개, `gather` 한 번

**문서 유형으로 분석기를 고르지 않는다. 항상 넷 다 돌린다.**

```python
analyzed = await asyncio.gather(
    summary.analyze(content),     # 요약        (기존 코드 그대로)
    category.analyze(content),    # 분류        (기존 코드 그대로)
    extract.analyze(content),     # 액션·결정·일정  (신규)
    amount.analyze(content),      # 금액        (신규)
)
```

### 왜 4개인가 — 통합하지 않은 이유

| | 분석기 4개 (채택) | 통합 1개 |
|---|---|---|
| 입력 토큰 | 약 11,200 (본문 4회) | 약 2,850 (본문 1회) |
| 병렬 | `gather` 한 번, 완전 병렬 | 해당 없음 |
| 기존 코드 | **요약·분류 0줄 수정** | 흡수하며 회귀 위험 |
| 실패 단위 | 하나 실패해도 나머지 살아남는다 | 전부 실패 |
| 금액 정확도 | **프롬프트 집중 가능** | 주의가 분산된다 |
| 프롬프트 버전 | 분석기별로 따로 올린다 | 하나로 뭉친다 |

**토큰 2배를 감수하고 4개로 간다.** 개발은 `USE_FAKE_AI` 로 하고 실제 호출은
데모·측정 때만이므로 감당 가능하다. 그리고 **금액은 정확도가 중요해 프롬프트를
섞지 않는 게 맞다.**

**통합 방식은 측정해서 비교한다.** 미니 프로젝트에서 OCR 엔진 3종을 측정해 비교한
것과 같은 패턴이고, "왜 그렇게 했나" 에 수치로 답할 수 있다.

### 순환 의존이 없어졌다

문서 유형으로 분석기를 고르지 않으므로 **"분류는 분석 결과인데 선택은 분석 전"**
문제가 사라졌다. `ANL-02` 분류는 다른 분석기와 나란히 돌고, 결과를
`documents.document_type` 에 확정값으로 올린다.

> **Spring 비교** — `Analyzer` Protocol 은 `interface Analyzer { AnalyzeResult
> analyze(String text); }` 이고, `dependencies.get_analyzer_registry()` 는
> `@Configuration` 에서 `Map<String, Analyzer>` 빈을 만드는 것과 같다.
> `asyncio.gather` 는 `CompletableFuture.allOf(...)` 에 해당한다.

### 분석기를 하나 추가하는 비용

| 손대는 곳 | |
|---|---|
| `analyzers/xxx_analyzer.py` | 새 클래스 |
| `analyzers/prompts.py` | 프롬프트 |
| `dependencies.py` | registry **한 줄** |
| `models/enums.py` | `AnalyzerType` 값 |

**라우터 · 서비스 · 스키마는 안 고친다.** `analysis_router` 가 `analyzer_types` 를
그대로 넘기고 `analyses.analyzer_type` 이 `String(30)` 이라 DB 변경도 없다.

---

## 11. 태스크·협업 `TSK`

| 기능 ID | 기능명 | 설명 | 상태 · 우선 · 담당 | 선행 | 완료 판정 기준 |
|---|---|---|---|---|---|
| `TSK-001-1` | 태스크 CRUD | 제목·설명·담당자·기한·상태를 가진 태스크를 생성·조회·수정·삭제한다. | 미구현 · P1 · 최재정 | PRJ-001-1 | 사람이 태스크를 직접 만들고 수정·삭제할 수 있다. |
| `TSK-001-2` | 칸반 상태 변경 <br>※ completed_at은 DLV-002-1 필수 입력 | 3열 칸반에서 드래그로 상태를 변경한다. | 미구현 · P1 · 최재정 | TSK-001-1 | 드래그로 상태가 바뀌고 완료 시 completed_at이 정확히 기록된다. |
| `TSK-002-1` | AI 제안 승인 <br>※ 자동 등록 금지 원칙 | AI 제안을 승인해 태스크로 확정한다. | 미구현 · P1 · 김보현 | ANL-002-1 | 승인해야 등록되고, 승인 전에는 보드에 나타나지 않는다. |
| `TSK-002-2` | AI 제안 거절 <br>※ 채택률 지표의 원천 | AI 제안을 거절한다. | 미구현 · P1 · 김보현 | ANL-002-1 | 거절한 제안은 다시 뜨지 않고 거절 사실이 기록된다. |
| `TSK-002-3` | AI 제안 수정 | 승인 전 AI 제안 내용을 수정한다. | 미구현 · P1 · 김보현 | ANL-002-1 | 수정 후 값으로 태스크가 확정된다. |
| `TSK-002-4` | 일괄 승인 | 문서 단위로 여러 제안을 한 번에 승인한다. | 미구현 · P1 · 김보현 | TSK-002-1 | 문서 단위로 다건 제안이 한 번에 승인된다. |
| `TSK-003-1` | 출처·판단 근거 표시 <br>※ 원문 위치 하이라이트는 SRH-002-2에서 이미 구현됐다 — 같은 방식(content_start·content_end + ?from=&to=)을 재사용할 수 있다 | 태스크에 출처 문서 링크와 AI 판단 근거를 표시한다. | 미구현 · P2 · 최재정, 김보현 | TSK-001-1 | 출처 문서 링크와 reason이 함께 보인다. |
| `TSK-003-2` | AI 생성 배지·필터 | AI 생성 태스크를 구분하고 필터링한다. | 미구현 · P2 · 최재정, 김보현 | TSK-001-1 | AI생성만·기한임박·출처문서별 필터가 정상 동작한다. |
| `TSK-003-3` | 활동 기록 조회 <br>※ activity_logs | 프로젝트 내 주요 변경을 활동 이력으로 조회한다. | 미구현 · P2 · 최재정, 김보현 | TSK-001-1 | 주요 변경이 활동 이력에 남는다. |
| `TSK-003-4` | 외부 알림 <br>※ 후순위 | 이메일·Slack으로 알림을 보낸다. | 미구현 · P3 · 최재정 | TSK-003-3 | 설정된 채널로 알림이 발송된다. |

### 설계 근거 (구판에서 옮김)

> 아래 글은 **구판 ID 기준**으로 쓴 기록이다. 그때의 판단을 그대로 남기는 것이
> 목적이라 ID 를 바꾸지 않았다. v5 ID 대응은 **부록 A · B** 를 본다.

### `tasks` 에 컬럼 두 개가 필요하다

**`completed_at` 이 없으면 `DLV-04` 주간 보고서를 만들 수 없다.** `status=DONE` 은
알 수 있어도 **언제 완료됐는지** 모른다. 목업 보드가 이미 `3월 7일 완료` 를
보여주는데 담을 컬럼이 없다. `updated_at` 도 함께 추가한다.

**`source_amount_item_id`** — 태스크 출처가 2갈래가 된다 (`source_analysis_id` +
금액 항목). `CHECK` 로 둘 중 최대 하나만 채워지게 한다.

---

## 12. 금액 `AMT`

| 기능 ID | 기능명 | 설명 | 상태 · 우선 · 담당 | 선행 | 완료 판정 기준 |
|---|---|---|---|---|---|
| `AMT-001-1` | 금액 항목 추출 <br>※ LLM은 추출만. **PR #31(8/19) 로 저장 스키마는 준비됐다** — `amount_items` ORM 모델 + 리비전 0015(`unit_price`·`category`). 추출 로직은 아직 없다 | 문서에서 항목명·수량·단위·단가·금액·원가구분·근거를 추출한다. | 미구현 · P1 · 박세현, 김보현 | ANL-002-1 | 계약 스키마에 맞는 값만 저장되고 문서에 없는 값은 비운다. |
| `AMT-001-2` | 금액 항목 승인·수정 | 추출된 금액 항목을 사용자가 승인하거나 수정한다. | 미구현 · P1 · 박세현, 김보현 | AMT-001-1 | 승인 전에는 어디에도 반영되지 않고 수정 전/후 값이 보존된다. |
| `AMT-002-1` | 금액 계산·합계 검증 <br>※ 계산은 코드가 수행. 결정론적 | 수량×단가로 항목 금액을 계산하고 문서 합계와 대조한다. | 미구현 · P1 · 김보현 | AMT-001-2 | 동일 입력에 동일 계산 결과가 나오고 불일치 금액이 표시된다. |
| `AMT-002-2` | 프로젝트 금액 집계 <br>※ 단위테스트로 확인 | 여러 문서의 금액을 프로젝트 단위로 모은다. | 미구현 · P1 · 김보현 | AMT-002-1 | 같은 입력이면 항상 같은 집계 결과가 나온다. |
| `AMT-003-1` | 금액 열람 권한 적용 <br>※ VIEWER 노출 정책 확정 필요 — 미결 | 역할에 따라 금액 열람을 제한한다. | 미구현 · P2 · 김보현 | PRJ-004-3 | 허용된 사용자만 금액을 조회할 수 있다. |
| `AMT-003-2` | 프로젝트 금액 현황 조회 | 계약금액·변경 증감·집행률·잔액을 프로젝트 단위로 보여준다. | 미구현 · P2 · 김보현 | AMT-002-2 | 현황 수치가 정확히 집계돼 표시된다. |
| `AMT-003-3` | 계산식·산출 근거 표시 | 금액마다 계산식과 출처 문서·원문 근거를 표시한다. | 미구현 · P1 · 김보현 | AMT-002-1 | 각 금액의 계산식과 원문 근거로 이동할 수 있다. |
| `AMT-004-1` | 근거 변경 탐지 및 재계산 | 근거 문서가 변경되면 영향받은 값을 다시 추출·계산한다. | 미구현 · P2 · 김보현 | AMT-002-1 | 변경된 필드만 재계산되고 모델 재학습은 없다. |
| `AMT-004-2` | 계산 버전·변경 이력 저장 | 변경 전후 값과 차액을 저장한다. | 미구현 · P2 · 김보현 | AMT-004-1 | 계산 버전과 차액이 추적된다. |
| `AMT-004-3` | 불일치 태스크 제안 | 합계 불일치 시 태스크를 제안한다. | 미구현 · P1 · 김보현 | AMT-002-1, TSK-002-1 | 불일치 시 승인형 태스크 제안 카드가 생기고 자동 등록은 하지 않는다. |

### 설계 근거 (구판에서 옮김)

> 아래 글은 **구판 ID 기준**으로 쓴 기록이다. 그때의 판단을 그대로 남기는 것이
> 목적이라 ID 를 바꾸지 않았다. v5 ID 대응은 **부록 A · B** 를 본다.

**원칙 — 있는 금액을 모은다. 없는 금액을 만들지 않는다.**

`추계`(단가 × 수량으로 없는 금액을 산출)를 버리고 `집계`(문서에 적힌 금액을 모으기)로
좁혔다. 그래서 단가 마스터가 필요 없고, **"그 숫자 근거가 뭐냐" 에 전부 문서에서
나왔다고 답할 수 있다.**

### `AMT-03` 과 `AMT-06` 이 이 영역의 가치다

**문서 하나로는 알 수 없는 것을 알려준다.**

```
용역계약서_최종.pdf        계약금액   72,000,000
계약_변경합의서_스캔.jpg    증액       +15,000,000
3월 2주차 주간보고.docx     집행액      60,000,000
                    ↓  자동 집계
현재 계약금액  87,000,000
집행액        60,000,000  (69%)
잔액          27,000,000

⚠ 변경합의서 기재 총액(85,000,000)과 계산값(87,000,000) 불일치 → 확인 태스크
```

**요약과 액션아이템은 문서 하나로 끝나지만 금액은 모아야 의미가 생긴다.** 그래서
"문서는 프로젝트 맥락 안에 있다" 는 컨셉을 증명하는 건 금액이다.

**그리고 검증 가능한 유일한 기능이다.** 요약 · 결정 · 일정은 AI 가 뽑은 게 맞는지
확인할 방법이 없지만 **금액은 재계산해서 대조**한다.

---

## 13. 산출물 `DLV`

| 기능 ID | 기능명 | 설명 | 상태 · 우선 · 담당 | 선행 | 완료 판정 기준 |
|---|---|---|---|---|---|
| `DLV-001-1` | 산출물 조건 선택 | 산출물 유형·기간·출력 형식(XLSX·HTML·MD)을 선택한다. | 미구현 · P1 · 김보현 | PRJ-001-1 | 형식을 고르지 않으면 생성 버튼이 비활성화된다. |
| `DLV-001-2` | 생성 대상 미리보기 <br>※ 빈 보고서 방지 + 비용 절약 | 생성 전 대상 문서·태스크·결정·기한·금액·승인대기 건수를 확인한다. | 미구현 · P1 · 김보현 | DLV-001-1 | LLM 호출 전에 건수가 보이고 대상이 없으면 생성이 방지된다. |
| `DLV-002-1` | 주간 보고서 생성 <br>※ tasks.completed_at 필수 | 선택 기간의 문서·태스크·결정·기한·금액 변동을 보고서로 만든다. | 미구현 · P1 · 김보현 | DLV-001-2, TSK-001-2 | 개요·문서목록·태스크·결정·기한·금액변동이 한 파일로 나온다. LLM 호출은 개요 1회. |
| `DLV-002-2` | 프로젝트 현황 한 장 생성 | 일정·태스크·결정·금액 현재 상태를 한 페이지로 만든다. | 미구현 · P2 · 김보현 | DLV-001-2 | 현재 상태가 한 페이지 요약으로 생성된다. |
| `DLV-002-3` | 인수인계 문서 생성 <br>※ 후순위 | 프로젝트 종료 시 전체 누적 내용을 인수인계 문서로 만든다. | 미구현 · P3 · 김보현 | DLV-002-2 | 프로젝트 전체 누적 내용이 하나의 문서로 생성된다. |
| `DLV-003-1` | 결정사항 대장 생성 | 결정 내용·결정일·출처·상태(확정/미결/뒤집힘)를 표로 만든다. | 미구현 · P1 · 김보현 | DLV-001-2, ANL-002-2 | 결정 상태별로 표가 정확히 생성된다. |
| `DLV-003-2` | 다음 회의 안건 생성 <br>※ DLV-003-1의 부산물 | 미결(PENDING) 결정만 모아 회의 안건을 만든다. | 미구현 · P1 · 김보현 | DLV-003-1 | PENDING 상태 항목만 안건으로 생성된다. |
| `DLV-003-3` | 생성 이력·다운로드 <br>※ deliverables 테이블 | 만든 산출물을 목록으로 보고 다시 내려받는다. | 미구현 · P1 · 김보현 | DLV-002-1 | 생성된 산출물이 이력으로 남고 재다운로드할 수 있다. |
| `DLV-003-4` | 갱신 필요 판정 <br>※ source_counts_json 스냅샷 비교 | 생성 후 원천 데이터가 늘면 다시 만들기를 안내한다. | 미구현 · P1 · 김보현 | DLV-003-3 | 생성 후 대상이 추가되면 갱신 필요 표시가 뜬다. |

### 설계 근거 (구판에서 옮김)

> 아래 글은 **구판 ID 기준**으로 쓴 기록이다. 그때의 판단을 그대로 남기는 것이
> 목적이라 ID 를 바꾸지 않았다. v5 ID 대응은 **부록 A · B** 를 본다.

**이 영역이 제품의 출력물이다.** 문서를 넣으면 문서가 나온다.

미니 프로젝트에서 **우리가 손으로 만든 것**을 자동으로 만드는 것이다.

| 우리가 손으로 만든 것 | 대응 기능 |
|---|---|
| 결정사항 대장 29건 | `DLV-05` |
| 주간 보고 | `DLV-04` |
| 회의 안건 정리 | `DLV-06` |
| 프로젝트 현황 보고 | `DLV-07` |

### `DLV-04` 의 LLM 호출이 1회뿐인 이유

**재요약의 입력이 원문이 아니라 저장된 요약이다.**

| 방식 | 문서 10건 기준 입력 토큰 |
|---|---:|
| 원문을 다시 보낸다 | **약 28,000** |
| **저장된 요약만 보낸다** | **약 1,500** |

`analyses.result_json['summary']` 에 이미 있는 값을 쓴다. **주간 보고서 한 장이
문서 반 건 분석보다 싸다.**

그리고 **개요 한 단락만 LLM 이 쓴다.**

```
주간 보고서
├─ 이번 주 개요        ← LLM 1회 (저장된 요약 N개를 재요약)
├─ 문서 목록           ← DB 쿼리
├─ 완료 / 신규 태스크   ← DB 쿼리 (tasks.completed_at 필요)
├─ 이번 주 결정사항     ← DB 쿼리
├─ 기한 임박 · 지난     ← DB 쿼리 + 날짜 계산
└─ 금액 변동           ← DB 쿼리 + 코드 합산
```

**나머지는 전부 결정론적이라 같은 기간을 다시 누르면 같은 표가 나온다.**

### `DLV-03` 미리보기가 세 가지를 해결한다

| | |
|---|---|
| 빈 보고서 방지 | 기간을 잘못 잡아 "문서 0건" 인 보고서를 만드는 걸 막는다 |
| 비용 절약 | **LLM 호출 전에** 무엇이 잡히는지 보인다. 미리보기는 DB 쿼리라 공짜 |
| 승인 유도 | `승인 대기 5건` 을 보여줘 "먼저 승인하고 만드시죠" 를 화면이 알려준다 |

### 파일 생성 도구가 이미 있다

미니 프로젝트에서 산출물을 만들려고 짠 스크립트를 그대로 쓴다.

| 도구 | 용도 |
|---|---|
| `도구/_md2html.py` | 마크다운 → 인쇄용 HTML |
| `도구/_build_wbs.py` · `도구/_build_feature_spec.py` | **`openpyxl` 없이 xlsx 생성** |

**엑셀 생성이 이미 검증됐다.** 이 문서의 xlsx 판이 그 도구로 만들어졌다.

### 시연은 우리 프로젝트로 한다

**Tasqra 에 우리 팀 문서를 넣는다.** 기획안 · 회의 메모 · 설계 문서를 올리면
결정사항 대장과 주간 보고서가 나온다.

> 우리가 이번 프로젝트에서 **손으로 만든 산출물을 이 도구가 자동으로 만듭니다.**

**손으로 만든 것과 자동 생성한 것을 나란히 놓을 수 있다.** 미니 프로젝트 심사에서
"주장에 기준·수치가 없다" 가 지적됐는데, 이 형태는 비교 대상이 있어 기준이 저절로
붙는다.

---

## 14. 검색·근거 `CHAT·RAG·SRH`

| 기능 ID | 기능명 | 설명 | 상태 · 우선 · 담당 | 선행 | 완료 판정 기준 |
|---|---|---|---|---|---|
| `SRH-001` | 의미 검색 <br>※ POST /api/search. 키워드·하이브리드는 SRH-003·SRH-004로 분리했다. '다른 프로젝트 문서 제외'는 '내가 멤버가 아닌 프로젝트 제외'로 읽는다 — 그래야 SRH-002-3과 모순되지 않는다 | 내가 멤버인 프로젝트 범위에서 벡터 의미 검색을 제공한다. 범위는 내 멤버십 전체와 특정 프로젝트 중에서 고른다. | 구현됨 · P1 · 김보현 | RAG-001-2 | 의미기반 질의가 관련 조각을 찾고, 내가 멤버가 아닌 프로젝트 문서는 어떤 경우에도 결과에 나오지 않는다. 문서를 열었다 돌아와도 검색 결과가 유지된다. |
| `SRH-002-1` | 검색 결과 재순위 <br>※ 후순위 | 검색 결과를 관련도에 따라 재정렬한다. | 미구현 · P3 · 김보현 | SRH-001 | 재정렬된 순서로 결과가 표시된다. |
| `SRH-002-2` | 근거 스니펫 연결 <br>※ 응답의 content_start·content_end 를 쓴다(서버 변경 없음). 원문이 검수로 수정되면 좌표가 어긋날 수 있다 — 길이를 벗어난 경우는 걸러내지만 내용만 바뀐 경우는 못 잡는다. 응답에 text_version 을 더하면 해결(미결) | 검색·응답 결과마다 출처 문서와 원문 인용을 연결한다. | 구현됨 · P1 · 김보현 | SRH-001 | 결과마다 출처 문서와 인용 근거가 표시되고, 조각을 누르면 원문의 해당 위치로 이동해 강조된다. |
| `SRH-002-3` | 유사 사업 단가 선례 검색 <br>※ 비용추계의 근거 — 도메인을 좁힌 이유. 검색 API 변경은 필요 없다. project_ids 에 (내 멤버십 − 현재 프로젝트)를 넘기면 된다 — _resolve_scope 가 부분집합을 받는다. **PR #32(8/19) 로 API 구현**(`GET /api/projects/{id}/amount-precedents`, 검증 19건). 화면이 아직 없어 완료 판정의 '표시된다' 를 만족하지 못한다 | 과거 유사 사업 문서에서 같은 항목의 단가를 찾는다. | **부분 구현** · P2 · 김보현 | SRH-001 | 과거 사업의 단가가 출처와 함께 표시된다. |
| `SRH-003` | 키워드 검색 <br>※ SRH-001에서 분리. 의미 검색은 고유명사·숫자에 약하다. 문서 목록의 q 필터(ilike)와는 다른 기능 — 조각 단위로 찾아야 근거 인용이 된다 | 고유명사·숫자·문서번호처럼 정확한 문자열로 문서와 조각을 찾는다. | 미구현 · P1 · 김보현 | RAG-001-1 | 고유 문자열이 의미 검색에서 누락돼도 키워드 검색으로 찾힌다. |
| `SRH-004` | 키워드·의미 결합(하이브리드) <br>※ 결합 방식(RRF 등) 미정. SRH-002-1 재순위와 같이 검토할 것 | 키워드 결과와 의미 검색 결과를 하나의 순위로 합친다. | 미구현 · P2 · 김보현 | SRH-001, SRH-003 | 두 방식 중 한쪽만 찾아내는 질의도 결합 결과에서 상위에 나온다. |
| `CHAT-001` | 문서 기반 질의응답 <br>※ 검색 범위 정책은 SRH-001에서 확정됐다(project_ids, 멤버 아닌 프로젝트는 404). 그 규칙을 그대로 재사용한다 | 접근 가능한 문서를 검색해 질문에 답하고 근거를 제공한다. | 미구현 · P3 · 김보현 | SRH-001 | 답변마다 근거 문서와 원문 인용이 표시되고 권한 밖 문서는 제외된다. |
| `RAG-001-1` | 텍스트 정규화·청킹 <br>※ CHARS_PER_TOKEN(실측 1.89)이 세 값과 곱해진다 — MAX_TOKENS(최대 글자), MIN_TOKENS(흡수 기준), OVERLAP_TOKENS(겹침 글자). 비율만 고치면 세 정책이 같이 변한다. 2026-08-18에 1.2→1.89로만 고쳐서 흡수 기준이 58자→91자로 넓어져 짧은 절이 삼켜지고 검색 정답이 1위→2위로 밀렸다. MIN_TOKENS를 48→30으로 같이 내려 해결. 도구/check_chunking.py 가 조각 경계를 대조해 이 회귀를 막는다 | 확정 텍스트를 정규화하고 청크로 자른다. | 구현됨 · P1 · 김보현 | REV-004-1 | element_type으로 단락을 묶고 토큰 수 기준으로 자르며 표는 행 단위를 유지한다. |
| `RAG-001-2` | 임베딩 생성·벡터 인덱스 저장 <br>※ 모델을 바꾸면 전체 재청킹이 필수다. 검색이 WHERE embedding_model = 현재 모델로 걸리므로, 안 하면 검색 결과가 조용히 0건이 된다. 실제 모델 dragonkue/BGE-m3-ko · 1024차원 | 청크를 임베딩으로 변환해 pgvector에 저장한다. | 구현됨 · P1 · 김보현 | RAG-001-1, SYS-002-1 | 청크마다 모델·차원·출처가 기록되고 프로젝트 범위 인덱스가 유지된다. |
| `RAG-001-3` | 검수 확정 시 재인덱싱 <br>※ 검수 완료 라우터에서 트랜잭션 커밋 후 큐에 넣는다(안에서 넣으면 워커가 커밋 전 본문을 읽는다). 큐 등록 실패는 삼키고 ChunkRepository.stale_document_ids()로 복구한다. 건너뛰기 0.010초 vs 재청킹 0.62~1.08초 | OCR 수정 후 해당 문서를 다시 인덱싱한다. | 구현됨 · P1 · 김보현 | RAG-001-2, REV-004-1 | OCR을 고치면 청크가 재임베딩되고 낡은 벡터로 검색되지 않는다. 본문이 바뀌지 않은 검수 완료는 재임베딩을 건너뛴다. 본문이 비면 기존 청크를 지운다. |
| `RAG-002-1` | 프롬프트 컨텍스트 조립 <br>※ 겹침이 길어지면 인접 조각이 겹치는 내용을 또 담아 예산을 낭비한다. OVERLAP_TOKENS 조정과 함께 볼 것 | 토큰 예산 안에서 관련 청크를 조립한다. | 미구현 · P1 · 김보현 | RAG-001-2 | 긴 문서에서도 컨텍스트 한도를 넘지 않는다. |
| `RAG-002-2` | 검색 품질 측정 <br>※ 도구/embed-test/ir_eval.py(질의 133건, accuracy@1·map@100·ndcg@10·recall@5) + 도구/check_rag04_quality.py(순위 회귀 3건). 청킹 값을 바꿀 때 필수 — 값만 고치고 안 재면 회귀를 놓친다(실제로 겪음) | 동일 질의셋으로 검색 적중률을 반복 측정한다. | 부분 구현 · P2 · 김보현 | RAG-001-2 | 같은 질의셋으로 청킹·임베딩 변경 효과를 비교할 수 있다. |

### 설계 근거 (구판에서 옮김)

> 아래 글은 **구판 ID 기준**으로 쓴 기록이다. 그때의 판단을 그대로 남기는 것이
> 목적이라 ID 를 바꾸지 않았다. v5 ID 대응은 **부록 A · B** 를 본다.

**도메인을 공공 SI·용역으로 좁힌 뒤 정식 범위가 됐다.** 앞서 "기간 필터가 그
자리를 채우고 있다" 는 이유로 범위에서 뺐는데, **비용추계의 단가 근거를 과거
문서에서 찾아올 수 있다는 점**이 근거를 바꿨다. 환각이 아니라 선례 기반 추정이
된다.

**임베딩은 금액 전용이 아니다.** 요약·분류·검색이 같은 파이프라인을 쓴다.
특히 45,000자 문서를 요약할 때 무엇을 프롬프트에 넣을지 고르는 데 쓰인다.

**`RAG-09` 가 가장 놓치기 쉽다.** 검수 전파 사슬(`REV-17` → `REV-18` → `ANL-06`)의
끝이 임베딩이다. OCR 을 고쳤는데 벡터DB 가 옛 텍스트를 갖고 있으면 검색이 낡은
것을 찾아온다. **검수가 무의미해진다.**

**`RAG-11` 챗봇은 후순위로 합의했다.** 의미 검색(`RAG-04`)과 단가 선례
검색(`RAG-12`)을 먼저 만든다. 그 둘은 금액 영역의 근거 확보에 직접 쓰이고,
챗봇은 그 위에 얹는 화면이다.

---

## 15. AI 추천 `REC`

| 기능 ID | 기능명 | 설명 | 상태 · 우선 · 담당 | 선행 | 완료 판정 기준 |
|---|---|---|---|---|---|
| `REC-001` | 액션 태스크 추천 및 프로젝트 갭 분석 <br>※ 프롬프트+RAG 방식 우선 검증. 선행인 RAG-002-1(컨텍스트 조립)은 김보현 담당이므로 그것이 끝난 뒤 착수 | 프로젝트 문서·업무 흐름을 분석해 놓친 작업과 부족한 내용을 근거와 함께 제안한다. | 검토중 · P2 · 박세현 | RAG-002-1, TSK-002-1 | 추천마다 근거·출처가 표시되고 사용자가 승인한 제안만 태스크가 된다. |

---

## 16. 공통·기반 `SYS`

| 기능 ID | 기능명 | 설명 | 상태 · 우선 · 담당 | 선행 | 완료 판정 기준 |
|---|---|---|---|---|---|
| `SYS-001-1` | 인증 미들웨어 <br>※ get_current_user. 라우터마다 개별 검사하면 누락된다 | 모든 보호 API에 사용자 인증을 적용한다. | 구현됨 · P0 · 최재정 | AUTH-002-1 | 토큰 없이 보호된 엔드포인트 호출 시 401을 받는다. |
| `SYS-001-2` | 프로젝트 스코프 강제 <br>※ 리포지토리 계층에서 차단 | 역할 권한과 프로젝트 경계를 서버 계층에서 강제한다. | 구현됨 · P0 · 최재정 | PRJ-001-1 | 다른 프로젝트의 식별자를 직접 입력해도 404를 받는다. |
| `SYS-002-1` | Alembic 마이그레이션 <br>※ 리비전 0001~0014. api 컨테이너가 기동 시 alembic upgrade head 를 돌린다 | DB 스키마 변경을 마이그레이션으로 관리한다. | 구현됨 · P0 · 김보현 | - | create_all() 의존 없이 컬럼 추가가 데이터 유실 없이 반영된다. |
| `SYS-002-2` | 비동기 작업 큐 <br>※ 태스크 documents.extract · chunks.build. 큐에는 정수 인자만 실리고 워커가 DB 를 다시 읽는다 — 그래서 넣는 쪽이 커밋을 먼저 해야 한다 | Celery·Redis로 백그라운드 작업을 처리한다. | 구현됨 · P0 · 최재정 | SYS-002-1 | 워커가 죽어도 작업이 큐에 남고 재시작 시 이어진다. |
| `SYS-002-3` | 영속 파일 저장소 <br>※ 산출물 파일도 같은 곳에 둔다 | S3·MinIO 계열로 파일을 저장한다. | 미구현 · P1 · 최재정 | - | 컨테이너 재기동 후에도 파일이 유지된다. |
| `SYS-002-4` | 임베딩 모델 서빙 경로 확정 <br>※ 현재는 호스트에서 embed_server.py 를 띄워 쓴다(개발 전용, 포트 8900). 컨테이너에 sentence-transformers 를 넣지 않기로 결정 — api·worker 각 2.3GB 로 가용 메모리를 넘는다. 모델을 바꾸면 전체 재청킹이 필요하다 | 청킹·검색이 쓸 임베딩 모델을 어디서 실행할지 정하고 배포에 반영한다. | 미결 · P1 · 김보현 | RAG-001-2 | 개발·배포 양쪽에서 같은 모델·차원으로 임베딩되고, 모델 이름이 document_chunks.embedding_model 과 일치한다. |
| `SYS-003-1` | 오류코드 체계·요청 로깅 | 일관된 오류코드와 request_id 로그를 제공한다. (LLM 재시도·폴백은 ANL-004-1이 전담) | 부분 구현 · P0 · 최재정, 김보현 | - | 주요 오류가 동일한 응답 형식과 서버 로그에 남고 요청 단위 추적이 가능하다. |
| `SYS-003-2` | API 호출 쿼터 <br>※ TOCTOU: 원자적 UPDATE로 체크·실행 일원화 | 프로젝트별 API 호출량을 제한한다. | 미구현 · P2 · 박세현 | - | 월 한도 초과 시 요청이 차단된다. |
| `SYS-003-3` | 로컬 sLLM 옵션 <br>※ 모델 선정 후 진행 | OpenAI 대신 로컬 모델을 선택적으로 쓴다. | 미구현 · P3 · 박세현 | - | AI_PROVIDER 설정으로 로컬 모델 사용이 전환된다. |

### 설계 근거 (구판에서 옮김)

> 아래 글은 **구판 ID 기준**으로 쓴 기록이다. 그때의 판단을 그대로 남기는 것이
> 목적이라 ID 를 바꾸지 않았다. v5 ID 대응은 **부록 A · B** 를 본다.

**`SYS-01` 은 P0 착수 전에 끝내야 한다.** `SYS-09` 는 미니 프로젝트에서 프리즈
때문에 미룬 한 줄짜리 결함이므로 첫날 고친다.

---

## 17. 기반·품질 항목 — v5 에 없어 보존

v5 는 사용자 기능 중심이라 아래 항목들이 빠져 있다. 화면에 보이지 않지만
**빠지면 조용히 문제가 되는** 것들이라 여기 남긴다. 구판 ID 를 그대로 쓴다 —
v5 에 대응이 없어서 새 ID 를 임의로 만들면 명세에 없는 ID 가 생긴다.

| 구판 ID | 항목 | 왜 남기나 |
|---|---|---|
| `AUTH-05` | 인증 의존성 `get_current_user` | 개별 라우터에서 검사하면 **누락된다.** 모든 프로젝트 스코프 API 에 적용됨을 목록으로 확인해야 한다 |
| `PRJ-07` | 권한 3종 (OWNER · EDITOR · VIEWER) | 역할별 허용 동작 표가 문서로 있고 각 항목이 테스트로 확인돼야 한다 |
| `PRJ-08` | **프로젝트 스코프 강제** | 다른 프로젝트의 `document_id` 를 직접 넣어도 404 가 나와야 한다. 리포지토리 계층에서 막는다 |
| `SYS-07` | N+1 방지 | 목록 조회 쿼리 수가 문서 수와 무관해야 한다 (PR #33 으로 3곳 처리) |
| `SYS-08` | FK 인덱스 | 조인 성능 |
| `SYS-04` | `request_id` 로깅 | 장애 추적 |
| `SYS-05` | Fake AI Client | 개발 중 실수로 상용 API 가 호출되어 비용이 나가는 것을 막는다 |
| `DOC-13` | 중복 파일 검사 (SHA-256) | `content_hash` 로 같은 파일 재업로드를 걸러낸다 |
| `DOC-14` | 파일 저장소 이전 (S3 · MinIO) | 컨테이너 재기동 후 파일 유지 |
| `REV-01` | 페이지 이미지 생성·저장 | 검수 화면의 전제 |
| `REV-03` | 이미지 위 Bounding Box 표시 | 같음 |
| `BAT-05` | OCR Worker 수 제한 병렬 | 메모리 초과 방지 |
| `ANL-08` | 프롬프트 버전 관리 | 결과가 바뀐 원인을 추적하려면 필요하다 |

**이 목록은 "v5 에서 빠졌으니 안 한다" 가 아니다.** 대부분 이미 구현됐거나
구현 중이고, v5 의 표 형식에 들어갈 자리가 없었을 뿐이다. v6 를 만들 때
`SYS` 영역에 넣을지 판단해야 한다.

---

## 18. 폐기한 기능 — 기록용

번호를 재사용하지 않는다. 왜 뺐는지가 결정 이력이다.

### 함께 폐기한 테이블

`analyzer_rules` · `unit_prices` · `cost_estimates` · `cost_estimate_lines`

### 폐기하지 않고 성격만 바꾼 것

| | 전 | 후 |
|---|---|---|

---

## 19. 비기능 요구사항

| 구분 | 요구 | 측정 방법 |
|---|---|---|
| 성능 | 10페이지 스캔 PDF 처리 완료 ≤ 60초 | 동일 파일 3회 평균 |
| 성능 | 목록 조회 쿼리 수가 문서 수와 무관 | 쿼리 로그 개수 (`SYS-07`) |
| 성능 | **주간 보고서 생성 ≤ 15초** | LLM 1회 + DB 쿼리 |
| 동시성 | 4~5인 동시 사용 시 정상 동작 | 동시 업로드 5건 |
| 정확도 | 액션아이템 승인율 ≥ 70% | `TSK-07` 거부 기록 기반 |
| 정확도 | 카테고리 분류 정확도 ≥ 85% | **`document_type_source=USER_CORRECTED` 비율** |
| 정확도 | **일정 추출 정확도** | Golden Dataset · 원문 대조 (날짜는 형식이 있다) |
| 정확도 | **금액 항목 추출 정확도** | Golden Dataset · 원문 대조 |
| 정확도 | **금액 집계 정확도 = 100%** | 단위테스트 (`AMT-06`) |
| 보안 | 타 프로젝트 데이터 접근 차단 | `PRJ-08` 침투 테스트 |
| 보안 | API 키는 서버에만 | 프론트 번들 검사 |
| 안정성 | 컨테이너 재기동 후 파일 유지 | `DOC-14` |

**분류 정확도 측정 방식이 바뀌었다.** 별도 정답 데이터를 만드는 대신 **사람이 AI
판별을 고친 비율**을 쓴다. 실사용 데이터라 발표에서도 정답셋보다 설득력이 있다.

**금액 지표를 두 줄로 나눈 것이 중요하다.** 추출은 원문이 정답이라 측정할 수 있고,
집계는 코드가 하니 100% 여야 한다. **"AI 가 금액을 얼마나 잘 맞추나" 는 정답이
없어서 측정할 수 없다.**

---

## 20. 하지 않을 것

| 안 함 | 이유 |
|---|---|
| 실시간 공동 편집 | Notion · Google Docs 대체가 아니다 |
| 간트 차트 등 본격 PMS | 범위 밖 |
| **사람이 원가를 입력하는 비용 관리 모듈** | 컨셉과 반대다. **우리는 문서에서 뽑는다** |
| **금액 추계 · 예측** | 없는 값을 만드는 일이다. 단가 근거를 댈 수 없다 |
| 모바일 네이티브 앱 | 반응형 웹으로 대응 |
| **법안 등 법률문서 자동 생성** | 환각 위험 · 법적 책임 · 검증 부담 |
| 파인튜닝 | 20일에 자리가 없다. 프롬프트 + 구조화 출력으로 목표 달성 가능 |
| **원문 위치 하이라이트** | 분석기 규격을 바꿔야 해서 세 사람이 서로 기다린다 |

**세 번째 항목의 경계를 분명히 한다.** 사람이 원가를 입력하는 관리 모듈은 안 하고,
**문서에서 추출한 금액의 집계와 검증은 한다.** 차이는 "입력을 누가 하는가" 다.

---

## 21. 미결 사항

### 우선순위

| | 안건 | 의견 |
|---|---|---|
| 1 | **P0+P1 이 80개, 구현 20일.** 덜어내기보다 **묶기**로 접근한다 (2절) | |
| 2 | `ANL-03` 항목 추출과 `AMT-01` 금액 중 무엇을 먼저 | **`ANL-03` 으로 제안 골격을 만들고 금액을 두 번째로 얹는다** |
| 3 | 검수 2단계(`REV-08`~`REV-15`)를 P2 로 두는 데 동의하는지 | 재정님 판단 |
| 4 | `DLV` 영역을 P1 로 두는 데 동의하는지 | **동의 필요.** 이게 없으면 "태스크 모음으로 뭘 하나" 에 답이 없다 |

### 설계

| | 안건 | 의견 |
|---|---|---|
| 5 | `tasks.completed_at` · `updated_at` 추가 | **필수.** 없으면 `DLV-04` 불가 |
| 6 | `tasks.source_amount_item_id` 추가 | 필수 |
| 7 | `documents.document_type` 7종 enum 실제 사용 | 필수 |
| 8 | `documents.document_type_source` 추가 | 선택. 분류 정확도 실측용 |
| 9 | `projects.started_on` · `due_on` 추가 | 선택. 일정 산출물용 |
| 10 | `activity_logs.action_type` 값 목록 | **팀 합의 필요.** 주간 보고서 재료 |
| 11 | 분석기 4개 vs 통합 1개 | **4개로 시작. 측정 후 재검토** |
| 12 | `VIEWER` 에게 금액 노출 여부 (`AMT-11`) | 미결 |
| 13 | 산출물 파일 저장 위치 | `DOC-14` S3 · MinIO 와 같은 곳 |

### OCR DB (`OCR-DB 설계안` 10절)

| | 안건 | 의견 |
|---|---|---|
| 14 | `page_number` 0 부터 · 1 부터 | **1 부터** |
| 15 | 좌표 기준 이미지 | **렌더링본** |
| 16 | `polygon_json` | MVP 제외, 컬럼만 준비 |
| 17 | 검수 완료 후 재수정 | **허용, `IN_PROGRESS` 로 되돌림** |
| 18 | 수정 이력 MVP 부터 | P2. 단 `ocr_engine`·`engine_version`·`preprocess_info` 는 MVP 부터 |
| 19 | 박스 1개 수정이 `ocr_revision` 을 항상 올리는지 | **올린다.** 안 올리면 전파 사슬이 깨진다 |
| 20 | 텍스트 재조립 동기 · 비동기 | **동기 · 같은 트랜잭션** |
| 21 | `group_id` 만 먼저 둘지 | **`group_id` 먼저.** `ocr_groups` 는 P2 |
| 22 | 재OCR 자동 교체 | **사용자 확인 후 반영** |
| 23 | 오래된 분석 자동 재실행 | **사용자 재실행 요구.** 자동은 API 비용이 튄다 |

### 운영

| | 안건 | 의견 |
|---|---|---|
| 24 | 배포 환경 — 클라우드 · 온프레미스 | OCR GPU 필요 여부와 직결 |
| 25 | AI 제공자 — OpenAI 유지 · 로컬 sLLM 병행 | **상용 API 유지 권장** (1조가 Gemma 1B 에서 언어 쏠림을 겪었다) |

---

## 부록 A. 구판 ID → v5 ID (눈으로 확인한 것)

커밋 메시지 · PR · 작업일지에 남은 옛 ID 를 찾을 때 쓴다.

### 검색·근거 — `RAG` 12건 전수

| 구판 | v5 | 기능명 |
|---|---|---|
| `RAG-01` | `RAG-001-1` | 텍스트 정규화·청킹 |
| `RAG-02` | `RAG-001-2` | 임베딩 생성·벡터 인덱스 저장 |
| `RAG-03` | `RAG-001-2` | 벡터 인덱스 구축 — `RAG-001-2` 에 합쳐졌다 |
| `RAG-04` | `**SRH-001**` | 의미 검색 |
| `RAG-05` | `**SRH-004**` | 키워드·의미 결합(하이브리드) |
| `RAG-06` | `**SRH-002-1**` | 검색 결과 재순위 |
| `RAG-07` | `RAG-002-1` | 프롬프트 컨텍스트 조립 |
| `RAG-08` | `**SRH-002-2**` | 근거 스니펫 연결 |
| `RAG-09` | `RAG-001-3` | 검수 확정 시 재인덱싱 |
| `RAG-10` | `RAG-002-2` | 검색 품질 측정 |
| `RAG-11` | `**CHAT-001**` | 문서 기반 질의응답 |
| `RAG-12` | `**SRH-002-3**` | 유사 사업 단가 선례 검색 |

`SRH-003` 키워드 검색은 **v5 신규**다. 구판에 짝이 없다.

### 가시성 — `VIS` 9건 전수

| 구판 | v5 | 비고 |
|---|---|---|
| `VIS-01` | `**DSH-001**` | 대시보드 지표 카드 → 프로젝트 핵심 현황 |
| `VIS-02` | `DSH-001` | 문서 유형 분포 — `DSH-001` 설명에 포함 |
| `VIS-03` | `**DSH-002**` | 승인 대기 배너 |
| `VIS-04` | `DSH-001` | 최근 문서 — `DSH-001` 에 포함 |
| `VIS-05` | `DSH-002` | 이번 주 활동 — `DSH-002` 에 포함 |
| `VIS-06` | `**SRH-003**` | 통합 검색(PostgreSQL FTS) → 키워드 검색 |
| `VIS-07` | `SRH-001` | 검색 화면의 의미 검색 표시 |
| `VIS-08` | ``DLV` 영역` | 주간 자동 리포트 — 구판에서 이미 이동 |
| `VIS-09` | `DSH-002` | 활동 타임라인 — `DSH-002` 에 포함 |

---

## 부록 B. 나머지 구판 ID

부록 A 밖의 구판 ID 는 **영역만** 대응한다. 접두어가 같으므로 그 영역의 v5 표에서
기능명으로 찾으면 된다.

| 구판 접두어 | v5 영역 | 주의 |
|---|---|---|
| `AUTH` `PRJ` `DOC` `BAT` `REV` `ANL` `TSK` `AMT` `DLV` `SYS` | 같은 접두어 | 3단으로 쪼개졌다. `AMT-01` 하나가 `AMT-001-1`·`AMT-001-2` 둘이 된 경우가 있다 |
| `VIS` | `DSH` · `SRH` | 부록 A 참고 |
| `RAG` | `RAG` · `SRH` · `CHAT` | 부록 A 참고 |

**1:1 로 단정하지 말 것.** 세분화로 하나가 여럿이 되었거나 여럿이 하나로 합쳐진
경우가 있다. 기능명과 완료 판정을 함께 보고 판단해야 한다.
