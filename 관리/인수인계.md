# 인수인계 — 임베딩 모델 확정 직후

```
작성      김보현 (Kiro 세션 인수인계용)
기준 시각  2026-08-14 오전
목적      새 세션에서 맥락 복원 없이 바로 작업을 이어받는다
읽는 순서  1절(작업 규칙) -> 3절(git 상태) -> 5절(미커밋 파일) -> 6절(결정 대기) -> 10절(다음 작업)
```

> **이 문서가 필요한 이유** — 이전 세션에서 대화 압축이 한 번 일어나 세부가
> 뭉개졌다. 실제로 리비전 번호를 `0010` 으로 잘못 기억한 사고가 있었다.
> **채팅 기억에 의존하지 않고 파일로 남긴다.**

<!-- 목차 시작 -->

## 목차

- [1. 작업 규칙 — 레포마다 권한이 다르다](#1-작업-규칙--레포마다-권한이-다르다)
  - [그 외 지켜온 규칙](#그-외-지켜온-규칙)
- [2. 지금 어디까지 왔나 — 한 줄](#2-지금-어디까지-왔나--한-줄)
- [3. git 상태 — 2026-08-14 오전 확인](#3-git-상태--2026-08-14-오전-확인)
  - [`deliver` (개인 · 산출물)](#deliver-개인--산출물)
  - [`Tasqra` (팀 · 코드)](#tasqra-팀--코드)
- [4. 확정된 것](#4-확정된-것)
  - [임베딩 모델](#임베딩-모델)
  - [측정 규모](#측정-규모)
  - [주의 — 채택 모델은 Ollama 공식 라이브러리에 없다](#주의--채택-모델은-ollama-공식-라이브러리에-없다)
  - [커밋된 산출물 문서 8개](#커밋된-산출물-문서-8개)
  - [문서를 공유용으로 손본 것 (`c098536` · `be4fae1`)](#문서를-공유용으로-손본-것-c098536--be4fae1)
- [5. 커밋 안 된 채로 남아 있는 것 — 가장 중요](#5-커밋-안-된-채로-남아-있는-것--가장-중요)
  - [5-1. Tasqra 벡터 DB 마이그레이션](#5-1-tasqra-벡터-db-마이그레이션)
  - [스키마 (작성된 그대로)](#스키마-작성된-그대로)
  - [5-2. `embed-test` 코드가 레포에 있지만 낡았다](#5-2-embed-test-코드가-레포에-있지만-낡았다)
  - [5-3. 코퍼스가 레포에 없어 재현이 안 된다](#5-3-코퍼스가-레포에-없어-재현이-안-된다)
- [6. 지금 결정 대기 중인 것 다섯 개](#6-지금-결정-대기-중인-것-다섯-개)
  - [B 가 왜 먼저인가](#b-가-왜-먼저인가)
  - [B 의 내용 — 원본 위치 하이라이트](#b-의-내용--원본-위치-하이라이트)
  - [B 의 결론 — 넣는다. 코드에서 확인한 근거 네 개](#b-의-결론--넣는다-코드에서-확인한-근거-네-개)
  - [B 를 쓸 때 반드시 지킬 것 세 가지 — 확인된 함정](#b-를-쓸-때-반드시-지킬-것-세-가지--확인된-함정)
- [7. 재정님과 협의할 안건 여섯 개](#7-재정님과-협의할-안건-여섯-개)
  - [테이블을 따로 빼는 것은 취향이 아니라 제약이다](#테이블을-따로-빼는-것은-취향이-아니라-제약이다)
- [8. 리비전 번호 상황](#8-리비전-번호-상황)
- [9. 미확인 목록 — 모른 채로 단정하지 않는다](#9-미확인-목록--모른-채로-단정하지-않는다)
- [10. 다음 작업 순서](#10-다음-작업-순서)
  - [10-1. 공지는 머지 뒤다 — 순서를 틀렸던 기록](#10-1-공지는-머지-뒤다--순서를-틀렸던-기록)
  - [4번 검증 명령 (로컬)](#4번-검증-명령-로컬)
- [11. 이번 시험에서 틀렸던 것 — 반복 방지](#11-이번-시험에서-틀렸던-것--반복-방지)
  - [철회한 서술 네 개 — 모두 표본이 작을 때 1~4문항 차이로 쓴 결론](#철회한-서술-네-개--모두-표본이-작을-때-14문항-차이로-쓴-결론)
  - [방법론에서 틀렸던 것](#방법론에서-틀렸던-것)
  - [같은 조건이란 세 가지다](#같은-조건이란-세-가지다)
- [12. 파일 지도](#12-파일-지도)
  - [`deliver` — 산출물](#deliver--산출물)
  - [`Tasqra` — 코드 (커밋 안 됨 · 2026-08-14 재작성)](#tasqra--코드-커밋-안-됨--2026-08-14-재작성)
  - [`deliver` — 시험 도구](#deliver--시험-도구)
  - [사용자 PC](#사용자-pc)

<!-- 목차 끝 -->

---

---

## 1. 작업 규칙 — 레포마다 권한이 다르다

| 레포 | 성격 | 커밋·푸시 |
|---|---|---|
| `bobohyeon/deliver` | **개인 레포** · 산출물 문서 | 변경 내용을 보여주고 **승인받은 뒤 에이전트가 직접** 해도 된다 |
| `ParkSehyeon1009/Tasqra` | **팀 레포** · 코드 | **에이전트는 절대 직접 하지 않는다** |

**팀 레포 절차는 항상 이 순서다.**

```
1  사용자가 먼저 레포 상태를 확인한다
2  에이전트가 변경 파일과 실행할 명령어를 정리해서 알려준다
3  사용자가 직접 커밋·푸시한다
```

**팀 공유 파일**(`docker-compose.yml` 등)을 고칠 때는 다른 팀원의 컨테이너나
마이그레이션이 깨지는지 먼저 알린다.

### 그 외 지켜온 규칙

| | 규칙 |
|---|---|
| 1 | 코드 파일 상단에 **3단 주석** — (1) 이 파일의 책임 (2) 다른 파일과의 관계 (3) Spring 비교 |
| 2 | 산출물 문서는 **흑백 무채색.** 강조는 검정 채움 하나. 색·과한 장식 없음 |
| 3 | **기능 ID 만 쓰지 않는다.** `RAG-01` 대신 `문서를 조각으로 자르기(RAG-01)` |
| 4 | 추측으로 단정하지 않는다. **코드·산출물로 확인한 뒤 말하고, 확인 불가면 그렇다고 명시한다** |
| 5 | 사용자는 **자바·Spring 배경.** 파이썬 개념은 Spring 대응물과 비교해 설명한다 |

---

## 2. 지금 어디까지 왔나 — 한 줄

> **임베딩 모델을 `dragonkue/BGE-m3-ko` 로 확정했고 산출물 문서 8개를 커밋·푸시했다.
> Tasqra 의 벡터 DB 마이그레이션(`0011`)은 파일만 써두고 커밋하지 않았다.**

---

## 3. git 상태 — 2026-08-14 오전 확인

### `deliver` (개인 · 산출물)

```
브랜치      docs/design-artifacts-0805
HEAD        be4fae1   chore: 공유용 HTML 6개 (생성물 · 되돌리려면 이 커밋만 revert)
            c098536   docs: RAG 임베딩 문서 6개에 목차 삽입 · 공유용 무채색 변환 추가
            9afebd8   fix: 인수인계 5-2절 정정 — embed-test 는 레포에 있고 낡은 것이다
원격 동기    푸시 완료 · 사용자 PC 도 pull 완료
PR          #2 (open) docs/design-artifacts-0805 -> main
origin/main 1fc1853  Initial commit      <- 주의
```

**주의 — `origin/main` 에 `Initial commit` 하나뿐이다.**
산출물 전부가 PR #2 에만 있고 main 에 머지된 적이 없다.
**의도한 것인지 확인이 필요하다.** 머지할지 브랜치로 계속 갈지 정하지 않았다.

### `Tasqra` (팀 · 코드)

```
로컬 브랜치  feat/rag-document-chunks   <- 소실됐다. 5-1절 참고
origin/main  9d9d72c  Merge pull request #14 from ParkSehyeon1009/dashboard-improvement
             (인수인계 작성 시점의 e23fdb8 = PR #13 에서 한 칸 나아갔다)
마이그레이션 head  20260812_0010  (ocr_content_ranges)   <- 그대로다
0011         아직 비어 있다. 재작성한 파일이 커밋 대기 중
```

**`origin/main` 이 PR #14(dashboard-improvement)로 나아갔지만 `versions/` 는
그대로 `0010` 이 마지막이다.** 즉 `0011` 번호는 아직 우리 것이다. 충돌 없다.

---

## 4. 확정된 것

### 임베딩 모델

| | |
|---|---|
| 컬럼 | **`vector(1024)`** |
| 기본 모델 | **`dragonkue/BGE-m3-ko`** (apache-2.0) |
| 대안 | `nlpai-lab/KURE-v1`(mit) · `Snowflake/snowflake-arctic-embed-l-v2.0`(apache-2.0) |
| 탈락 | `e5` 3종 · `nomic`(768) · `Qwen3-Embedding-0.6B` |
| 근거 | 글자 안 겹치는 질의 **64개 중 41개** · 8개 지표 전부 1위 |
| 한계 | 2위(37)와 **4문항** 차. 안겹침만 보면 유의하지 않다(p=0.22). 전체 133문항으로는 유의(p=0.039) |

**대안 둘은 1위와 통계적으로 구별되지 않는다.** 형식적 대안이 아니라 실제로
바꿔 써도 되는 후보다. 차원이 같아 DB 를 안 건드리고 교체 가능하다.

### 측정 규모

```
문서 10건 · 청크 649개 · 210,078자 · 평균 305자
질의 133개 (겹침 69 · 안겹침 64) · 정답 청크 62개
CPU 전용 · sentence-transformers · float32 · 양자화 없음
512토큰 초과 청크 0개 -> 잘림 없음
```

### 주의 — 채택 모델은 Ollama 공식 라이브러리에 없다

허깅페이스 전용이라 **GGUF + `Modelfile` 로 직접 등록**해야 한다.
`임베딩 생성·저장(RAG-02)` 에 "모델 등록" 작업이 하나 늘어난다.

공식 경로가 꼭 필요하면 `arctic-embed-l-v2.0` 이 `ollama pull` 한 줄로 끝난다.

### 커밋된 산출물 문서 8개

| 파일 | 상태 |
|---|---|
| `관리/RAG_임베딩모델_전체측정표.md` | **신규** — 전 지표 단일 기준표 |
| `관리/RAG_임베딩모델_선정기준표.md` | **신규** — 8기준 × 9후보 · 판정선 · McNemar |
| `관리/RAG_임베딩모델_선정_결과서.md` | 갱신 (버전 1.2) |
| `관리/RAG_임베딩모델_비교분석표.md` | 갱신 |
| `관리/RAG_임베딩모델_선정_한장요약.md` | 갱신 |
| `관리/RAG_임베딩모델_공개벤치마크_인용표.md` | 갱신 |
| `관리/API_계약_RAG_검색_초안.md` | 7-1절 신설 (모델 교체 가능성) |
| `관리/산출물_변경대기_목록.md` | `A-5` 추가 (PDF 공백 소실) |

### 문서를 공유용으로 손본 것 (`c098536` · `be4fae1`)

RAG 문서 6개에 **목차가 하나도 없었다.** 결과서는 2,731줄에 제목 150개다.

| | |
|---|---|
| 목차 | 6개 문서에 삽입. 본문은 안 고쳤다 (추가 281줄 · **삭제 0**) |
| `도구/_add_toc.py` | 신규. 표시 구간만 다시 쓰므로 여러 번 돌려도 겹치지 않는다 |
| `도구/_md2html.py` | `--흑백` 추가 — A4 세로 · 무채색. 기존 꾸밈새는 캡처용(파란색 A4 가로)이었다 |
| `산출물/공유/*.html` | 6개. 생성물이라 커밋을 분리했다. **되돌리려면 `be4fae1` 만 revert** |

원본이 바뀌면 HTML 은 **자동으로 안 바뀐다.** 다시 만들어야 한다.

```bash
python 도구/_add_toc.py
python 도구/_md2html.py 관리/RAG_임베딩모델_*.md --흑백 --출력=산출물/공유
```

목차 링크는 GitHub 앵커 규칙(`·` `—` 제거 · 공백→붙임표)으로 만들었고,
**사용자가 브라우저에서 HTML 이 제대로 나오는 것을 확인했다.**

---

## 5. 커밋 안 된 채로 남아 있는 것 — **가장 중요**

### 5-1. Tasqra 벡터 DB 마이그레이션

> **2026-08-14 사고 기록 — 이전 세션의 파일이 소실됐다.**
> 브랜치 `feat/rag-document-chunks` 는 **로컬 전용이라 푸시되지 않았고**,
> 그 로컬은 이전 세션의 샌드박스였다. **샌드박스는 세션이 끝나면 사라진다.**
> 새 세션에서 `Tasqra` 를 다시 클론해 확인한 결과 `versions/` 의 마지막은
> `20260812_0010` 이고 `app/models/chunk.py` 는 없다. **전량 소실이 맞다.**
>
> **교훈 — 샌드박스의 커밋 안 된 작업은 세션이 끝나면 없어진다.**
> 팀 레포라 에이전트가 푸시할 수 없으므로, **작업한 날 사용자가 바로 커밋**하거나
> 최소한 파일 내용을 `deliver` 쪽에 붙여 남긴다.

**2026-08-14 재작성했다.** 리비전 번호는 실제 작성일에 맞춰 `20260813_0011` ->
**`20260814_0011`** 로 바뀌었다. `down_revision` 은 그대로 `20260812_0010` 이다.

| 상태 | 파일 | 내용 |
|---|---|---|
| 신규 | `backend/migrations/versions/20260814_0011_document_chunks.py` | 확장 + 테이블 + **CHECK 8** + **인덱스 3** |
| 신규 | `backend/app/models/chunk.py` | `DocumentChunk` 엔티티 (3단 주석 있음) |
| 수정 | `backend/app/models/__init__.py` | `DocumentChunk` 등록 |
| 수정 | `backend/app/models/document.py` | `Document.chunks` 관계 추가 |
| 수정 | `backend/requirements.txt` | `pgvector==0.4.1` 추가 |
| 수정 | `docker-compose.yml` | DB 이미지 `postgres:16-alpine` -> `pgvector/pgvector:pg16` |

**CHECK 가 6 -> 8** 인 이유는 결정 B 를 반영해 `content_start` · `content_end` 의
CHECK 두 개가 늘었기 때문이다 (6절 B).

**인덱스가 4 -> 3** 인 이유는 `ix_chunk_doc (document_id, seq)` 를 **뺐기** 때문이다.
`uq_document_chunk_seq (document_id, seq)` 가 **같은 컬럼 순서의 btree 인덱스를
이미 만든다.** 완전히 중복이라 쓰기 비용만 늘었다. (Spring 비교 — JPA 에서
`@UniqueConstraint` 와 `@Index` 를 같은 컬럼에 겹쳐 선언한 것과 같은 상황이다.)

**손대지 않은 파일** — `backend/app/analyzers/structured.py` · `structured_usage.md`
는 그 이전 세션에서 만들어져 있던 것이다. 이번 작업과 무관하다.

### 스키마 (작성된 그대로)

```
document_chunks
  id               BigInteger    PK
  document_id      BigInteger    FK documents.id CASCADE
  seq              Integer       NOT NULL
  page_number      Integer       nullable        출처 표시용
  text             Text          NOT NULL        인용 원문
  char_count       Integer       NOT NULL
  token_count      Integer       NOT NULL
  content_start    Integer       nullable        <- 결정 B. 본문 내 시작 위치
  content_end      Integer       nullable        <- 결정 B. 본문 내 끝 위치
  embedding        Vector(1024)  NOT NULL
  embedding_model  String(100)   NOT NULL        모델 교체용
  embedding_dim    SmallInteger  NOT NULL        CHECK = 1024
  text_version     Integer       NOT NULL        stale 판정
  created_at / updated_at

CHECK 8개   ck_document_chunk_{seq|page_number|char_count|token_count|text_version|embedding_dim}
            ck_document_chunk_{content_start|content_end}      <- 결정 B
UNIQUE      uq_document_chunk_seq (document_id, seq)   btree 인덱스도 이것이 만든다
인덱스 3개   ix_chunk_stale (document_id, text_version)
            ix_chunk_model (embedding_model, document_id)
            ix_chunk_vec  HNSW · vector_cosine_ops
```

**검증한 것** — 파이썬 문법 · 리비전 사슬 `0001 -> 0011` 끊김 없음 · 분기 없음
**검증 안 한 것** — **실제 `alembic upgrade head`.** 샌드박스에 Docker 가 없다

### 5-2. `embed-test` 코드가 레포에 있지만 낡았다

> **정정** — 이 절은 처음에 "레포에 없다" 고 적었는데 **틀렸다.**
> `도구/embed-test/` 는 레포에 있고 파일 16개가 추적되고 있다.
> 정확한 상태는 **"있는데 5차 측정 이전 판이다"** 다.

**결과서가 `check_model_cost.py` · `make_queries_13.py` · `check_encoding.py` 를
근거로 인용하는데 레포에는 그 세 파일이 없다.** 문서가 없는 파일을 가리킨다.

최신은 사용자 PC `C:\dev\embed-test` 에만 있다.

| 파일 | 레포 | PC | 조치 |
|---|---|---|---|
| `run_eval.py` | **로컬 5모델** (+API 4 +self-test) | **로컬 9모델** · `force_fp32` · `max_seq_length` | 덮어쓰기 |
| `convert_inputs.py` | pymupdf 만 | `PDF_OVERRIDES` · pdfplumber 폴백 | 덮어쓰기 |
| `requirements.txt` | `einops`·`pdfplumber` 없음 | 추가 필요 | 덮어쓰기 |
| `queries.csv` | 73문항 | **133문항** | 덮어쓰기 |
| `check_model_cost.py` | **없음** | 있음 | 추가 |
| `make_queries_13.py` | **없음** | 있음 | 추가 |
| `check_encoding.py` | **없음** | 있음 | 추가 |
| `queries_13.csv` | **없음** | 있음 | 추가 |
| `make_chunks.py` · `check_queries.py` | 있음 | 변경 없음(추정) | 확인 후 판단 |

레포 `run_eval.py` 의 모델 목록에 `arctic` · `nomic` · `BGE-m3-ko` · `Qwen3` 가
없는 것이 낡았다는 증거다.

### 5-3. 코퍼스가 레포에 없어 재현이 안 된다

`.gitignore` 가 이렇게 막고 있다. **의도된 설정이고 잘 짜여 있다.**

```
chunks.csv · results.csv · results_detail.csv   생성물, 커밋 안 함
input/*                                        원본 문서 (저작권·용량)
corpus/*  단 *_샘플.md 만 예외                  샘플만 커밋
queries.csv                                    직접 만든 것이라 커밋함
```

**그래서 결과서는 "문서 10건 · 649청크" 를 근거로 쓰는데, 레포만 받은 사람은
그 코퍼스를 재현할 수 없다.** 지금 레포에는 샘플 3개(`산출물내역서_샘플` ·
`입찰공고_샘플` · `회의록_샘플`)뿐이다.

| 선택지 | 내용 |
|---|---|
| A | 그대로 둔다. **재현 불가를 문서에 명시**한다 |
| B | `chunks.csv`(470KB)를 예외로 커밋한다. 텍스트가 있으니 **측정은 재현된다** |
| **C** | **행안부 고시만 커밋한다.** 법령이라 재배포 제약이 없다(저작권법 제7조 제2호) |

**C 를 권한다.** 104청크로 코퍼스의 16%이고, 최소한 고시 기반 질의 60개는
남이 재현할 수 있다. **미결 사항이다.**

---

## 6. 지금 결정 대기 중인 것 다섯 개

| | 물음 | 상태 |
|---|---|---|
| **A** | `deliver` 를 main 에 머지할까 (PR #2), 브랜치로 계속 갈까 | 미정 |
| **B** | **마이그레이션에 `content_start` · `content_end` 두 컬럼을 넣을까** | **넣는다 (2026-08-14) · nullable · 코드로 근거 확인** |
| **C** | `embed-test` 최신 코드로 레포를 덮어쓰기 (5-2절) | 미착수 |
| **D** | 코퍼스를 어디까지 커밋할까 — 권고는 **행안부 고시만** (5-3절) | 미정 |
| **E** | 인수인계 문서가 둘이다 — `작업인수인계.md`(8-11 미니프로젝트) 와 이 문서 | 미정. 권고는 **둘 유지 · 서로 가리키게** |

**끝난 것 — 로컬 클론 두 벌.** `C:\dev\deliver` 와 바탕화면
(`Desktop\BH\project_doc\deliver`) 에 같은 레포가 있어 "PC에 파일이 없다" 는
혼란이 있었다. 내가 예시 경로를 `C:\dev` 로 적은 탓이다.
`C:\dev\deliver` 를 정본으로 쓰고 바탕화면 쪽은 정리했다.

### B 가 왜 먼저인가

**컨테이너를 재생성해서 마이그레이션을 돌린 뒤에 컬럼을 추가하면 또 한 번
재생성해야 한다.** 지금은 커밋 전이라 자유롭게 고칠 수 있다.

### B 의 내용 — 원본 위치 하이라이트

재정님이 리비전 `0010` 으로 `ocr_elements` 에 `content_start` · `content_end` 를
넣었다. `extracted_texts.content` 문자열 안의 위치다.
**청크도 같은 좌표를 가지면 이게 된다.**

```
청크 (content_start, content_end)
   ∩ ocr_elements (content_start, content_end)
   -> 그 청크를 만든 element 들
   -> element 의 x · y · width · height (정규화 좌표)
   -> 원본 페이지 이미지에 검색 근거를 네모로 표시
```

**출처 표시가 "3쪽" 에서 "3쪽 이 위치" 로 올라간다.** 정수 컬럼 두 개 값이다.

**나중에 넣으면 `ALTER` 는 쉽지만 기존 행의 값을 채울 수 없다.** 오프셋을 알려면
재청킹을 해야 한다. `nullable` 로 두면 청킹 규칙이 안 정해져도 무해하다.

### B 의 결론 — 넣는다. 코드에서 확인한 근거 네 개

**추측이 아니라 `origin/main` 코드를 읽고 확인했다.**

| | 확인한 것 | 어디서 |
|---|---|---|
| 1 | `ocr_elements.content_start`·`content_end` 는 `extracted_texts.content` 의 오프셋이고 **nullable** 이다 | `models/document.py:127-128` |
| 2 | **오프셋이 살아 있게 유지된다.** 사람이 OCR 글자를 고치면 본문을 그 자리에서 갈아끼우고 **뒤따르는 element 들의 오프셋을 `delta` 만큼 밀어준다** | `services/document_service.py:_replace_ocr_content` (132-162) |
| 3 | `ocr_elements` 에 `x`·`y`·`width`·`height` 정규화 좌표가 있다 -> 교집합으로 **네모를 그릴 수 있다** | `models/document.py:116-119` |
| 4 | `extracted_texts.text_version` 이 **본문이 실제로 바뀔 때만** 증가한다 -> 청크 stale 판정이 성립한다 | `document_service.py:182, 234` |

**2번이 결정적이다.** 오프셋이 최초 적재 때만 계산되는 값이면 편집 한 번에
전부 틀어져서 하이라이트가 거짓말을 한다. 그런데 실제로는 편집마다 유지된다.
**좌표계가 살아 있으므로 청크가 같은 좌표를 갖는 것이 의미가 있다.**

### B 를 쓸 때 반드시 지킬 것 세 가지 — 확인된 함정

| | 함정 | 대응 |
|---|---|---|
| 1 | **오프셋이 `NULL` 인 element 가 존재한다.** 같은 문자열이 여러 번 나와 위치를 특정 못 하면 `0010` 백필이 `NULL` 을 넣는다. 신규 적재(`extraction_service.py:138-142`)도 `find()` 실패 시 `NULL` | 교집합 조회에 `content_start IS NOT NULL` 을 넣는다. **하이라이트는 "되면 그린다" 이고 보장 기능이 아니다.** 안 되면 페이지 번호까지만 출처로 쓴다 |
| 2 | `is_in_content = false` 인 element 는 본문에 없다. `_replace_ocr_content` 가 이때 기대값을 `""` 로 본다 | 교집합에 `is_in_content = true` 를 넣는다. 7절 2번(제외 element 는 검색에서 뺀다)과 방향이 같다 |
| 3 | **`_replace_ocr_content` 는 `ocr_elements` 만 밀어준다. 청크 오프셋은 안 밀어준다** | 그래서 편집 뒤 청크 오프셋은 낡는다. 다만 `text_version` 이 올라가므로 **낡은 것을 알 수 있다.** 재청킹으로 해결하고, **오프셋을 부분 보정하려 들지 않는다** (7절 5번과 같은 안건) |

**3번을 청크까지 밀어주도록 고치지 않는 이유** — 청크는 여러 element 를 묶은
것이라 경계가 편집으로 깨질 수 있다. 오프셋만 밀면 텍스트와 어긋난다.
**재청킹이 정답이고, 그 트리거는 7절 5번에서 정해야 한다.**

---

## 7. 재정님과 협의할 안건 여섯 개

**청킹 규칙과 테이블 스키마는 분리된다.** 규칙이 바뀌면 행만 다시 만들면 되고
스키마 변경이 아니다. **딱 하나 걸리는 것이 "청킹 규칙이 어떤 컬럼을 요구하는가"** 이고
그것이 아래 3번이다.

| | 안건 | 우리 쪽 입장 |
|---|---|---|
| 1 | 청크 경계를 무엇으로 끊나 — `element_type` 단락? 토큰 수? | **둘 다.** 단락에서 끊고 토큰 수로 상한 |
| 2 | `is_excluded` · `is_in_content` 인 element 를 넣나 | **제외.** 사람이 뺀 것을 검색에 넣으면 검수가 무의미 |
| 3 | **청크가 `content_start` · `content_end` 를 갖나** | **갖자고 제안** (6절 B) |
| 4 | 표를 어떻게 다루나 | **행 단위로 자르고 머리행을 조각마다 복제.** 우리 측정에서 확인 |
| 5 | 텍스트가 갱신되면 재청킹은 누가 트리거하나 | `text_version` 증가로 감지는 되는데 **누가 부르는지 미정** |
| 6 | 청크 최대 토큰 | 채택 모델이 8192 라 여유. 512 상한 모델을 대안으로 둘 거면 512 안쪽 |

**5번이 가장 애매하다.** `OCR 수정 후 텍스트 재조립(REV-17)` 로 텍스트를 고치면
벡터가 낡는다. **작업 큐가 없으므로**(코드 확인 결과 celery·redis 0건) 동기로
돌릴지, 사용자가 버튼을 누를지 정해야 한다.

### 테이블을 따로 빼는 것은 취향이 아니라 제약이다

**pgvector 의 HNSW 인덱스는 "테이블의 한 컬럼" 을 색인한다. 벡터 하나가 행 하나여야 한다.**

| 방식 | 가능한가 |
|---|---|
| `document_chunks` 행마다 벡터 하나 | **가능.** HNSW 색인이 붙는다 |
| `extracted_texts` 에 JSONB 배열로 벡터 묶음 | **불가.** 배열 안의 벡터에는 색인을 못 만든다 |
| 문서당 벡터 하나 | 색인은 되지만 45,000자를 벡터 하나로 요약하는 셈. 근거 스니펫 인용 불가 |

---

## 8. 리비전 번호 상황

```
20260812_0009  extracted_char_sources   재정 · 머지됨
20260812_0010  ocr_content_ranges       재정 · 머지됨 · 현재 head
20260814_0011  document_chunks          보현 · 재작성됨 · 커밋 대기
       0012    단가·원가구분·문서유형 CHECK 9종   보현 예정
```

**`20260813_0011` 이 아니라 `20260814_0011` 이다.** 8월 13일에 쓴 파일이 소실돼
8월 14일에 다시 썼고, 파일명 날짜와 `revision` 문자열을 실제 작성일에 맞췄다
(기존 관례가 파일명 날짜 = `revision` 날짜다). `down_revision` 은 `20260812_0010`.

**리비전 사슬 검증 결과** — `0001 -> 0002 -> ... -> 0010 -> 0011` 끊김 없음 ·
분기 없음 · head 하나. `grep` 으로 11개 파일의 `revision`/`down_revision` 을
전부 대조했다.

**`0010` 을 보현 몫으로 알고 있었는데 재정님이 먼저 썼다.**
그래서 CHECK 9종이 `0012` 로 밀렸다. **리비전을 쓸 때는 항상 `origin/main` 의
`versions/` 를 먼저 확인한다.**

CHECK 명명 선례 — `ck_{테이블단수}_{컬럼}` (예 `ck_ocr_element_content_start`)

---

## 9. 미확인 목록 — 모른 채로 단정하지 않는다

> **리비전 번호 충돌이 세 번 났다 (0010 · 0012 · 0013).**
> 세 번째는 커밋 직전에 잡았다. `origin/main` 의 `versions/` 만 보는 것으로는
> **부족하다** — 머지되지 않은 원격 브랜치가 같은 번호를 쓰고 있을 수 있다.
> `0013` 은 `document-async-processing` 브랜치가 쓰고 있어서 우리 것을 `0014` 로
> 옮겼다. 앞으로는 이렇게 확인한다.
>
> ```bash
> git fetch --all
> for b in $(git branch -r | grep -v HEAD); do
>   echo "--- $b"; git ls-tree --name-only $b backend/migrations/versions/ | tail -3
> done
> ```

> **2026-08-14 해소됨 — 리비전 0011 관련 미확인 3건이 전부 확인됐다.**
> 사용자 로컬에서 컨테이너를 재생성해 실측했다. 아래 세 줄은 기록으로 남긴다.
>
> | 항목 | 결과 |
> |---|---|
> | `alembic upgrade head` | **통과.** `alembic current` = `20260814_0011 (head)` |
> | HNSW 인덱스 | **생성됨.** `ix_chunk_vec hnsw (embedding vector_cosine_ops)` |
> | DB 이미지 교체 후 기동 | **정상.** `pgvector/pgvector:pg16` healthy · api 정상 |
> | 기존 테이블 보존 | **17개 전부 정상.** 기존 16 + `document_chunks` 1 |
> | 실제 스키마 | **컬럼 15 · CHECK 8 · 인덱스 5** — 설계와 일치 |
>
> `down -v` 로 볼륨을 비웠으므로 `0001`~`0011` 전체가 다시 돌아 통과한 것이다.

| 항목 | 상태 |
|---|---|
| ~~`alembic upgrade head` 실제 통과 여부~~ | **확인 완료 (위 표)** |
| ~~HNSW 인덱스 생성~~ | **확인 완료 (위 표)** |
| ~~DB 이미지 교체 시 기존 볼륨~~ | 볼륨을 비우고(`down -v`) 새로 만들어 우회했다. **기존 볼륨을 붙이는 것은 여전히 미확인** |
| DB 이미지 교체 시 기존 볼륨이 붙는지 | 미확인. `alpine -> debian` 이라 로케일 차이로 실패할 수 있다 |
| GGUF(양자화)본 검색 점수 | 미확인. **배포본이 fp32 와 다를 수 있다** |
| Ollama 경로 속도 | 미확인. 후보 전부에 해당 |
| 나머지 6종의 믿을 수 있는 비용 | `check_model_cost.py` 로 3종만 쟀다 |
| 속도 여러 번 측정 중앙값 | 한 번씩만 쟀다. `KURE-v1` 이 47.5~77.7초로 흩어졌다 |
| **`e5-large` 이상 현상 원인** | 1024인데 9종 중 최하위. 가설 둘 기각 |
| `Qwen3` 의 깨끗한 메모리 | 지금 값은 fp32 변환으로 두 벌이 겹쳤다 |
| API 모델 실측 | 안 했다. 하네스는 붙여 뒀다 |
| `BGE-m3-ko` 의 Ollama GGUF 등록 절차 검증 | 미확인 |

---

## 10. 다음 작업 순서

**2026-08-14 갱신.** 1번(결정 B)은 끝났고 마이그레이션은 재작성됐다.
**지금 가장 급한 것은 3번 — 커밋이다. 안 하면 또 소실된다.**

| | 할 일 | 선행 조건 | 상태 |
|---|---|---|---|
| ~~1~~ | ~~결정 B~~ | — | **완료** — 넣는다 (6절) |
| ~~3~~ | ~~마이그레이션 커밋~~ | — | **완료.** `a716fdd`·`cccf3ff` · 브랜치 `feat/rag-document-chunks` 푸시됨 |
| ~~4~~ | ~~컨테이너 재생성 + `alembic upgrade head`~~ | — | **완료.** 9절 표 참고 |
| **10** | **PR (`feat/rag-document-chunks` -> `main`) -> 머지 -> 그다음 공지** | 3·4번 | **다음.** 순서 주의 (아래) |

### 10-1. 공지는 머지 뒤다 — 순서를 틀렸던 기록

처음에 **"공지 먼저, PR 나중"** 이라고 안내했는데 **틀렸다.**

**`main` 에 머지되기 전에는 팀원이 `git pull` 해도 아무것도 받지 않는다.**
변경이 `feat/rag-document-chunks` 에만 있기 때문이다. 그 상태에서
`docker compose down -v` 를 돌리면 **로컬 개발 데이터만 날리고 얻는 것이 없다.**

```
1  PR 올린다        팀원 영향 없음. 각자 main 에 있어서 pull 해도 변화 없다
2  리뷰 · 머지
3  머지 직후 공지    이때부터 pull 이 실제로 변경을 가져온다
```

미리 알릴 것이 있다면 **"pull 하라"가 아니라 "곧 머지된다"는 예고**다.
성격이 다르므로 메시지를 둘로 나눈다.

**예고에 넣을 것** — 리뷰하려고 브랜치를 직접 체크아웃하는 경우에도
`down -v` 가 필요하다는 점. 리뷰어는 로컬에서 돌려볼 수 있다.

> **일반화 — 팀 공유 파일(`docker-compose.yml` 등)의 변경 공지는 "머지 시점"에
> 맞춘다.** 브랜치에만 있는 변경을 두고 실행 명령을 안내하면 팀원이 헛일을 하거나
> 데이터를 잃는다.
| 2 | 재정님과 7절 안건 협의 (특히 5번 재청킹 트리거) | 병행 가능 | 착수 |
| 5 | `embed-test` 최신본을 `도구/embed-test/` 에 덮어쓰기 | 없음 | 사용자 PC 파일 필요 |
| 6 | `requirements.txt` 에 `einops` · `pdfplumber` | 5번 | 대기 |
| 7 | 채택 모델 GGUF 등록 | 없음 | 대기 |
| 8 | 리비전 `0012` (CHECK 9종) | 3·4번 이후 | 대기 |
| 9 | 세현님께 `embed-test` 압축 전달 | 없음 | 대기 |

### 4번 검증 명령 (로컬)

```powershell
docker compose down -v
docker compose up -d --build
docker compose exec api alembic upgrade head
docker compose exec db psql -U postgres -d tasqra -c "\d document_chunks"
```

**개발 데이터가 지워진다.** 컨테이너 재생성은 팀에 공지 완료 상태다.

---

## 11. 이번 시험에서 틀렸던 것 — 반복 방지

**새 세션에서 같은 실수를 하지 않기 위해 남긴다.**

### 철회한 서술 네 개 — 모두 표본이 작을 때 1~4문항 차이로 쓴 결론

| 철회 | 이유 |
|---|---|
| "768 이 384 보다 낮다" | 뒤집혔다. 384(19) < 768(22) |
| "한국어 특화가 효과 있다" | 다국어 `arctic`(37) = `KURE-v1`(37) |
| "재순위로 384 를 쓸 수 있다" | 근거였던 R@10 도약이 사라졌다 |
| "API 를 볼 필요가 줄었다" | 근거가 9종 중 **최하위 모델 하나**였다 |

> **범주 안의 격차가 범주 간 격차보다 크다.** `arctic` 37 대 `e5-large` 18 —
> 둘 다 "다국어" 다. **범주로 고르지 말고 개별 모델을 재야 한다.**

### 방법론에서 틀렸던 것

| | 무엇 | 교훈 |
|---|---|---|
| 1 | `e5-large` 폭락을 **토큰 잘림으로 단정** | 재보니 잘린 청크가 0개였다 |
| 2 | `Qwen3` 70배 느림을 **모델 특성으로 단정** | `bfloat16` 이었다. fp32 로 맞추니 2~2.5배 |
| 3 | 패딩 가설 — **산술이 맞아떨어졌는데 전제가 틀렸다** | 패딩 모양을 찍어보니 `(8, 398)` 이었다 |
| 4 | 메모리 "2차에서 정상 측정됐다" | **가중치보다 작은 값**을 찍고 있었다 |
| 5 | 문서를 늘려 판별하려 함 | 청크 6.7배 늘려도 정답·질의가 그대로면 순위가 안 바뀐다. **질의를 늘려야 갈린다** |
| 6 | 리비전 번호를 `0010` 으로 기억 | 재정님이 먼저 썼다. **항상 origin 확인** |
| 7 | 배포 경로(Ollama 공식 여부)를 **채택 결정 뒤에 확인** | 선정 기준에 넣어야 했다 |

> **계산이 맞는 것과 전제가 맞는 것은 다르다.**
> 그럴듯한 근거를 먼저 만들고 확인을 나중에 하는 실수를 세 번 했다.

### 같은 조건이란 세 가지다

| | 조건 | 이번 시험 |
|---|---|---|
| 1 | **접두어** — 모델이 선언한 값을 그대로 쓴다 | 맞췄다 |
| 2 | **최대 입력 길이** — 잘림이 없는지 토큰으로 확인 | 맞췄다 |
| 3 | **정밀도(dtype)** | **놓쳤다** |

**셋 중 하나만 어긋나도 성능 비교가 아니라 설정 비교가 된다.**

---

## 12. 파일 지도

### `deliver` — 산출물

| 문서 | 무엇을 담나 |
|---|---|
| `관리/RAG_임베딩모델_전체측정표.md` | **숫자만.** 전 지표 · 값이 의심되면 여기로 |
| `관리/RAG_임베딩모델_선정기준표.md` | 성능 외 8기준 × 9후보 · 판정선 · McNemar |
| `관리/RAG_임베딩모델_선정_한장요약.md` | 가장 많이 읽히는 문서 · 용어 · Spring 대응 |
| `관리/RAG_임베딩모델_비교분석표.md` | 실측 + API 사양 + 배포 무게 |
| `관리/RAG_임베딩모델_선정_결과서.md` | 근거 전문 (약 1,700줄) |
| `관리/RAG_임베딩모델_공개벤치마크_인용표.md` | MTEB-ko-retrieval 대조 |
| `관리/API_계약_RAG_검색_초안.md` | 검색 API 계약 · **7절 스키마 · 7-1절 모델 교체** |
| `관리/산출물_변경대기_목록.md` | 미반영 변경 · **A-5 = PDF 공백 소실(제품 결함)** |
| `관리/인수인계.md` | **이 문서** |
| `관리/작업인수인계.md` | 8월 11일 미니프로젝트 인수인계. **이 문서와 별개다** (결정 E) |
| `산출물/공유/*.html` | 위 RAG 문서 6개의 무채색 인쇄본 · 생성물 |

### `Tasqra` — 코드 (커밋 안 됨 · 2026-08-14 재작성)

| 파일 | 무엇 |
|---|---|
| `backend/migrations/versions/20260814_0011_document_chunks.py` | 벡터 DB 마이그레이션 |
| `backend/app/models/chunk.py` | `DocumentChunk` 엔티티 |
| `backend/app/models/__init__.py` | `DocumentChunk` 등록 (수정) |
| `backend/app/models/document.py` | `Document.chunks` 관계 (수정) |
| `backend/requirements.txt` | `pgvector==0.4.1` (수정) |
| `docker-compose.yml` | DB 이미지 교체 (수정) · **팀 공유 파일** |

**커밋 전이다. 세션이 끝나면 또 사라진다** (5-1절 사고 기록).

### `deliver` — 시험 도구

```
도구/embed-test/      레포에 있으나 5차 측정 이전 판이다 (5-2절)
                      신규 3개(check_model_cost · make_queries_13 · check_encoding)가 없다
                      corpus/ 는 샘플 3개뿐 — 실제 10건은 .gitignore 로 제외 (5-3절)
```

### 사용자 PC

```
C:\dev\deliver              deliver 레포 정본 클론 · 브랜치 docs/design-artifacts-0805
C:\dev\Tesqra\Tasqra        Tasqra 레포 클론   <- 바깥 폴더 이름이 "Tesqra" 다
C:\dev\embed-test           시험 도구 최신 일체 — 여기에만 있다 (5-2절)
```

> **경로를 추측해서 사고가 났다 (2026-08-14).**
> 이 문서에 `Tasqra` 의 로컬 경로가 없었는데 `C:\dev\Tasqra` 로 **추측해서**
> 명령을 안내했다. 실제는 `C:\dev\Tesqra\Tasqra` 다. 그 결과 사용자가
> **`deliver` 레포 안에서** `git checkout main` · `git checkout -b
> feat/rag-document-chunks` 를 실행했다.
>
> **두 레포는 형제 폴더가 아니다. 상대경로(`..\deliver\...`)가 성립하지 않는다.
> 패치는 절대경로로 안내한다.**
>
> `deliver` 의 `main` 은 파일이 `README.md` **하나뿐**이다(1fc1853 Initial commit).
> 그래서 `main` 계열 브랜치로 옮기면 작업 폴더에서 산출물 89개가 사라진 것처럼
> 보인다. **git 에는 그대로 있다.** 되돌리는 법은 아래.
>
> ```powershell
> cd C:\dev\deliver
> git checkout docs/design-artifacts-0805
> git branch -d feat/rag-document-chunks
> ```

**시험 도구는 아직 PC 에만 있다.** 레포의 `도구/embed-test/` 는 5차 측정
이전 판이다 (결정 C). 바탕화면 클론은 정리했으므로 `C:\dev\deliver` 하나만 본다.
