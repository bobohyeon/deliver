# -*- coding: utf-8 -*-
"""통합테스트시나리오 CSV 생성 — 기능 위주(계층 연동·데이터 정합성) 관점.

단위테스트는 화면에서 사용자가 수행하는 절차를 확인하고,
통합테스트는 그 결과가 API·DB·파일에 올바르게 반영되는지를 확인한다.
"""
import csv

DATE = "2026. 8. 5."

HEADER = [
    "ID", "기능", "순번", "테스트 항목", "테스트 데이터",
    "시험일자", "시험결과", "Y/N", "결함/이상 내용",
]

GROUPS = [
    ("IT-PDF-01", "문서 업로드 · 텍스트 추출 연동", [
        ("업로드 1건 후 문서·추출 테이블에 함께 기록되는지", "PDF 1건",
         "documents 와 extracted_texts 에 각각 1행 추가"),
        ("문서 종류별 추출 방식이 올바르게 저장되는지", "PDF · DOCX · HWPX · 이미지",
         "TEXT_LAYER / HYBRID / DOCX / HWPX / OCR 중 해당 값 저장"),
        ("페이지 수 초과 시 데이터가 생성되지 않는지", "35페이지 PDF",
         "오류 응답 후 documents 행이 추가되지 않음"),
        ("글자 수 초과 시 데이터가 생성되지 않는지", "장문 DOCX",
         "오류 응답 후 documents 행이 추가되지 않음"),
        ("업로드 파일이 지정 폴더에 저장되는지", "PDF 1건",
         "uploads 폴더에 임의 이름(UUID)으로 저장. 원본 파일명 그대로 저장되지 않음"),
        ("추출 실패 시 저장된 파일이 남지 않는지", "글자가 없는 이미지",
         "오류 응답 후 uploads 폴더에 파일이 남지 않음"),
    ]),

    ("IT-PDF-02", "AI 분석 연동", [
        ("분석 실행 결과가 분석 테이블에 기록되는지", "요약 + 분류 실행",
         "analyses 에 2행 추가"),
        ("분석 메타 정보가 함께 기록되는지", "-",
         "provider · model_name · tokens_in · tokens_out · latency_ms 값 존재"),
        ("재분석 시 기존 이력이 유지되는지", "동일 문서 재분석",
         "기존 행이 덮어써지지 않고 새 행이 추가됨"),
        ("분석 결과가 목록 조회에 반영되는지", "분석 후 목록 조회",
         "카테고리와 요약 미리보기에 최신 분석 결과 표시"),
        ("추출되지 않은 문서의 분석이 차단되는지", "추출 실패 문서",
         "오류 응답 후 analyses 행이 추가되지 않음"),
        ("실제 AI 연동 시 제공자·모델이 기록되는지", "USE_FAKE_AI=false",
         "provider=openai · model_name=gpt-4o-mini 기록, 요약이 실제 문장으로 생성"),
    ]),

    ("IT-PDF-03", "문서 조회 · 검색 연동", [
        ("페이징 계산이 정확한지", "문서 25건 · size=20",
         "1페이지 20건 · 2페이지 5건 · total_pages=2"),
        ("본문 검색에서 공백 표기 차이가 흡수되는지", "검색어 '제안자' 와 '제 안 자'",
         "두 검색의 결과 건수가 동일"),
        ("일치하지 않는 검색어가 걸러지는지", "검색어 'zzzz없는단어'",
         "total=0, 목록 비어 있음"),
        ("공백만 입력한 경우 조건이 적용되지 않는지", "검색어 '   '",
         "전체 건수와 동일 (필터 미적용)"),
        ("카테고리 필터가 분석 결과를 기준으로 동작하는지", "category=보고서",
         "해당 카테고리로 분류된 문서만 반환"),
        ("분석 전 문서의 값이 비어 있는지", "미분석 문서",
         "category 와 summary_preview 가 비어 있음(null)"),
    ]),

    ("IT-PDF-04", "요약 다운로드 연동", [
        ("요약 · 카테고리 · 분류 근거가 모두 포함되는지", "분석 완료 문서",
         "세 항목이 파일에 포함되고 원문은 포함되지 않음"),
        ("파일 내용의 줄 구분이 유지되는지", "-",
         "제목 · 카테고리 · 요약이 각각 줄로 나뉘어 저장"),
        ("한글 파일명이 유지되는지", "한글 파일명 문서",
         "저장된 파일명이 깨지지 않음"),
        ("재분석 후 최신 결과가 반영되는지", "재분석 후 다운로드",
         "가장 최근 분석의 요약이 파일에 반영"),
        ("분석하지 않은 문서의 다운로드가 차단되는지", "분석 전 문서",
         "오류 응답 (409) 반환"),
    ]),

    ("IT-PDF-05", "문서 삭제 연동", [
        ("연관 데이터가 함께 삭제되는지", "분석 완료 문서 1건",
         "documents · extracted_texts · analyses 행이 모두 감소"),
        ("업로드된 원본 파일이 제거되는지", "-",
         "uploads 폴더에서 해당 파일이 사라짐"),
        ("존재하지 않는 문서 삭제가 차단되는지", "id=999999",
         "오류 응답 (404) 반환"),
        ("삭제 결과가 목록 조회에 반영되는지", "-",
         "전체 건수 감소 및 목록에서 사라짐"),
    ]),

    ("IT-PDF-06", "OCR 엔진 비교", [
        ("세 엔진 결과가 모두 반환되는지", "글자가 많은 이미지",
         "paddle · tesseract · easyocr 각각 텍스트·글자 수·소요 시간 반환"),
        ("정답 데이터를 제공하면 실제 정확도가 계산되는지", "이미지 + LabelMe JSON",
         "ground_truth_accuracy 값이 산출됨"),
        ("정답 데이터가 없으면 대용 지표를 쓰는지", "이미지만",
         "avg_confidence 를 정확도로 표시하고 화면에 그 사실을 안내"),
        ("문서 파일도 비교 가능한지", "PDF 1건",
         "첫 페이지를 이미지로 변환해 비교 결과 반환"),
        ("허용되지 않는 형식이 차단되는지", "확장자만 png 인 텍스트 파일",
         "오류 응답 반환"),
    ]),

    ("IT-PDF-07", "오류 응답 일관성", [
        ("오류 응답 형식이 통일되어 있는지", "잘못된 요청",
         "code · message · request_id 를 포함한 동일 형식"),
        ("오류 종류별 상태 코드가 맞는지", "형식·용량·페이지·미분석·미존재 위반",
         "400 / 413 / 409 / 404 등 정의된 상태 코드로 응답"),
        ("화면에 안내 문구가 표시되는지", "AI 분석 실패 유발",
         "서버 메시지와 함께 보조 안내 문구 표시"),
        ("요청 식별자로 서버 로그를 대조할 수 있는지", "오류 응답의 request_id",
         "서버 로그에서 동일한 식별자 확인"),
    ]),

    ("IT-PDF-08", "실행 환경 · 설정", [
        ("컨테이너가 모두 기동되는지", "docker compose up -d",
         "db · api · frontend 3개 컨테이너 정상 상태"),
        ("헬스 체크가 응답하는지", "/health",
         "정상 응답 확인"),
        ("데이터베이스 테이블이 생성되는지", "-",
         "documents · extracted_texts · analyses 3개 테이블 존재"),
        ("설정 변경이 애플리케이션에 반영되는지", "환경 변수 변경 후 컨테이너 재생성",
         "변경한 설정값이 앱에 적용됨"),
        ("설정 파일의 항목이 일치하는지", "-",
         ".env 와 .env.example 의 키 목록이 동일"),
        ("패키지 추가가 컨테이너에 반영되는지", "프론트 의존성 추가 후 재빌드",
         "새 패키지가 컨테이너에서 정상 인식됨"),
    ]),
]

rows = []
for group_id, feature, items in GROUPS:
    for order, (title, data, result) in enumerate(items, start=1):
        rows.append([group_id, feature, order, title, data, DATE, result, "", ""])

with open("통합테스트시나리오.csv", "w", encoding="utf-8-sig", newline="") as f:
    writer = csv.writer(f)
    writer.writerow(HEADER)
    writer.writerows(rows)

print(f"생성: 통합테스트시나리오.csv ({len(rows)}행)")
for group_id, feature, items in GROUPS:
    print(f"  {group_id}  {len(items)}항목  {feature}")
